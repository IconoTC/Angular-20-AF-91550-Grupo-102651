---
serie: Angular Modern (v 17-18-19-20-21-22)
title: Angular 22 -Formularios
file: 04.Forms.md
description: Formularios en Angular 22. Formularios basados en plantillas, formularios reactivos, validación de formularios, manejo de eventos y los nuevos formularios basados en signals de Angular 22
--- 

- [📕Rutas anidadas](#rutas-anidadas)
- [🌐Páginas de la feature Auth](#páginas-de-la-feature-auth)
  - [Páginas](#páginas)
    - [Componentes](#componentes)
    - [🌐LoginPage template](#loginpage-template)
    - [🌐RegisterPage template](#registerpage-template)
    - [👁️‍🗨️Test de las páginas LoginPage y RegisterPage](#️️test-de-las-páginas-loginpage-y-registerpage)
  - [Rutas](#rutas)
    - [👁️‍🗨️Test de las rutas (skipped)](#️️test-de-las-rutas-skipped)
  - [Link al login](#link-al-login)
    - [Ajustes de los tests correspondientes](#ajustes-de-los-tests-correspondientes)
- [📕Rutas con parámetros](#rutas-con-parámetros)
  - [Rutas con parámetros: uso de Observables](#rutas-con-parámetros-uso-de-observables)
  - [Rutas con parámetros: interoperatividad de Observables a Signals](#rutas-con-parámetros-interoperatividad-de-observables-a-signals)
  - [Rutas con parámetros: uso de signals (forma actual)](#rutas-con-parámetros-uso-de-signals-forma-actual)
- [Sub rutas en la feature Auth](#sub-rutas-en-la-feature-auth)
  - [Definición en el router](#definición-en-el-router)
  - [Renderizado condicional en la vista en las páginas LoginPage y RegisterPage](#renderizado-condicional-en-la-vista-en-las-páginas-loginpage-y-registerpage)
  - [Acceso a los parámetros desde LoginPage y RegisterPage usando signals](#acceso-a-los-parámetros-desde-loginpage-y-registerpage-usando-signals)
  - [Componentes](#componentes-1)
    - [👁️‍🗨️Test final de las páginas LoginPage y RegisterPage](#️️test-final-de-las-páginas-loginpage-y-registerpage)
    - [🧿Sidebar en el core](#sidebar-en-el-core)
    - [Uso del sidebar en la página de login](#uso-del-sidebar-en-la-página-de-login)
    - [Cierre del sidebar al navegar a otra página](#cierre-del-sidebar-al-navegar-a-otra-página)
    - [Uso del sidebar en la página de register](#uso-del-sidebar-en-la-página-de-register)
    - [👁️‍🗨️Test del componente SideBar](#️️test-del-componente-sidebar)
- [📕Rutas con estado](#rutas-con-estado)
- [Feedback del login en la feature Auth](#feedback-del-login-en-la-feature-auth)
  - [🧿Componente LoginState con acceso al estado de la navegación](#componente-loginstate-con-acceso-al-estado-de-la-navegación)
    - [👁️‍🗨️Test del componente LoginState](#️️test-del-componente-loginstate)
  - [🧿Componente LoginSimulated](#componente-loginsimulated)
    - [👁️‍🗨️Test del componente LoginSimulated](#️️test-del-componente-loginsimulated)
  - [🌐Cambios en la página LoginPage](#cambios-en-la-página-loginpage)
    - [👁️‍🗨️Actualización del test del componente](#️️actualización-del-test-del-componente)
- [📕Forms](#forms)
- [Classic Forms: samples with LoginForm](#classic-forms-samples-with-loginform)
  - [📕Template driven forms](#template-driven-forms)
  - [🧿LoginFormTdf: Template-driven Form](#loginformtdf-template-driven-form)
    - [NgForm](#ngform)
    - [Controles del formulario y ngModel](#controles-del-formulario-y-ngmodel)
    - [Valor del formulario](#valor-del-formulario)
    - [Validación y feedback](#validación-y-feedback)
    - [Submit y Navegación](#submit-y-navegación)
    - [Resultado final](#resultado-final)
    - [👁️‍🗨️Test del componente LoginFormTdf](#️️test-del-componente-loginformtdf)
  - [🧿LoginFormMdfRx: Model-driven Form (Rx)](#loginformmdfrx-model-driven-form-rx)
  - [📕Template driven forms](#template-driven-forms-1)
    - [Lógica en el componente](#lógica-en-el-componente)
    - [Vinculación al template](#vinculación-al-template)
    - [Valor del formulario](#valor-del-formulario-1)
    - [Validación y feedback](#validación-y-feedback-1)
    - [Submit y Navegación](#submit-y-navegación-1)
    - [Resultado final](#resultado-final-1)
    - [👁️‍🗨️Test del componente LoginFormMdfRx](#️️test-del-componente-loginformmdfrx)
- [📕Signals Forms. Conceptos básicos](#signals-forms-conceptos-básicos)
- [Formulario de login basado en signals](#formulario-de-login-basado-en-signals)
  - [🧿LoginFormSignalsForm: Signal-based Form](#loginformsignalsform-signal-based-form)
    - [Lógica en el componente](#lógica-en-el-componente-1)
    - [Vinculación al template](#vinculación-al-template-1)
    - [Valor del formulario](#valor-del-formulario-2)
    - [Validación y feedback](#validación-y-feedback-2)
  - [📕Submit API en los Signal Forms](#submit-api-en-los-signal-forms)
    - [Directiva formRoot](#directiva-formroot)
    - [Submit y Navegación](#submit-y-navegación-2)
    - [Resultado final](#resultado-final-2)
    - [👁️‍🗨️Test del componente LoginFormSignals](#️️test-del-componente-loginformsignals)
- [📕Custom Controls](#custom-controls)
- [Custom Controls en el login](#custom-controls-en-el-login)
  - [🧿Custom Input](#custom-input)
    - [Estilos del componente](#estilos-del-componente)
    - [👁️‍🗨️Alternativas en el Test del componente Input](#️️alternativas-en-el-test-del-componente-input)
    - [👁️‍🗨️Test unitario del componente Input](#️️test-unitario-del-componente-input)
    - [👁️‍🗨️Test del componente Input en un HostComponent](#️️test-del-componente-input-en-un-hostcomponent)
    - [👁️‍🗨️Test programático integrando el componente Input y el FieldTree](#️️test-programático-integrando-el-componente-input-y-el-fieldtree)
  - [🧿Custom CheckBox](#custom-checkbox)
    - [👁️‍🗨️Test del componente CheckBox](#️️test-del-componente-checkbox)
  - [🧿Custom Button](#custom-button)
    - [👁️‍🗨️Test del componente Button](#️️test-del-componente-button)
  - [🧿LoginPage](#loginpage)
    - [👁️‍🗨️Update Test de LoginPage](#️️update-test-de-loginpage)
  - [🧿LoginFormCustom y su test](#loginformcustom-y-su-test)
- [📕Controles de selección](#controles-de-selección)
- [Signals Forms y controles de selección](#signals-forms-y-controles-de-selección)
  - [🕸️Servicio Auth y tipos para el registro](#️servicio-auth-y-tipos-para-el-registro)
    - [👁️‍🗨️Actualización del test del servicio Auth: register()](#️️actualización-del-test-del-servicio-auth-register)
  - [Páginas u navegación interna en la feature Auth](#páginas-u-navegación-interna-en-la-feature-auth)
    - [🧿Componente RegisterFormSimulate](#componente-registerformsimulate)
      - [👁️‍🗨️Test del componente RegisterFormSimulate](#️️test-del-componente-registerformsimulate)
    - [🌐Actualizaciones de RegisterPage](#actualizaciones-de-registerpage)
      - [👁️‍🗨️Actualización de los tests de RegisterPage](#️️actualización-de-los-tests-de-registerpage)
    - [🌐Actualizaciones de LoginPage](#actualizaciones-de-loginpage)
      - [👁️‍🗨️Actualización del test de LoginPage](#️️actualización-del-test-de-loginpage)
  - [🧿RegisterForm basado en signals (sin controles personalizados)](#registerform-basado-en-signals-sin-controles-personalizados)
    - [Controles de selección del registro](#controles-de-selección-del-registro)
    - [Lógica en el componente](#lógica-en-el-componente-2)
    - [Vinculación al template](#vinculación-al-template-2)
    - [Valor del formulario](#valor-del-formulario-3)
    - [Vinculación al template: problemas con objetos](#vinculación-al-template-problemas-con-objetos)
    - [Validación y feedback](#validación-y-feedback-3)
    - [Validaciones dependientes de otros campos](#validaciones-dependientes-de-otros-campos)
    - [Submit y Navegación](#submit-y-navegación-3)
    - [Resultado final](#resultado-final-3)
    - [👁️‍🗨️Test del componente registerForm](#️️test-del-componente-registerform)
  - [Custom Controls y selección de opciones](#custom-controls-y-selección-de-opciones)
    - [🧿Custom RadioButtons](#custom-radiobuttons)
    - [Validaciones personalizadas](#validaciones-personalizadas)
      - [👁️‍🗨️Test del componente RadioButtons](#️️test-del-componente-radiobuttons)
    - [🧿Custom Select](#custom-select)
      - [👁️‍🗨️Test del componente Select](#️️test-del-componente-select)
  - [🧿RegisterFormCustom usando los controles personalizados](#registerformcustom-usando-los-controles-personalizados)
    - [👁️‍🗨️Test del componente RegisterFormCustom](#️️test-del-componente-registerformcustom)
- [Extras (descartes)](#extras-descartes)
  - [Custom Controls](#custom-controls-1)
    - [🧿Custom CheckBox-Free](#custom-checkbox-free)
      - [Uso desde el formulario](#uso-desde-el-formulario)
    - [Signal model()](#signal-model)
  - [Mejoras en los custom controls](#mejoras-en-los-custom-controls)
    - [🧿Directiva NativeAttrs](#directiva-nativeattrs)


## 📕Rutas anidadas

Podemos distinguir dos formas de organizar las rutas anidadas:

1. **Rutas anidadas independientes**, sin página padre: Se definen rutas hijas que no dependen de una ruta padre, es decir, cada ruta hija es una página independiente y no tiene una página padre que la contenga. 

```ts
{
  path: 'auth',
  children: [
    {
      path: 'login',
      loadComponent: () => import('./features/auth/pages/login-page'),
    },
    {
      path: 'register',
      loadComponent: () => import('./features/auth/pages/register-page'),
    },
  ],
},
```

Las rutas `auth/login` y `auth/register` son rutas hijas independientes, ya que no dependen de una página padre que las contenga. Cada ruta hija es una página independiente y no tiene una página padre que la contenga

2. **SubRutas de una página**: Se definen rutas hijas que se anidan bajo una ruta padre.

```ts
{
  path: 'auth',
  component: () => import('./features/auth/pages/auth-page'),
  children: [
    {
      path: 'login',
      loadComponent: () => import('./features/auth/pages/login-page'),
    },
    {
      path: 'register',
      loadComponent: () => import('./features/auth/pages/register-page'),
    },
  ],
},
```

Las rutas `auth/login` y `auth/register` son rutas hijas de la ruta padre `auth`, que tiene un componente (página) asociado. Esto significa que cuando se navega a una de las rutas hijas, el componente (página) de la ruta padre se renderiza incluyendo su propio router-outlet, que indica donde se renderizará el componente de la ruta hija.


En cualquiera de los caos, la carga de los componentes puede ser lazy, como se muestra en los ejemplos, usando `loadComponent` para cargar los componentes de las rutas hijas de forma diferida. O podría no serlo, cargando los componentes de las rutas hijas de forma inmediata, usando `component` en lugar de `loadComponent`.

## 🌐Páginas de la feature Auth

### Páginas

Creamos dentro de la feature 2 paginas alternativas de login y registro, que se renderizan en el mismo layout que las demás páginas, con el \<h2> habitual y los estilos compartido de las demás páginas.

```shell
ng g c features/auth/pages/login-page --flat --project course-02
ng g c features/auth/pages/register-page --flat --project course-02
```

- Su única función es permitir la carga de los correspondientes componentes login y register
- Se crean como `pages` porque van a cargarse desde el router, y no como componentes que se renderizan dentro de otras páginas
- En consecuencia se exportan como `default` para que puedan cargarse de forma perezosa (lazy loading) desde el router
- No se crean en la raíz de la feature, `features/auth`, sino en la carpeta `pages` dentro de la feature, por su carácter de páginas anidadas
- No existe una página global a la feature, por lo que nuestras páginas se renderizan bajo el router-outlet de la app, y no bajo un router-outlet de la feature. 

#### Componentes

Para cada una de las páginas añadimos un componente inicial, en el que más adelante añadiremos el formulario.

```shell
ng g c features/auth/components/login-form --project course-02
ng g c features/auth/components/register-form --project course-02
```

Por el momento, se limitan a mostrar un párrafo

```ts 
template: `<p>login form</p>`,
template: `<p>register form</p>`,
```

#### 🌐LoginPage template

- incorpora el componente `alc-login-form` dentro de un `alc-card`, y un enlace a la página de registro, utilizando el **routerLink** para navegar a la página de registro.

```ts
@Component({
  selector: 'alc-login-page',
  imports: [RouterLink, LoginForm, Card],
  template: `
    <alc-card>
      <alc-login-form />
    </alc-card>
    <p>Si no tienes cuenta, <a [routerLink]="['/auth', 'register']">regístrate aquí</a>.</p> `,
  styles: ``,
})
export default class LoginPage {}
```

#### 🌐RegisterPage template

- se limita a renderizar el componente `alc-register-form` dentro de un `alc-card`, y un enlace a la página de login, utilizando el **routerLink** para navegar a la página de login.

```ts
@Component({
  selector: 'alc-register-page',
  imports: [RouterLink, RegisterForm, Card],
  template: `
    <alc-card>
      <alc-register-form />
    </alc-card>
    <p>Si ya tienes cuenta, <a [routerLink]="['/auth', 'login']">inicia sesión aquí</a>.</p> `,
  styles: ``,
})
```
#### 👁️‍🗨️Test de las páginas LoginPage y RegisterPage

Comprobamos que se renderiza el componente `alc-login-form` en la página de login y el componente `alc-register-form` en la página de registro, y que los enlaces a las páginas de registro y login funcionan correctamente.

Como el componente incluye navegación, usando el routerLink, es necesario importar router provider, igual que se hace en el fichero de configuraciones, para poder simular la navegación y evitar errores de compilación. 


```ts
describe('LoginPage', () => {
  let component: LoginPage;
  let fixture: ComponentFixture<LoginPage>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      imports: [LoginPage],
      providers: [provideRouter([])],
    }).compileComponents();

    fixture = TestBed.createComponent(LoginPage);
    component = fixture.componentInstance;
    await fixture.whenStable();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });

  it('should have render the component login form', () => {
    const debugForm = fixture.debugElement.query(By.directive(LoginForm));
    expect(debugForm).toBeTruthy();
    expect(debugForm.nativeElement).toBeInstanceOf(HTMLElement);
  });

  it('should have render the link to register-page', () => {
    const debugP = fixture.debugElement.query(By.css('p a'));
    const elementP = debugP.nativeElement as HTMLElement;
    expect(elementP.textContent).toContain('regístrate aquí');
  });
});
```

Prácticamente el mismo test se puede hacer para la página de registro, comprobando que se renderiza el componente `alc-register-form` y que el enlace a la página de login funciona correctamente.

### Rutas

En el router de la aplicación podríamos recoger estas páginas como rutas hijas de la ruta `auth`, que no tiene componente asociado, y que nos permite agrupar las rutas de login y registro bajo un mismo path.

```ts
{
  path: 'auth',
  children: [
    {
      path: 'login',
      loadComponent: () => import('./features/auth/pages/login-page'),
    },
    {
      path: 'register',
      loadComponent: () => import('./features/auth/pages/register-page'),
    },
  ],
},
```

En lugar de eso, nos llevamos las rutas hijas a un router específico de la feature, que nos permite agrupar las rutas de login y registro bajo un mismo path, y que se carga de forma perezosa (lazy loading) desde el router de la aplicación.

```ts auth.routes.ts
import { Routes } from '@angular/router';

export const authRoutes: Routes = [
  {
    path: 'login',
    title: 'Login | Course 02',

    loadComponent: () => import('../pages/login-page'),
  },
  {
    path: 'register',
    title: 'Register | Course 02',

    loadComponent: () => import('../pages/register-page'),
  },
];
```

El router principal de la aplicación se limita a cargar el router de la feature, que se encarga de cargar las rutas hijas de login y registro.

```ts app.routes.ts
import { authRoutes } from './features/auth/routes/auth.routes';

//...
{
  path: 'auth',
  children: authRoutes
},
```

#### 👁️‍🗨️Test de las rutas (skipped)

Seguimos el mismo patrón que en el test del router de la aplicación

- un componente de testing, con un método que puede recibir un parámetro para navegar a la correspondiente ruta
- el router inyectado y espiado
- los test que comprueban que se invoca en el router el proceso de navegación a la ruta correspondiente, con el parámetro correcto.

```ts

@Component({
  template: '',
})
class TestComponent {
  private router = inject(Router);
  goto(route: string) {
    this.router.navigate([route]);
  }
}

describe('Routes', () => {
  let component: TestComponent;
  let fixture: ComponentFixture<TestComponent>;
  let navigateSpy: Mock;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      imports: [
        // RouterTestingModule.withRoutes(routes)
        RouterModule.forRoot(authRoutes),
      ],
    }).compileComponents();

    fixture = TestBed.createComponent(TestComponent);
    component = fixture.componentInstance;

    const router = TestBed.inject(Router);
    navigateSpy = vi.spyOn(router, 'navigate');
    await fixture.whenStable();
  });

  it('should create the Test Components', () => {
    expect(component).toBeTruthy();
  });

  it('should navigate to LoginPage', async () => {
    component.goto('login');
    await fixture.whenStable();
    expect(navigateSpy).toHaveBeenLastCalledWith(['login']);
  });

  it('should navigate to RegisterPage', async () => {
    component.goto('register');
    await fixture.whenStable();
    expect(navigateSpy).toHaveBeenLastCalledWith(['register']);
  });
});
```

Como vimos al hablar de las rutas globales de la aplicación, este test es poco realista y es preferible testar la navegación en las páginas destino del enrutado.

Como hicimos en el caso anterior, comentamos es test con `skip`, para que no se ejecute, y lo dejamos como referencia de cómo se podría hacer un test de navegación a las rutas hijas de la feature.

### Link al login

En el componente `user`, que ya tenemos en el `header`, añadimos un enlace al login utilizando el **routerLink**, que nos permite acceder a la página de login desde cualquier página de la app.

```html
<a [routerLink]="['/auth', 'login']" id="menu-icon" >(Icono SVG)</a>
```

- para reutilizar el componente `menu`
  - eliminamos el tamaño de fuente en la clase `vertical`
  - añadimos una clase con el tamaño de fuente cuando lo usamos en `app` como menu para mobile 

```html
<alc-menu
  slot="menu-vertical"
  [isVertical]="true"
  class="vertical-menu"
  [options]="menuOptions()"
/>
```

#### Ajustes de los tests correspondientes

Si un componente que incluye navegación, usando el routerLink, es necesario importar router provider, igual que se hace en el fichero de configuraciones, para poder simular la navegación y evitar errores de compilación. Ya no se usa el módulo RouterTestingModule en los tests.

Se puede incluir el array original de rutas de la aplicación o cualquier alternativa, incluso un array vacío.

```ts
  providers: [ provideRouter(routes)]
```

Modificamos en este sentido

- user (hemos añadido un enlace al login)
- header (incluye user)

En el test de user, eliminamos la parte correspondiente al manejador del click, que ya no existe.

## 📕Rutas con parámetros

En una ruta pueden indicarse parámetros, que pueden ser obligatorios u opcionales. Los parámetros obligatorios se indican con `:paramName`, mientras que los parámetros opcionales se indican con `:paramName?`. Por ejemplo:

```ts
{
  path: 'user/:id',
  loadComponent: () => import('./features/user/user-page'),
}
```

Cualquier url que incluya `user/` seguido de un valor, como `user/123`, `user/abc`, `user/xyz`, etc., navegará a la misma página, que podrá recoger el valor del parámetro `id` y renderizar el contenido correspondiente.

### Rutas con parámetros: uso de Observables

Tradicionalmente, para recoger el valor de los parámetros de la ruta se ha utilizado el servicio `ActivatedRoute`, que nos permite acceder a los parámetros de la ruta como observables de RxJS, y suscribirnos a los cambios en los mismos. 

El servicio `ActivatedRoute` también nos permite acceder a los parámetros de la ruta de forma síncrona, mediante la propiedad `params.snapshot`, pero no nos permite recibir los cambios en los parámetros cuando la ruta cambia, por lo que en este caso no es útil.

Volviendo al acceso reactivo a los parámetros existen varias opciones, como `params`, `paramMap`, `queryParams` y `queryParamMap`, que nos permiten acceder a los parámetros de la ruta y a los parámetros de consulta (query parameters) de forma reactiva, utilizando observables de RxJS.

La aproximación más propia de RxJs y más simple, sería no hacer privado el servicio y utilizar directamente el observable en la vista, utilizando el pipe `async` para suscribirnos al observable y obtener el valor del parámetro de la ruta.

```ts
export default class LoginPage {
  protected readonly #activatedRouter = inject(ActivatedRoute);

  // constructor() {}
}
``` 

```html
    @let formType = (activatedRouter.params | async)?.['formType'] || 'tdf'">

    @if (formType === 'tdf') {
      <alc-login-form-tdf />
    } @elseif (formType === 'mdf-rx') {
      <alc-login-form-mdf-rx />
    } @elseif (formType === 'signals') {
      <alc-login-form-signals />
    }
```

La directiva `@let` nos permite declarar una variable local en la vista, que se inicializa con el valor del observable `activatedRouter.params`, y que se actualiza automáticamente cuando el valor del observable cambia.

La gran ventaja de usar el pipe `async` es que no necesitamos suscribirnos al observable ni cancelar la suscripción cuando el componente se destruye, ya que Angular se encarga de ello automáticamente.

### Rutas con parámetros: interoperatividad de Observables a Signals

Usando el servicio `ActivatedRoute`, tanto con `params` como con `paramMap`, podemos acceder a los parámetros de la ruta, y suscribirnos a los cambios en los mismos, para actualizar el valor de la signal `formType` y renderizar el formulario correspondiente según el valor la signal.

```ts
type FormType = 'tdf' | 'mdf-rx' | 'signals';

export default class LoginPage {
  readonly #activatedRouter = inject(ActivatedRoute);
  readonly #destroyRef = inject(DestroyRef);
  protected readonly formType = signal<FormType>('tdf');

  constructor() {
    // this.#activatedRouter.paramMap
    this.#activatedRouter.params
      .pipe(takeUntilDestroyed(this.#destroyRef))
      .subscribe((params) => {
        this.formType.set(params['formType'] || 'tdf');
      });
  }
}
```

El operador de observables `takeUntilDestroyed` nos permite cancelar la suscripción cuando el componente se destruye, evitando posibles fugas de memoria. 

### Rutas con parámetros: uso de signals (forma actual)

Las versions más recientes de Angular permiten el uso de signals para acceder a los parámetros de la ruta, lo que nos permite simplificar aún más el código y evitar el uso de observables y suscripciones.

Para ello es necesario configurar el provider `Router` con la opción `withComponentInputBinding()`, que nos permite acceder a los parámetros de la ruta desde cualquier componente de la aplicación, sin necesidad de pasar los parámetros como inputs.   

```ts angular.config.ts
providers: [provideRouter(appRoutes, withComponentInputBinding())];
```

## Sub rutas en la feature Auth

### Definición en el router

Añadimos una nueva ruta de login y registro para que acepten un parámetro `formType`, que nos permite renderizar distintos formularios según el valor del parámetro.

```ts
 {
    path: 'login',
    title: 'Login | Course 02',
    redirectTo: 'login/tdf',
  },
  {
    path: 'login/:formType',
    title: 'Login | Course 02',
    loadComponent: () => import('../pages/login-page'),
  },
  {
    path: 'register',
    title: 'Register | Course 02',
    redirectTo: 'register/monolithic',
  },
  {
    path: 'register/:formType',
    title: 'Register | Course 02',
    loadComponent: () => import('../pages/register-page'),
  },
```

La redirección de la ruta `login` a `login/tdf` y de la ruta `register` a `register/monolithic` nos permite definir un valor por defecto para el parámetro `formType`, que se utiliza cuando no se especifica un valor en la url.

Al añadir nuevas rutas, debemos también incorporarlas en el test de las rutas, para comprobar que se navega correctamente a la ruta correspondiente según el valor del parámetro.

```ts
it('should navigate to LoginPage with params', async () => {
  component.goto('login/tdf');
  await fixture.whenStable();
  expect(navigateSpy).toHaveBeenLastCalledWith(['login/tdf']);
});
// ...
it('should navigate to RegisterPage with monolithic params', async () => {
  component.goto('register/monolithic');
  await fixture.whenStable();
  expect(navigateSpy).toHaveBeenLastCalledWith(['register/monolithic']);
});
//...
```

### Renderizado condicional en la vista en las páginas LoginPage y RegisterPage

En lugar de renderizar un único formulario, podemos renderizar distintos formularios según el valor del parámetro `formType`, utilizando la directiva `@if` para renderizar el formulario correspondiente.

En el caso del login, renderizamos un formulario distinto según el valor del parámetro `formType`, que puede ser `tdf` (template driven form), `mdf-rx` (model driven form con RxJS) o `signals` (signals form).

```html
@if (!formType() ||formType() === 'tdf') {
  <p>Ejemplo de Template Driven Form</p> 
  <alc-card>
     <!-- <alc-login-form /> -->
     <p>Template Driven Form is not implemented yet.</p>
  </alc-card>
} @else if (formType() === 'mdf-rx') {
  <p>Ejemplo de Model Driven Form (RxJs)</p>
  <alc-card>
    <!-- <alc-login-form /> -->
    <p>Model Driven Form (RxJs) is not implemented yet.</p>
  </alc-card>
} @else if (formType() === 'signals') {
  <p>Ejemplo de Signals Form</p>
  <alc-card>
    <!-- <alc-login-form /> -->
    <p>Signals Form is not implemented yet.</p>
  </alc-card>
  <p>Si no tienes cuenta, <a [routerLink]="['/auth', 'register']">regístrate aquí</a>.</p>
}
```

En el caso del registro, renderizamos un formulario distinto según el valor del parámetro `formType`, que puede ser `monolithic` (monolithic form) o `custom-components` (custom components form).

```html
@if (!formType() || formType() === 'monolithic') {
  <p>Ejemplo de registro en un solo componente</p>
  <alc-card>
    <!-- <alc-register-form /> -->
    <p>Monolithic Form is not implemented yet.</p>
  </alc-card>
} @else if (formType() === 'custom-components') {
  <p>Ejemplo de registro con componentes personalizados</p>
  <alc-card>
    <!-- <alc-register-form /> -->
    <p>Custom Components Form is not implemented yet.</p>
  </alc-card>
}
<p>Si ya tienes cuenta, <a [routerLink]="['/auth', 'login']">inicia sesión aquí</a>.</p>
```

### Acceso a los parámetros desde LoginPage y RegisterPage usando signals

Recordemos que hemos configurado el provider `Router` con la opción `withComponentInputBinding()`, que nos permite acceder a los parámetros de la ruta desde cualquier componente de la aplicación, sin necesidad de pasar los parámetros como inputs.

En el componente solo necesitamos definir el parámetro como un input, y Angular se encargará de inyectar el valor del parámetro de la ruta en el componente.

```ts
type LoginFormType = 'tdf' | 'mdf-rx' | 'signals';

export default class LoginPage {
 protected readonly formType = input<LoginFormType>('tdf')
}

type RegisterFormType = 'monolithic' | 'custom-components';

export default class RegisterPage {
  protected readonly formType = input<RegisterFormType>('monolithic');
}
```

De esta forma no necesitamos inyectar el servicio ActivatedRoute y evitamos toda interacción con los elementos de RxJS.

En algunos casos es necesario añadir una función de transformación para validar el valor del parámetro y devolver un valor por defecto en caso de que el valor del parámetro no sea válido, como sucede con la '' (cadena vacía) que puede llegar desde una url sin parámetros.

```ts
type LoginFormType = 'tdf' | 'mdf-rx' | 'signals';

export default class LoginPage {
 protected readonly formType = input<LoginFormType, LoginFormType>('tdf', {
    transform: (value: string | null | undefined) => {
      if (value === 'tdf' || value === 'mdf-rx' || value === 'signals') {
        return value;
      }
      return 'tdf';
    },
  });
}
```

En nuestro caso no es necesario, ya que hemos definido una redirección de la ruta `login` a `login/tdf`, y de la ruta `register` a `register/monolithic`, que nos permite definir un valor por defecto para el parámetro `formType`, que se utiliza cuando no se especifica un valor en la url.

Podemos comprobar el funcionamiento de la renderización condicional accediendo a las rutas `login/tdf`, `login/mdf-rx` y `login/signals`, en el navegador y comprobando que se renderiza la vista correspondiente según el valor del parámetro `formType`.

Lo mismo hacemos en el caso del componente RegisterPage, que también tiene un parámetro de ruta `formType` que nos permite renderizar distintos formularios de registro según el valor del parámetro.

```ts
type RegisterFormType = 'monolithic' | 'custom-components';

export default class RegisterPage {
  protected readonly formType = input<RegisterFormType, RegisterFormType>('monolithic', {
    transform: (value: string | null | undefined) => {
      if (value === 'monolithic' || value === 'custom-components') {
        return value;
      }
      return 'monolithic';
    },
  });
}
```

De nuevo, podemos comprobar el funcionamiento de la renderización condicional accediendo a las rutas `register/monolithic` y `register/custom-components`, en el navegador y comprobando que se renderiza la vista correspondiente según el valor del parámetro `formType`.

### Componentes

Para mantener las páginas lo más simples posibles, implicadas únicamente en el proceso de navegación, creamos los componentes de login-form... y register-form, que se encargan de la lógica de negocio y de la presentación de los formularios.

Del primero de ellos haremos tres versiones, una basada en template-driven forms, otra en model-driven forms y otra en signals, para mostrar distintos ejemplos de formularios en Angular. Del registro las versiones serán dos, una monolítica y otra basada en componentes personalizados.

```shell
ng g c features/auth/components/login-form-tdf --project course-02
ng g c features/auth/components/login-form-mdf-rx --project course-02
ng g c features/auth/components/login-form-signals --project course-02
ng g c features/auth/components/register-form --project course-02
ng g c features/auth/components/register-form-custom --project course-02
```

Incorporamos los componentes en las páginas, con el renderizado condicional que vimos antes: 

- La página de login renderiza condicionalmente el componente login-form adecuado, junto con un enlace a la página de registro
- La página de registro renderiza el componente register-form adecuado, junto con un enlace a la página de login

#### 👁️‍🗨️Test final de las páginas LoginPage y RegisterPage

Como en los anteriores test de las páginas de la aplicación simulamos la navegación utilizando `RouterTestingHarness`, y comprobamos que se renderiza el componente correspondiente según el valor del parámetro `formType`, y que los enlaces a las páginas de registro y login funcionan correctamente.

En la preparación definimos la ruta en el provider `provideRouter(routes, withComponentInputBinding()` para que los parámetros de la ruta se inyecten en los componentes como inputs, y creamos un `RouterTestingHarness` para poder simular la navegación 

```ts
let component: LoginPage;
let harness: RouterTestingHarness;
let debugHarness: DebugElement;

beforeEach(async () => {
  await TestBed.configureTestingModule({
    imports: [LoginPage],
    providers: [
      provideRouter(authRoutes),
      withComponentInputBinding()
    ],
  }).compileComponents();

  harness = await RouterTestingHarness.create();
  debugHarness = harness.fixture.debugElement;

  await harness.fixture.whenStable();
});
```

Por ejemplo, para la página de login con el parámetro `formType` igual a `tdf`, el test sería el siguiente:

```ts
it('should have render the view for "tdf"', async () => {
  await harness.navigateByUrl('/login/tdf', LoginPage);
  await harness.fixture.whenStable();
  component = debugHarness.query(By.directive(LoginPage)).componentInstance;

  const debugForm = debugHarness.query(By.directive(LoginForm));
  expect(debugForm).toBeTruthy();
  expect(debugForm.nativeElement).toBeInstanceOf(HTMLElement);
  expect(component['formType']()).toBe('tdf');
});

it('should have render the link to register-page', () => {
  const debugP = debugHarness.query(By.css('p a'));
  const elementP = debugP.nativeElement as HTMLElement;
  expect(elementP.textContent).toContain('regístrate aquí');
});
```

#### 🧿Sidebar en el core

Para mostrar distintos ejemplos de un mismo formulario, en la pagina de login tendremos un menú en un sidebar, por lo que vamos añadir en el core un componente `sidebar` que nos permita renderizar un menú lateral en cualquier página de la app.

```shell
ng g c core/components/sidebar --project course-02
```

- En l0s componentes Header y Footer eliminamos los margenes, que eran innecesarios y ahora se hacen visibles al usar el sidebar

En el nuevo componente añadimos un input `isOpenFromParent` que nos permite definir el estado inicial del sidebar, abierto o cerrado, desde la página en la que nos encontremos.


```ts
export class SideBar {
  readonly isOpenFromParent = input<boolean>(false);
  protected readonly isOpen = linkedSignal(() => this.isOpenFromParent());
  protected readonly size = signal('3rem');

  protected toggle(): void {
    this.isOpen.set(!this.isOpen());
  }
}
```

- Utilizamos una linked signal `isOpen` para controlar el estado del sidebar, abierto o cerrado, que depende del input `isOpenFromParent` para definir el estado del sidebar, que puede ser abierto o cerrado según la página en la que nos encontremos.
- En el template del componente utilizamos `@if` para renderizar el contenido del sidebar sólo cuando está abierto, y un svg como icono para abrir o cerrar el sidebar.
- El método que responde al click en el icono de abrir/cerrar el sidebar, modifica el valor de la linked signal `isOpen`, que por su naturaleza permite recibir el valor del input `isOpenFromParent` desde el componente padre, y al mismo tiempo, es modificable en nuestro componente, lo que nos permite controlar el estado del sidebar desde la página en la que nos encontremos.

- La proyección de contenido se realiza mediante el uso de `ng-content`, lo que nos permite renderizar cualquier contenido dentro del sidebar, sin necesidad de definirlo en el componente. 

```html
    <button class="icons" (click)="toggle()">
      @if (isOpen()) {
        <svg
          id="close-icon"
          xmlns="http://www.w3.org/2000/svg"
          viewBox="0 0 640 640"
          [attr.width]="size()"
          [attr.height]="size()"
          fill="currentColor"
        >
          <!--!Font Awesome Free v7.2.0 by @fontawesome - https://fontawesome.com License - https://fontawesome.com/license/free Copyright 2026 Fonticons, Inc.-->
          <path
            d="M144 480C144 488.8 151.2 496 160 496L480 496C488.8 496 496 488.8 496 480L496 160C496 151.2 488.8 144 480 144L160 144C151.2 144 144 151.2 144 160L144 480zM160 544C124.7 544 96 515.3 96 480L96 160C96 124.7 124.7 96 160 96L480 96C515.3 96 544 124.7 544 160L544 480C544 515.3 515.3 544 480 544L160 544zM224 320C224 313.3 226.8 307 231.7 302.4L343.7 198.4C350.7 191.9 360.9 190.2 369.6 194C378.3 197.8 384 206.5 384 216L384 424C384 433.5 378.3 442.2 369.6 446C360.9 449.8 350.7 448.1 343.7 441.6L231.7 337.6C226.8 333.1 224 326.7 224 320z"
          />
        </svg>
      } @else {
        <svg
          id="open-icon"
          xmlns="http://www.w3.org/2000/svg"
          viewBox="0 0 640 640"
          [attr.width]="size()"
          [attr.height]="size()"
          fill="currentColor"
        >
          <!--!Font Awesome Free v7.2.0 by @fontawesome - https://fontawesome.com License - https://fontawesome.com/license/free Copyright 2026 Fonticons, Inc.-->
          <path
            d="M496 160C496 151.2 488.8 144 480 144L160 144C151.2 144 144 151.2 144 160L144 480C144 488.8 151.2 496 160 496L480 496C488.8 496 496 488.8 496 480L496 160zM480 96C515.3 96 544 124.7 544 160L544 480C544 515.3 515.3 544 480 544L160 544C124.7 544 96 515.3 96 480L96 160C96 124.7 124.7 96 160 96L480 96zM416 320C416 326.7 413.2 333 408.3 337.6L296.3 441.6C289.3 448.1 279.1 449.8 270.4 446C261.7 442.2 256 433.5 256 424L256 216C256 206.5 261.7 197.8 270.4 194C279.1 190.2 289.3 191.9 296.3 198.4L408.3 302.4C413.2 306.9 416 313.3 416 320z"
          />
        </svg>
      }
    </button>
    @if (isOpen()) {
      <div class="content">
        <ng-content />
      </div>
    }
```

La posición del sidebar es absoluta, por lo que se renderiza sobre el contenido de la página, y no desplaza el contenido de la página cuando se abre o se cierra.

```css
:host {
  background-color: var(--color-background-primary);
  display: block;
  position: absolute;
  top: 0;
  left: 0;
  z-index: 10;
  width: fit-content;
  height: 100%;
  &:has(#open-icon) {
    width: 50px;
  }
}
  button {
    background-color: transparent;
    border: none;
    margin: 0 0 1rem 0;
    padding: 0;
    width: 100%;
  }
.icons {
  background-color: color-mix(in srgb, var(--color-primary) 10%, transparent);
  color: var(--color-primary);
  display: flex;
  justify-content: flex-end;
  align-items: center;
}
.content {
  padding-inline-start: 2rem;
  padding-inline-end: 3rem;
}
```

Los iconos svg se incluyen en una etiqueta \<button> que luego pierde su aspecto de botón en el css pero que conserva todas las características de accesibilidad propia de los botones, como el foco y la navegación con teclado.

#### Uso del sidebar en la página de login

Usamos el sidebar en la página de login, para mostrar un menú lateral con los distintos tipos de formularios que podemos renderizar en esa página, utilizando el componente `alc-side-bar` y el componente `alc-menu` que también nos ha permitido crear el menu principal.

En el siguiente ejemplo aparece el acceso a los parameters basados en observables, pero sería lo mismo si actualizamos la página para que utilice signals para acceder a los parámetros de la ruta.

```ts
type FormType = 'tdf' | 'mdf-rx' | 'signals';

@Component({
  selector: 'alc-login-page',
  imports: [RouterLink, LoginFormTdf, LoginFormMdfRx, LoginFormSignals, Card, SideBar, Menu],
  template: `
    <alc-side-bar [isOpenFromParent]="isOpenSideBar" >
      <alc-menu class="side-bar-menu" [isVertical]="true" [options]="menuOptions()" />
    </alc-side-bar>
    ...
    `
})
export default class LoginPage {
  readonly #router = inject(Router);
  readonly destroyRef = inject(DestroyRef);
  protected readonly formType = input<FormType>();

  protected readonly isOpenSideBar = signal(false);
  protected readonly menuOptions = signal<MenuOption[]>([
    {
      label: 'Template Driven Form',
      path: '/auth/login/tdf',
    },
    {
      label: 'Model Driven Form (RxJs)',
      path: '/auth/login/mdf-rx',
    },
    {
      label: 'Signals Form',
      path: '/auth/login/signals',
    },
  ]);
}
```

- la signal `isOpenSideBar` nos permite controlar el estado del sidebar, abierto o cerrado, desde la página de login, y se pasa como input al componente `alc-side-bar` para que pueda modificar su estado.
- como proyección de contenido, el sidebar renderiza un menú lateral con las distintas opciones de formularios que podemos renderizar en la página de login, utilizando el componente `alc-menu` y pasando como input la signal `menuOptions`, que contiene las distintas opciones del menú.

#### Cierre del sidebar al navegar a otra página

Finalmente, en el sidebar, añadimos una opción extra, que pueda no incluirse en la demo durante el curso. Como la proyección es un menu, añadimos un efecto que nos permite cerrar el sidebar cuando se navega a otra página. 

Como la navegación se realiza a través del router de Angular, que utiliza observables de RxJs, podemos suscribirnos a los **eventos del router** y cerrar el sidebar cuando se produce un evento de navegación `NavigationStart`. Aplicamos el mismo patrón que en el caso de los parámetros de la ruta, utilizando el operador `takeUntilDestroyed` para cancelar la suscripción cuando el componente se destruye.

```ts
export class SideBar  {
 // Reactivity to navigation

  readonly destroyRef = inject(DestroyRef);
  readonly #router = inject(Router);

  constructor() {
    this.#router.events
      .pipe(takeUntilDestroyed(this.destroyRef))
      .subscribe((event) => {
        if (event instanceof NavigationStart) {
          this.isOpen.set(false);
        }
      });
  }
}
```

#### Uso del sidebar en la página de register

Seguiremos el mismo patrón en la página de registro, para mostrar un menú lateral con los distintos tipos de formularios que podemos renderizar en esa página, utilizando el componente `alc-side-bar` y el componente `alc-menu` que también nos ha permitido crear el menu principal.

En este caso las opciones serán dos:

- monolithic, que renderiza un formulario de registro completo, con todos los campos en una sola página
- custom-components, que renderiza un formulario de registro utilizando componentes personalizados para cada campo del formulario.

```html
@if (!formType() || formType() === 'monolithic') {
  <p>Ejemplo de registro en un solo componente</p>
  <alc-card>
    <alc-register-form />
  </alc-card>
} @else if (formType() === 'custom-components') {
  <p>Ejemplo de registro con componentes personalizados</p>
  <alc-card>
    <alc-register-form-customs />
  </alc-card>
}
```

El componente completo, usando signal para acceder a los parámetros quedaría como sigue

```ts
export class RegisterPage {
  protected readonly formType = input<RegisterFormType>('monolithic');
  protected readonly isOpenSideBar = signal(false);
  protected readonly menuOptions = signal<MenuOption[]>([
    {
      label: 'Monolithic Form',
      path: '/auth/register/monolithic',
    },
    {
      label: 'Custom Components',
      path: '/auth/register/custom-components',
    },
  ]);
}
```

#### 👁️‍🗨️Test del componente SideBar 

Comprobamos que 

- el componente se renderiza correctamente y abierto
- puede abrirse cuando esta cerrado
- puede cerrarse cuando está abierto

```ts
it('should create and initialize as open', () => {
  expect(component).toBeTruthy();
  expect(component['isOpen']()).toBe(false);
  // Close icon NOT visible because isOpen is false
  fixture.detectChanges();
  const closeIcon = fixture.nativeElement.querySelector('#close-icon');
  expect(closeIcon).toBeNull();
  // Open icon visible because isOpen is false
  const openIcon = fixture.nativeElement.querySelector('#open-icon');
  expect(openIcon).toBeTruthy();
});

it('should toggle state to OPEN when clicked', async () => {
  const button = fixture.nativeElement.querySelector('button');
  button.click();
  fixture.detectChanges();
  await fixture.whenStable();
  expect(component['isOpen']()).toBe(true);
});

it('should toggle state to CLOSED when is OPEN and clicked', async () => {
  fixture.componentRef.setInput('isOpenFromParent', true);
  fixture.detectChanges();
  await fixture.whenStable();
  expect(component['isOpen']()).toBe(true);
  const button = fixture.nativeElement.querySelector('button');
  button.click();
  fixture.detectChanges();
  await fixture.whenStable();
  expect(component['isOpen']()).toBe(false);
});
```

- se cierra al navegar a otra página

```ts 

beforeEach(async () => {
    await TestBed.configureTestingModule({
      imports: [SideBar],
      providers: [
        provideRouter([
          {
            path: 'some-route',
            redirectTo: '',
          },
        ]),
      ],
    }).compileComponents();

    fixture = TestBed.createComponent(SideBar);
    component = fixture.componentInstance;
    await fixture.whenStable();
  });

it('should close when navigation starts', async () => {
  const router = TestBed.inject(Router);
  fixture.componentRef.setInput('isOpenFromParent', true);
  fixture.detectChanges();
  await fixture.whenStable();
  expect(component['isOpen']()).toBe(true);
  // Simulate navigation an generate NavigationStart event
  router.navigateByUrl('/some-route');
  fixture.detectChanges();
  await fixture.whenStable();
  expect(component['isOpen']()).toBe(false);
});
```

Por otro lado comprobamos que el contenido proyectado se renderiza dentro del sidebar, para lo que necesitamos un `test host component` que nos permita proyectar contenido dentro del sidebar, y comprobar que se renderiza correctamente.

```ts 
@Component({
  selector: 'alc-test-host',
  imports: [SideBar],
  template: ` <alc-side-bar [isOpenFromParent]="true"> Text content </alc-side-bar> `,
})
class TestHostComponent {}

describe('SideBar with TestHostComponent', () => {
  let hostFixture: ComponentFixture<TestHostComponent>;
  let hostComponent: TestHostComponent;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      imports: [SideBar, TestHostComponent],
    }).compileComponents();

    hostFixture = TestBed.createComponent(TestHostComponent);
    hostComponent = hostFixture.componentInstance;
    await hostFixture.whenStable();
  });

  it('should create the host component and sidebar', () => {
    expect(hostComponent).toBeTruthy();
    const sideBarElement = hostFixture.nativeElement.querySelector('alc-side-bar');
    expect(sideBarElement).toBeTruthy();
  });

  it('should render the sidebar content', () => {
    const sideBarElement = hostFixture.nativeElement.querySelector('alc-side-bar');
    expect(sideBarElement.textContent).toContain('Text content');
  });
});
```

## 📕Rutas con estado

Como ya sabemos, el servicio `Router`, ademas de gestionar las rutas al ser instanciado con ellas, permite refinar determinadas operaciones relacionadas con las rutas

Al navegar a una ruta concreta (`navigate()` o `navigateByUrl()`) se puede acceder a su estado, capaz de almacenar información todo lo compleja que sea necesario

```ts
 this.#router.navigate(['/auth/login/tdf'], {
    state: { user: 'john_doe', role: 'admin', token: 'abc123' },
  });
```

Para acceder a este estado en el componente destino de la ruta, debemos inyectar el servicio `Router` y aprovechar dos de sus características: 

- es posible suscribirse a los eventos de navegación usando el método `events()`, que devuelve un observable. 

Estos eventos se desencadenan en las distintas etapas del ciclo de vida de la navegación 
  - `NavigationStart`, cuando se inicia la navegación y contiene la URL solicitada
  - `RoutesRecognized`, cuando el router determina qué ruta coincide con la URL y contiene la información del estado de la ruta
  - `GuardsCheckStart`, cuando comienza la fase de verificación de los guardias. El router evalúa los guardias de ruta como `canActivate` y `canDeactivate`
  - `GuardsCheckEnd`, cuando se completa la evaluación de los guardias y contiene el resultado (permitido/denegado)
  - `ResolveStart`, cuando comienza la fase de resolución de datos y los resolvers de la ruta comienzan a obtener datos
  - `ResolveEnd`, cuando se completa la resolución de datos y todos los datos requeridos
  - `NavigationEnd`, cuando la navegación se completa con éxito y el router actualiza la URL
  - `NavigationSkipped`, cuando el router omite la navegación (por ejemplo, si es la misma URL)
O en caso de error,
  - `NavigationCancel`, cuando el router cancela la navegación, a menudo debido a un guardia que devuelve false
  - `NavigationError`, cuando la navegación falla, por ejemplo, debido a rutas inválidas o errores de resolvers.

- desde la etapa `RoutesRecognized` es posible obtener información sobre el estado delas gracias al método `currentNavigation()`. Esta información deja de estar disponible una vez que se completa el proceso de navegación, por lo que es importante acceder a ella en el momento adecuado.

```ts
  readonly routerState = signal<NavigationState | null>(null);
  readonly #router = inject(Router);
  
  constructor() {
    this.#router.events
      .pipe(takeUntilDestroyed(this.destroyRef))
      .subscribe((event) => {
        if (event instanceof RoutesRecognized) {
          const navigation = this.#router.getCurrentNavigation();
          if (navigation && navigation.extras.state) {
            this.routerState.set(navigation.extras.state);
            console.log('Navigation state:', this.routerState());
          }
        }
      });
  }
```

## Feedback del login en la feature Auth

Para ver un ejemplo de uso de estas características, añadiremos:

- un nuevo componente `login-state` que se renderizará en la página de login,
- un nuevo componente `login-simulated`, incluido entre las opciones de `login-page` 
- este último simula un servicio de login, y nos permite navegar a la ruta `/auth/login-state` pasando un estado con información sobre el resultado del login
- acceso a ese estado desde el componente `login-state` para mostrar la información en la vista

Crearemos los componentes que necesitamos:


```shell
ng g c features/auth/components/login-state --project course-02
ng g c features/auth/components/login-simulated --project course-02
```

### 🧿Componente LoginState con acceso al estado de la navegación

Recibe como input el estado de la navegación, obtenido desde el router en el componente padre (LoginPage), y  muestra en la vista información sobre los login con éxito y los login fallidos.

```ts
Component({
  selector: 'alc-login-state',
  imports: [],
  template: `
    @if (navState().hasOwnProperty('email')) {
      <p>Login exitoso.</p>
      <p>Bienvenido, {{ navState()['email'] }}!</p>
    } @else {
      <p>Error en el login.</p>
      <p>{{ navState()['error'] || 'No se recibió información del usuario.' }}</p>
    }
  `,
  styles: ``,
})
export class LoginState {
  readonly navState = input<Record<string, unknown>>({});
}
```

#### 👁️‍🗨️Test del componente LoginState

Damos valor al input `navState` y comprobamos que se renderiza correctamente la información en la vista, según el valor del input.

```ts
beforeEach(async () => {
  // Configuración del TestBed y creación del componente
  fixture.componentRef.setInput('navState', {} as LoginResponse);
  await fixture.whenStable();
});
```

En el caso de un login exitoso, se renderiza un mensaje de bienvenida con el email del usuario

```ts
it('should render login success message when navState has no error', () => {
  fixture.componentRef.setInput('navState', mockLoginResponseSuccess);
  fixture.detectChanges();

  const element = fixture.nativeElement as HTMLElement;
  expect(element.querySelector('p')?.textContent).toContain('Login exitoso.');
  expect(element.querySelectorAll('p')[1]?.textContent).toContain(
    `Bienvenido, ${mockLoginResponseSuccess.info?.email}!`
  );
})
```

En el caso de un login fallido, se renderiza un mensaje de error con la información del error recibido desde el servicio de login.

```ts
it('should render login error message when navState has an error', () => {
  fixture.componentRef.setInput('navState', mockLoginResponseError);
  fixture.detectChanges();

  const element = fixture.nativeElement as HTMLElement;
  expect(element.querySelector('p')?.textContent).toContain('Error en el login.');
  expect(element.querySelectorAll('p')[1]?.textContent).toContain(
    mockLoginResponseError.error
  );
});
```

### 🧿Componente LoginSimulated

El template de este componente contiene dos botones, que sin un formulario de login, nos permite simular el login con éxito o error.

```ts
@Component({
  selector: 'alc-login-simulated',
  imports: [RouterLink],
  template: `
    <p>Simulación de login</p>
    <div>
      <button (click)="login()">Login</button>
      <button (click)="login(false)">Login con Error</button>
    </div>
  `,
  styles: `
    div {
      display: flex;
      gap: 1rem;
      margin-top: 1rem;
      justify-content: center;
    }
  `,
})
```

En el componente definimos un método `login()` que simula un login con éxito o error, y navega a la ruta `/auth/login-state` pasando un estado con información sobre el resultado del login.

```ts
protected login(successes = true) {
  console.log('Login button clicked');
  const response: LoginResponse = successes
    ? {
        error: '',
        token: 'abc123',
        info: {
          id: 1,
          email: 'pepe@example.com',
          loginDate: new Date(),
          rememberMe: true,
        },
      }
    : {
        error: 'Credenciales inválidas',
        token: '',
      };
  this.#router.navigate(['/auth/login/after-login'], {
    state: { ...response },
  });
}
```

#### 👁️‍🗨️Test del componente LoginSimulated

Comprobamos que el primer botón llama al router para navegar a la ruta `/auth/login-state` con un estado de login exitoso

```ts
it('should navigate to after-login on click of successful login', () => {

  const elementButtons = fixture.nativeElement.querySelectorAll('button');
  elementButtons[0].click();
  fixture.detectChanges();
  expect(router.navigate).toHaveBeenCalledWith(['/auth/login/after-login'], expect.objectContaining({
    state: expect.objectContaining({
      error: '',
      token: 'abc123',
    })
  }))
});
```


Comprobamos que el segundo botón llama eal router para navegar a la misma ruta con un estado de login fallido.

```ts
it('should navigate to after-login on click of failed login', () => {
  const elementButtons = fixture.nativeElement.querySelectorAll('button');
  elementButtons[1].click();
  fixture.detectChanges();
  expect(router.navigate).toHaveBeenCalledWith(['/auth/login/after-login'], expect.objectContaining({
    state: expect.objectContaining({
      error: 'Credenciales inválidas',
      token: '',
    })
  }))
});
```


### 🌐Cambios en la página LoginPage

Añadimos las nuevas opciones en el tipo LoginFormType, que nos permitirán

- renderizar el componente `login-simulated` cuando se navega a la ruta `/auth/login-simulated`
- renderizar el componente `login-state` cuando se navega a la ruta `/auth/login-state`

```ts
type LoginFormType = 'simulate' | 'tdf' | 'mdf-rx' | 'signals' | 'after-login' ;
```

Añadimos las nuevas opciones de renderizado,  como ya son cinco, podríamos sustituir los @if por un @switch, que nos permite renderizar el componente adecuado según el valor del parámetro `formType`.

```ts
<alc-card>
  @let type = formType();
  @switch (type) {
    @case ('simulate') {
      <alc-login-simulated />
    }
    @case ('tdf') {
      <alc-login-form-tdf />
    }
    @case ('mdf-rx') {
      <alc-login-form-mdf-rx />
    }
    @case ('signals') {
      <alc-login-form-signals />
    }
    @case ('after-login') {
      <alc-login-state [navState]="navState()" />
    }
    @default never;
  }
</alc-card>
```

Al añadir la opción `@default never;` añadimos un 'type checking' exhaustivo en typescript y nos aseguramos de que si falta el caso correspondiente a algún valor posible del parámetro `formType`, tendremos un error de typescript. Para que este proceso sea posible, es necesario hacer el switch sobre un valor de tipo primitivo y limitado a una serie de opciones. Poe eso, en primer lugar creamos una variable local a a plantilla (`@let`) donde almacenamos el valor de la signal `formType()`, que es de tipo `LoginFormType`, y luego hacemos el switch sobre esa variable local.


Para el caso `simulate`:

- añadimos en el template la renderización condicional del componente `login-simulated` cuando el valor del parámetro `formType` es `simulate` 
- añadimos una opción el menu que se muestra en el sidebar, para poder navegar a la ruta correspondiente

```ts
protected readonly menuOptions = signal<MenuOption[]>([
  {
    label: 'Simulate login (no form)',
    path: '/auth/login/simulate',
  },
  // otras opciones
]);  
```

Para el caso `after-login`:

- añadimos en el template la renderización condicional del componente `login-state` cuando el valor del parámetro `formType` es `after-login`, y le pasamos como input el estado de la navegación obtenido desde el router.

```ts
@else if (formType() === 'after-login') {
  <alc-card>
    <alc-login-state [navState]="navState()" />
  </alc-card>
} 
```

- en el componente añadimos una nueva signal `routerState` que nos permite almacenar el estado de la navegación obtenido desde el router, y que se pasa como input al componente `login-state`.

```ts
  readonly #router = inject(Router);
  protected navState = signal<LoginResponse>({error: '', token: ''});

  constructor() {
    this.#router.events.subscribe((event) => {
      if (event instanceof NavigationEnd) {
        const currentNav = this.#router.currentNavigation();
        if (currentNav && currentNav.extras.state) {
          this.navState.set(currentNav.extras.state as LoginResponse);
        }
      }
    });
  }
```

Como ya vimos, el estado de la navegación se pierde una vez que se completa la navegación, por lo que es importante acceder a él en el momento adecuado, en este caso en el evento `NavigationEnd`.

#### 👁️‍🗨️Actualización del test del componente

Necesitamos añadir los casos de uso correspondientes a las ds nuevas opciones de navegación.

En la primera de ellas, `simulate` basta comprobar que se renderiza el componente adecuado según el valor del parámetro `formType`, 

```ts
it('should have render the view for "simulate"', async () => {
  await harness.navigateByUrl('/login/simulate', LoginPage);
  await harness.fixture.whenStable();
  component = debugHarness.query(By.directive(LoginPage)).componentInstance;

  const debugForm = debugHarness.query(By.directive(LoginSimulated));
  expect(debugForm).toBeTruthy();
  expect(debugForm.nativeElement).toBeInstanceOf(HTMLElement);
  expect(component['formType']()).toBe('simulate');
});
```

En el caso de la opción `after-login`, además de comprobar que se renderiza el componente `login-state`, necesitamos comprobar que se actualiza la información del estado de la navegación.

Para ello no podemos usar el método `navigateByUrl()` del `RouterTestingHarness`, ya que no nos permite pasar un estado de navegación, por lo que necesitamos acceder directamente al servicio `Router` inyectado en el harness utilizar su método `navigate()` para navegar a la ruta correspondiente, pasando el estado de navegación como parámetro.

```ts
let harnessRouter: Router;

beforeEach(async () => {
  await TestBed.configureTestingModule({
    imports: [LoginPage],
    providers: [provideRouter(authRoutes), withComponentInputBinding()],
  }).compileComponents();

  harness = await RouterTestingHarness.create();
  debugHarness = harness.fixture.debugElement;
  harnessRouter = harness.fixture.componentRef.injector.get(Router);
  await harness.fixture.whenStable();
});
```

En un caso de prueba pararemos los valores de un login con error

```ts
it('should have render the view for "after-login" after error login', async () => {
  harnessRouter.navigate(['/login/after-login'], {
    state: { error: 'Error de prueba', token: '' },
  });
  // await harness.navigateByUrl('/login/after-login', LoginPage);
  await harness.fixture.whenStable();
  component = debugHarness.query(By.directive(LoginPage)).componentInstance;

  const debugState = debugHarness.query(By.directive(LoginState));
  expect(debugState).toBeTruthy();
  expect(debugState.nativeElement).toBeInstanceOf(HTMLElement);
  expect(component['formType']()).toBe('after-login');
  const state = component['navState']();
  expect(state).toEqual({ error: 'Error de prueba', token: '' });
});
```

En otro caso pasaremos los valores de un login con éxito

```ts
it('should have render the view for "after-login" after success login', async () => {
  harnessRouter.navigate(['/login/after-login'], {
    state: { error: '', token: 'Token de prueba' },
  });
  await harness.fixture.whenStable();
  component = debugHarness.query(By.directive(LoginPage)).componentInstance;

  const debugState = debugHarness.query(By.directive(LoginState));
  expect(debugState).toBeTruthy();
  expect(debugState.nativeElement).toBeInstanceOf(HTMLElement);
  expect(component['formType']()).toBe('after-login');
  const state = component['navState']();
  expect(state).toEqual({ error: '', token: 'Token de prueba' });
});
```

## 📕Forms

Como hemos dicho, con LoginForm veremos ejemplos de los tres tipos de formularios que Angular nos permite crear:

- **Template-driven** forms: se definen en el template de la página, utilizando directivas de Angular para enlazar los campos del formulario con las propiedades del componente. Son más sencillos de implementar, pero menos flexibles y escalables.
- **Reactive forms o Model-driven forms**: se definen en el componente, utilizando la clase FormGroup y FormControl para crear un modelo de datos que representa el formulario. Son más flexibles y escalables, pero requieren más código y conocimientos de Angular.
- **Signal forms**: nuevos formularios basados en signals, también definidos en el componente

## Classic Forms: samples with LoginForm

### 📕Template driven forms

- Dependen de las directivas `ngForm` y `ngModel` incluidas en el módulo `FormsModule`, 
- la aplicación de la segunda de ellas da la primera de ellas da lugar a un objeto NgForm que representa el formulario y que nos permite acceder a sus propiedades y métodos, como `valid`, `invalid`, `pristine`, `dirty`, `value`, `resetForm()`, etc.
- el uso de la directiva `ngModel` nos permite enlazar el valor de un control del formulario con una propiedad del objeto NgForm, y el atributo `name`, nativo de HTML, nos permite definir el nombre del control dentro del formulario.
- el uso de la directiva `ngSubmit` nos permite ejecutar un método del componente cuando se envía el formulario, pasándole el objeto NgForm que representa el formulario, desactivándose por defecto el comportamiento nativo de envío del formulario, que recarga la página.
- la validación se define en el template del formulario, utilizando los atributos nativos de HTML5, como `required`, `minlength`, `maxlength`, `pattern`, `email`, etc.

El componente (modelo o controller) no es responsable de la creación y gestión del formulario; solo lo es de su lógica de negocio: el formato de los datos, y su envío al backend.  

Pese a todo esto, el objeto NgForm contiene a su vez un FormGroup, para representar el formulario, como los que veremos al hablar de los reactive forms. Por tanto la diferencia entre template-driven forms y reactive forms no es tanto la existencia de un modelo de datos que representa el formulario, sino la forma en que se crea y gestiona dicho modelo. 

### 🧿LoginFormTdf: Template-driven Form

En el componente `login-form-tdf` definimos el formulario en el template del componente, utilizando directivas de Angular para enlazar los campos del formulario con las propiedades del componente.

#### NgForm

Al importar el módulo `FormsModule` en la feature, podemos utilizar las directivas `ngModel` y `ngForm` para enlazar los campos del formulario con las propiedades del componente.

- `ngForm` tiene como selector `form`, por lo que se aplica implícitamente al elemento `form` nativo de html. Sin embargo, podemos utilizar una referencia local para acceder al objeto de tipo `NgForm` creado por la directiva, que representa el formulario, y acceder a sus propiedades y métodos.

- en la etiqueta form utilizamos también la directiva `ngSubmit`, que nos permite ejecutar un método del componente cuando se envía el formulario, pasándole el objeto `NgForm` que hemos referenciado 

```ts
@Component({
  selector: 'alc-login-form-tdf',
  imports: [FormsModule, JsonPipe],
  template: `
    <form #loginForm="ngForm" (ngSubmit)="formSubmit(loginForm)">
      <button type="submit" class="btn btn-primary">
        Login
      </button>
      <pre>{{ loginForm.value | json }}</pre>
    </form>
    `
    })
    export class LoginFormTdf {

        protected formSubmit(loginForm: NgForm) {
          console.log('loginForm', loginForm);
          console.log('loginForm.value', loginForm.value);
        }
    }
}
```

Al mostrar este objeto en consola, vemos que incluye:

-  un objeto `form` de tipo `FormGroup`,  y muchas de sus propiedades expuestas en el propio NgForm
-  propiedades como `valid`, `invalid`, `pristine`, `dirty`, etc. que definen el estado del formulario
-  una propiedad `value` que contiene los valores de los campos del formulario
-  métodos como `resetForm()` que nos permite resetear el formulario a su estado inicial

```shell
_NgForm {_composedValidatorFn: undefined, _composedAsyncValidatorFn: undefined, _rawValidators: Array(0), _rawAsyncValidators: Array(0), _onDestroyCallbacks: Array(0), …}
callSetDisabledState: "always"
form: FormGroup {_pendingDirty: false, _hasOwnPendingAsyncValidator: null, _pendingTouched: false, _updateOn: undefined, _onCollectionChange: ƒ, …}
name: undefined
ngSubmit: EventEmitter_ {closed: false, currentObservers: null, observers: Array(0), isStopped: false, hasError: false, …}
options: undefined
submittedReactive: () => signalGetFn(node)
__ngContext__: 36
_composedAsyncValidatorFn: undefined
_composedValidatorFn: undefined
_directives: Set(0) {size: 0}
_onDestroyCallbacks: []
_rawAsyncValidators: []
_rawValidators: []
_submitted: () => {…}
asyncValidator: (...)
control: (...)
controls: Object
dirty: false
disabled: (...)
enabled: (...)
errors: null
formDirective: (...)
invalid: false
path: (...)
pending: false
pristine: true
status: "VALID"
statusChanges: (...)
submitted: (...)
touched: false
untouched: true
valid: true
validator: (...)
value: Object
valueChanges: (...)
```

#### Controles del formulario y ngModel

En cada uno de los controles del formulario, utilizamos la directiva `ngModel` para enlazar el valor del control con una propiedad del componente, y el atributo `name`, nativo de HTML, para definir el nombre del control dentro del formulario.

```html
  <label class="form-control" for="email">
    <span> Email </span>
    <input type="email" id="email" name="email" class="form-control" ngModel />
  </label>
```

Si se necesita un valor inicial distinto de la cadena vacía, se puede utilizar la propiedad `ngModel` para enlazar el valor del control con una propiedad del componente, y definir el valor inicial en el componente.

```html
   <label class="form-control checkbox" for="rememberMe">
    <input type="checkbox" id="rememberMe" name="rememberMe" [ngModel]="false" />
    <span>Remember me</span>
  </label>
```

#### Valor del formulario

Como herramienta en desarrollo, que luego eliminaremos, añadimos un \<pre> que nos permite ver el valor del formulario en tiempo real, utilizando el pipe `json` para mostrarlo en formato JSON.

```html
  <pre>{{ loginForm.value | json }}</pre>
```

#### Validación y feedback

Las validaciones se definen en el template del formulario, utilizando los atributos nativos de HTML5, como `required`, `minlength`, `maxlength`, `pattern`, `email`, etc.

Para mostrar el feedback de las validaciones, podemos utilizar las propiedades del objeto `NgForm` y de los controles del formulario, como `valid`, `invalid`, `pristine`, `dirty`, `touched`, `untouched`, etc.

```html
  @if (loginForm.controls['email']?.invalid && loginForm.controls['email']?.touched) {
    <div class="error">
      @if (loginForm.controls['email']?.hasError('required')) {
        <p>El correo electrónico es obligatorio.</p>
      }
      @if (loginForm.controls['email']?.hasError('email')) {
        <p>Por favor, introduce una dirección de correo electrónico válida.</p>
      }
    </div>
  }
```

Para evitar el envío de formularios inválidos, podemos deshabilitar el botón de envío cuando el formulario es inválido, utilizando la propiedad `invalid` del objeto `NgForm`.

```html
  <button type="submit" class="btn btn-primary" [disabled]="loginForm.invalid">
    Login
  </button>
```

Además puede añadirse una comprobación extra en el método que maneja el envío del formulario, para asegurarnos de que no se envía un formulario inválido.

```ts
  protected formSubmit(loginForm: NgForm) {
    if (loginForm.valid) {
      console.log('loginForm', loginForm);
      // Navigate to the home page after successful login
      console.log('Form submitted:', loginForm.value);
      loginForm.resetForm(); // Reset the form after submission
    } else {
      console.log('Form is invalid');
    }
  }
```

#### Submit y Navegación

El proceso de submit incluirá la llamada a un servicio, responsable del login de cara al backend. Si este aun no existe, el submit solo sera un console log para mostrar los datos que se enviarían al backend, y un reset del formulario para dejarlo limpio para un nuevo intento de login.

En el caso de que el servicio de login exista, se inyectará en el componente y se llamará a su método `login()`, pasándole los datos del formulario. Este método devolverá un observable que nos permitirá suscribirnos a su resultado, y navegar a la página de login, que cargará el componente de mostrar feedback al usuario tanto en caso de acierto como de error.

Si el formulario es inválido, se navegará a la misma página de login, pero con un estado que indique el error, para que el componente de feedback muestre el mensaje correspondiente. Esta situación trata de evitarse con los mecanismos de validación, pero se codifica por precaución, por si llegará a producirse.


```ts
export class LoginFormTdf {
  readonly #router = inject(Router);
  readonly #auth = inject(Auth);
  readonly #destroyRef = inject(DestroyRef);

  protected readonly isSending = signal(false);

  protected formSubmit(loginForm: NgForm) {
    if (loginForm.valid) {
      this.isSending.set(true);
      this.#auth
      .login(loginForm.value, {
        delayTime: 2000,
      })
      .pipe(takeUntilDestroyed(this.#destroyRef))
      .subscribe((state) => {
          // Navigate to the home page after successful login
          this.#router.navigate(['/auth', 'login', 'after-login'], {
            state,
          });
          loginForm.resetForm(); // Reset the form after submission
          this.isSending.set(false);
        });
    } else {
      // console.log('Form is invalid:', loginForm.value);
      this.#router.navigate(['/auth', 'login', 'after-login'], {
        state: {
          error: 'Form is invalid',
          token: '',
        },
      });
      loginForm.resetForm(); // Reset the form after submission
      this.isSending.set(false);
    }
  }
}
```

La signal `isSending` nos permite deshabilitar el botón de envío mientras se está procesando la solicitud de login, para evitar que el usuario haga clic varias veces en el botón y envíe varias solicitudes al backend.

```html
<div class="form-control" [class.sending]="isSending()">
  <button type="submit" class="btn btn-primary" [disabled]="!loginForm.form.valid">
    Login
  </button>
</div>
```

```css
.sending {
  pointer-events: none;
  position: relative;

  button {
    color: transparent;
  }

  &:before {
    content: '';
    position: absolute;
    background-color: var(--color-background);
    top: 0;
    bottom: 0;
    left: 0;
    right: 0;
    opacity: 0.8;
  }

  &::after {
    content: 'Sending...';
    font-size: 0.9rem;
    position: absolute;
    top: 0;
    bottom: 0;
    left: 0;
    right: 0;
    color: var(--color-primary);
    margin-top: 0.5rem;
    z-index: 10;
  }
}
```

#### Resultado final

El resultado final del componente es el siguiente

```ts
import { JsonPipe } from '@angular/common';
import { Component, inject } from '@angular/core';
import { FormsModule, NgForm } from '@angular/forms';
import { Router } from '@angular/router';

@Component({
  selector: 'alc-login-form-tdf',
  imports: [FormsModule, JsonPipe],
  template: `
    <form #loginForm="ngForm" (ngSubmit)="formSubmit(loginForm)">
      <label class="form-control" for="email">
        <span> Email </span>
        <input type="email" id="email" name="email" ngModel email required />
      </label>
      @if (loginForm.controls['email']?.invalid && loginForm.controls['email']?.touched) {
        <div class="error">
          @if (loginForm.controls['email']?.hasError('required')) {
            <p>El correo electrónico es obligatorio.</p>
          }
          @if (loginForm.controls['email']?.hasError('email')) {
            <p>Por favor, introduce una dirección de correo electrónico válida.</p>
          }
        </div>
      }
      <label class="form-control" for="password">
        <span>Password</span>
        <input
          type="password"
          id="password"
          name="password"
          ngModel
          required minlength="6"
        />
      </label>
      @if (loginForm.controls['password']?.invalid && loginForm.controls['password']?.touched) {
        <div class="error">
          @if (loginForm.controls['password']?.hasError('required')) {
            <p>La contraseña es obligatoria.</p>
          }
          @if (loginForm.controls['password']?.hasError('minlength')) {
            <p>La contraseña debe tener al menos 6 caracteres.</p>
          }
        </div>
      }
      <label class="form-control checkbox" for="rememberMe">
        <input type="checkbox" id="rememberMe" name="rememberMe" [ngModel]="false" />
        <span>Remember me</span>
      </label>
      <div class="form-control">
        <button type="submit" class="btn btn-primary" [disabled]="!loginForm.form.valid">
          Login
        </button>
      </div>
    </form>
    <pre>{{ loginForm.value | json }}</pre>
  `,
  styles: `
    form {
      display: flex;
      flex-direction: column;
      gap: 1rem;
      width: 80vw;
      max-width: 400px;

      .form-control {
        display: flex;
        flex-direction: column;
        gap: 0.5rem;

        &.checkbox {
          flex-direction: row;
          align-items: center;
        }
      }
    }

    input,
    textarea {
      padding: 0.5rem;
      font-size: 1rem;
      color: var(--color-primary-hot);
      background-color: var(--color-background-primary);
      border: none;
      border-block-end: 2px solid var(--color-primary);
      border-radius: 4px;

      &:focus-visible {
        outline: var(--color-primary) auto 1px;
        background-color: var(--color-background);
      }
    }

    button {
      padding: 0.5rem 1rem;
      font-size: 1rem;
      color: var(--color-background);
      background-color: var(--color-primary);
      border: none;
      border-radius: 4px;
      cursor: pointer;

      &:disabled {
        background-color: color-mix(in srgb, var(--color-primary) 30%, transparent);
        cursor: not-allowed;
      }
    }

    .error {
      color: var(--color-tertiary);
      font-size: 0.8rem;
    }
  `,
})
export class LoginFormTdf {
  readonly #router = inject(Router);
  readonly #auth = inject(Auth);
  readonly #destroyRef = inject(DestroyRef);

  protected formSubmit(loginForm: NgForm) {
    if (loginForm.valid) {
      console.log('loginForm', loginForm);
      // Navigate to the home page after successful login
      console.log('Form submitted:', loginForm.value);
      this.#auth
      .login(loginForm.value)
      .pipe(takeUntilDestroyed(this.#destroyRef))
      .subscribe((state) => {
        this.#router.navigate(['/auth', 'login', 'after-login'], {
          state,
        });
        loginForm.resetForm(); // Reset the form after submission
      });
    } else {
      console.log('Form is invalid:', loginForm.value);
    }
  }
}
```

#### 👁️‍🗨️Test del componente LoginFormTdf

Para simular completamente la acción del usuario rellenando los campos del formulario tenemos que seguir varios pasos:

- acceder al elemento del DOM
- modificar su valor con el que queremos simular, 
- utilizando `dispatchEvent` para simular el evento `input` que se produce cuando el usuario escribe en un campo de texto, y el evento `change` que se produce cuando el usuario cambia el valor de un campo de tipo checkbox.
- igualmente simular el evento `blur` para simular que el usuario ha salido del campo, lo que provoca que Angular marque el control como `touched`, y por tanto se muestren los mensajes de error si el campo es inválido.
- utilizar `fixture.whenStable()` para esperar a que Angular haya procesado los cambios y actualizado el estado del formulario o
- utilizar `fixture.detectChanges()` para que Angular detecte los cambios y actualice el estado del formulario.

```ts
emailInput = element.querySelector('input[type="email"]') as HTMLInputElement;
emailInput.value = 'invalid-email';
emailInput.dispatchEvent(new Event('input'));
emailInput.dispatchEvent(new Event('blur')); // Trigger blur event to mark the field as touched
fixture.detectChanges();
```      

El otro factor importante es definir correctamente todos los casos de uso del test, comprobando

- que el componente se renderiza correctamente
- que el formulario está vacío y es inválido, con el botón de submit deshabilitado
- que al rellenar los campos con valores válidos, el formulario se vuelve válido y el botón de submit se habilita
- que al rellenar los campos con valores inválidos, el formulario se vuelve inválido y se muestran los mensajes de error correspondientes a cada uno de los requerimientos de validación definidos en el template del formulario (required, email, minlength, etc.)

En la preparación se pueden definir los elementos del DOM que vamos a utilizar en los tests, para no tener que buscarlos en cada test, y así mejorar la legibilidad del código. Pueden usarse variables independientes o un array con todos los controles.

```ts
describe('Form use and validation', () => {
  //...
  let emailInput: HTMLInputElement;
  let passwordInput: HTMLInputElement;
  let rememberMeInput: HTMLInputElement;
  let submitButton: HTMLButtonElement;

  beforeEach(() => {
    //...
    router = TestBed.inject(Router);
    await fixture.whenStable();
    const element = fixture.nativeElement as HTMLElement;
    emailInput = element.querySelector('input[type="email"]') as HTMLInputElement;
    passwordInput = element.querySelector('input[type="password"]') as HTMLInputElement;
    rememberMeInput = element.querySelector('input[type="checkbox"]') as HTMLInputElement;
    submitButton = element.querySelector('button[type="submit"]') as HTMLButtonElement;
  });
  // tests
});
```

Al inyectar el router, estamos accediendo a la misma instancia que utiliza el componente definida como privada. De esa forma el ámbito de visibilidad privado en los servicios nunca es un inconveniente a la hora de llevar a cabo los tests.

Otra cuestión a tener en cuenta es la existencia de un servicio de login, que es el que se encarga de enviar los datos del formulario al backend y devolver un observable con el resultado. En los tests no queremos que se realicen llamadas al backend, por lo que podemos espiar el método `login()` del servicio y devolver un observable simulado con el resultado que queramos.

```ts
  vi.spyOn(auth, 'login').mockReturnValue(of({ error: '', token: 'abc123' }));
```

Una alternativa es crear un mock del servicio de login, e incluirlo en el provider del servicio, definiendo el método `login()` para que devuelva un observable simulado con el resultado que queramos.

```ts
providers: [
  {
    provide: Auth,
    useValue: {
      login: vi.fn().mockReturnValue(of({ error: '', token: 'abc123' })),
    },
  },
],
```

En cualquiera de los dos casos, inyectamos el servicio de login en el test para poder espiar su método `login()` y comprobar que se ha llamado correctamente, y con los parámetros correctos.

```ts
let auth: Auth;

beforeEach(() => {
  //...
  auth = TestBed.inject(Auth);
});
```

El otro factor importante es definir correctamente todos los casos de uso del test, comprobando

- que el componente se renderiza correctamente
- que si el formulario está vacío, es inválido y con el botón de submit deshabilitado
- que en el caso anterior si forzamos el submit, se navega a la página de login/after-login con un estado correspondiente a error
- que al rellenar los campos con valores válidos, el formulario se vuelve válido y el botón de submit se habilita
- que al rellenar los campos con valores inválidos, el formulario se vuelve inválido y se muestran los mensajes de error correspondientes a cada uno de los requerimientos de validación definidos en el template del formulario (required, email, minlength, etc.)

Los test del formulario y sus validaciones serían los siguiente


```ts
it('should render the form with input fields and submit button', () => {
  fixture.detectChanges();
  expect(emailInput).toBeTruthy();
  expect(passwordInput).toBeTruthy();
  expect(rememberMeInput).toBeTruthy();
  expect(submitButton).toBeTruthy();
});

it('should disable the submit button when the form is invalid', () => {
  fixture.detectChanges();
  expect(submitButton.disabled).toBe(true);
});

it('should NOT submit a INVALID form', () => {
  vi.spyOn(router, 'navigate');
  submitButton.disabled = false;
  fixture.detectChanges();
  submitButton.click();
  fixture.detectChanges();
  expect(router.navigate).toHaveBeenCalledWith(['/auth', 'login', 'after-login'], {
    state: {
      error: 'Form is invalid',
      token: '',
    },
  });
});

it('should enable the submit button when the form is valid', () => {
  vi.spyOn(router, 'navigate');
  vi.spyOn(console, 'log');
  //.mockImplementation(() => Promise.resolve(true));
  fixture.detectChanges();
  emailInput.value = 'test@example.com';
  emailInput.dispatchEvent(new Event('input'));
  passwordInput.value = 'password';
  passwordInput.dispatchEvent(new Event('input'));
  rememberMeInput.checked = true;
  rememberMeInput.dispatchEvent(new Event('change'));
  fixture.detectChanges();
  expect(submitButton.disabled).toBe(false);
  submitButton.click();
  expect(auth.login).toHaveBeenCalledWith(
    {
      email: 'test@example.com',
      password: 'password',
      rememberMe: true,
    },
    {
      delayTime: 2000,
    },
  );
  const state = {};
  expect(router.navigate).toHaveBeenCalledWith(['/auth', 'login', 'after-login'], { state });

});
```

En este caso, para los expects espiamos los métodos del router y del servicio de autenticación para comprobar que se han llamado correctamente, y con los parámetros correctos. 

Con frecuencia es recomendable, como hemos hecho, añadir un mock de su implementación para evitar acciones no deseadas, como llamadas a un backend, etc.

En otro bloque del test podemos comprobar que se muestran los mensajes de error cuando el formulario es inválido, y que desaparecen cuando el formulario es válido.

```ts
describe('Form errors in email validation', () => {
  let emailInput: HTMLInputElement;

  beforeEach(() => {
    fixture.detectChanges();
    const element = fixture.nativeElement as HTMLElement;
    emailInput = element.querySelector('input[type="email"]') as HTMLInputElement;
  });

  it('should show error messages when the email is empty', () => {
    fixture.detectChanges();
    emailInput.value = '';
    emailInput.dispatchEvent(new Event('input'));
    emailInput.dispatchEvent(new Event('blur')); // Trigger blur event to mark the field as touched
    fixture.detectChanges();
    const errorElement = fixture.nativeElement.querySelector('.error');
    expect(errorElement).toBeTruthy();
    expect(errorElement.textContent).toContain('obligatorio');
  });

  it('should show error messages when the email is invalid', () => {
    fixture.detectChanges();
    emailInput.value = 'invalid-email';
    emailInput.dispatchEvent(new Event('input'));
    emailInput.dispatchEvent(new Event('blur')); // Trigger blur event to mark the field as touched
    fixture.detectChanges();
    const errorElement = fixture.nativeElement.querySelector('.error');
    expect(errorElement).toBeTruthy();
    expect(errorElement.textContent).toContain('válida');
  });

  it('should NOT show error messages when the email is valid', () => {
    fixture.detectChanges();
    emailInput.value = 'test@example.com';
    emailInput.dispatchEvent(new Event('input'));
    emailInput.dispatchEvent(new Event('blur')); // Trigger blur event to mark the field as touched
    fixture.detectChanges();
    const errorElement = fixture.nativeElement.querySelector('.error');
    expect(errorElement).toBeFalsy();
  });
});
```

Repetimos los mismos test para el caso de la password, comprobando que se muestran los mensajes de error cuando el formulario es inválido, y que desaparecen cuando el formulario es válido.

### 🧿LoginFormMdfRx: Model-driven Form (Rx)

### 📕Template driven forms

Los formularios reactivos se definen a nivel del componente, creando un objeto de la clase `FormGroup`. Sus propiedades son a su vez objetor de la clase `FormControl`, que representan los distintos controles del formulario

```ts
  public readonly formGroup = new FormGroup({
    email: new FormControl(''),
    password: new FormControl(''),
  });
```

Tanto el objeto `FormGroup` como los objetos `FormControl` permiten acceder las propiedades y métodos, del formulario t de cada control, como `value`, `valid`, `invalid`, `reset()`, etc.

Para la creación del FormGroup, Angular proporciona un servicio, `FormBuilder`, que implementa el patrón factory y en su método group devuelve una instancia de FormGroup, recibiendo como parámetro un objeto que implícitamente dará lugar a los FormControls que compondrán el FormGroup, asignándole a cada uno su valor inicial

```ts
  readonly #fb = inject(FormBuilder);
  public formGroup = this.#fb.group({
    email: [''],
    password: [''],
  });
```

Las validaciones se definen a nivel del componente, utilizando los validadores de Angular, como `Validators.required`, `Validators.minLength`, `Validators.maxLength`, `Validators.pattern`, etc. equivalentes a los atributos nativos de HTML5, como `required`, `minlength`, `maxlength`, `pattern`, etc.

```ts
  public formGroup = this.#fb.group({
    email: ['', [Validators.required, Validators.email]],
    password: ['', [Validators.required, Validators.minLength(6)]],
  });
```

Se pueden añadir **validadores personalizados**, que son funciones que reciben un control del formulario y devuelven un objeto con los errores de validación, o null si el control es válido.

```ts
  public formGroup = this.#fb.group({
    email: ['', [Validators.required, Validators.email]],
    password: ['', [Validators.required, Validators.minLength(6), this.customPasswordValidator]],
  });

  private customPasswordValidator(control: FormControl): ValidationErrors | null {
    const value = control.value;
    if (value && !/[A-Z]/.test(value)) {
      return { missingUpperCase: true };
    }
    return null;
  }
```

Igualmente, como tercer elemento del array que define cada formControl, podemos añadir **validadores asíncronos**, que es una función que recibe un control del formulario y devuelve un observable que emite un objeto con los errores de validación, o null si el control es válido.

```ts
  public formGroup = this.#fb.group({
    email: ['', [Validators.required, Validators.email], [this.asyncEmailValidator]],
    password: ['', [Validators.required, Validators.minLength(6)]],
  });

  private asyncEmailValidator(control: FormControl): Observable<ValidationErrors | null> {
    return of(control.value).pipe(
      delay(1000),
      map(value => {
        if (value === 'admin@example.com') {
          return { emailTaken: true };
        } 
        return null;
      })
    );
  }
```

Como vemos, la creación y gestión del modelo de datos que representa el formulario se realiza en el componente, y no en el template, como ocurría con los template-driven forms. Esto nos permite tener un mayor control sobre el formulario, y nos permite crear formularios más complejos y dinámicos.

En el template, utilizamos las directivas `formGroup` y `formControlName` para enlazar el formulario con el objeto `FormGroup` creado en el componente, y cada control del formulario con su correspondiente `FormControl`.

```html
  <form [formGroup]="formGroup" (ngSubmit)="onSubmit()">
    <label class="form-control" for="email">
      <span> Email </span>
      <input type="email" id="email" formControlName="email" />
    </label>
    ...
  </form>
```

Igual que en los template-driven forms, la directiva `ngSubmit` nos permite ejecutar un método del componente cuando se envía el formulario, sin que sea necesario pasarle valores, ya que el objeto `FormGroup` que representa el formulario es parte del componente, y podemos acceder a él directamente desde el método que maneja el envío del formulario.

```ts
  protected onSubmit() {
    if (this.formGroup.valid) {
      console.log('Form submitted:', this.formGroup.value);
      this.formGroup.reset(); // Reset the form after submission
    } else {
      console.log('Form is invalid');
    }
  }
```

#### Lógica en el componente

Los formularios reactivos (model-driven forms) se definen en el componente, utilizando la clase `FormGroup` y `FormControl` para crear un modelo de datos que representa el formulario.

Por lo general, estas clases no se utilizan directamente, sino mediante el servicio `FormBuilder`, que nos permite crear los formularios de forma más sencilla y con menos código.

```ts

interface LoginForm {
  email: string;
  password: string;
  rememberMe: boolean;
}

export class LoginFormMdfRx {

  readonly #fb = inject(FormBuilder);

  readonly loginForm: FormGroup = this.#fb.group({
    email: [''],
    password: [''],
    rememberMe: [false],
  });
}
```
- obtenemos on `FormGroup<any>` el objeto que representa el formulario, y que nos permite acceder a sus propiedades y métodos, como `value`, `valid`, `invalid`, `reset()`, etc.
- en el se incluyen los controles del formulario, que son objetos de tipo `FormControl`, y que nos permiten igualmente acceder a sus propiedades y métodos, como `value`, `valid`, `invalid`, `reset()`, etc.

#### Vinculación al template

En los imports incluimos el módulo `ReactiveFormsModule`, que nos permite utilizar las directivas `formGroup` y `formControlName` para enlazar el formulario con el template.

- en la etiqueta \<form> del template utilizamos la directiva `formGroup` para enlazar el formulario con el objeto `FormGroup` creado en el componente, 
- en cada uno de los controles utilizamos la directiva `formControlName` para enlazar cada control del formulario con su correspondiente `FormControl` utilizando siempre el valor definido en el FormGroup. (Podemos eliminar el atributo nativo `name` que ya no es necesario)

```html
  <form [formGroup]="loginForm" (ngSubmit)="formSubmit()">
    <label class="form-control" for="email">
      <span> Email </span>
      <input type="email" id="email" formControlName="email" />
    </label>
    ...
  </form>
```

#### Valor del formulario

De nuevo, como herramienta en desarrollo, que luego eliminaremos, añadimos un \<pre> que nos permite ver el valor del formulario en tiempo real, utilizando el pipe `json` para mostrarlo en formato JSON.

```html
  <pre>{{ loginForm.value | json }}</pre>
```

#### Validación y feedback

Las validaciones ya NO se definen en el template del formulario, sino en el componente, utilizando los validadores de Angular, como `Validators.required`, `Validators.minLength`, `Validators.maxLength`, `Validators.pattern`, etc. equivalentes a los atributos nativos de HTML5, como `required`, `minlength`, `maxlength`, `pattern`, etc.

```ts
export class LoginFormMdfRx {
  readonly #fb = inject(FormBuilder);

  readonly loginForm: FormGroup = this.#fb.group({
    email: ['', [Validators.required, Validators.email]],
    password: ['', [Validators.required, Validators.minLength(6)]],
    rememberMe: [false],
  });
}
```

Para mostrar el feedback de las validaciones, podemos utilizar las propiedades del objeto `FormGroup` y de los controles del formulario, como `valid`, `invalid`, `pristine`, `dirty`, `touched`, `untouched`, etc. Es el mismo mecanismo que utilizábamos en los template-driven forms

```html
  @if (loginForm.controls['email']?.invalid && loginForm.controls['email']?.touched) {
    <div class="error">
      @if (loginForm.controls['email']?.hasError('required')) {
        <p>El correo electrónico es obligatorio.</p>
      }
      @if (loginForm.controls['email']?.hasError('email')) {
        <p>Por favor, introduce una dirección de correo electrónico válida.</p>
      }
    </div>
  }
```

Para evitar el envío de formularios inválidos, podemos deshabilitar el botón de envío cuando el formulario es inválido, utilizando la propiedad `invalid` del objeto `NgForm`.

```html
  <button type="submit" class="btn btn-primary" [disabled]="loginForm.invalid">
    Login
  </button>
```

Además puede añadirse una comprobación extra en el método que maneja el envío del formulario, para asegurarnos de que no se envía un formulario inválido.

```ts
  protected formSubmit(loginForm: NgForm) {
    if (loginForm.valid) {
      console.log('loginForm', loginForm);
      // Navigate to the home page after successful login
      console.log('Form submitted:', loginForm.value);
      loginForm.reset(); // Reset the form after submission
    } else {
      console.log('Form is invalid');
    }
  }
```

#### Submit y Navegación

El proceso de submit es prácticamente el mismo que en el formulario anterior: 

- establecemos el flag `isSending` a true para deshabilitar el botón de submit mientras se procesa la solicitud de login
- lanzamos la llamada a un servicio, responsable del login de cara al backend, pasándole `formaData`, obtenido a partir del valor del `formGroup`
- en la suscripción a la respuesta del servicio, navegamos a la página de login, que cargará el componente de mostrar feedback al usuario tanto en caso de acierto como de error
- añadimos un reset del formulario (ahora con el método `reset()` del FormGroup) para dejarlo limpio para un nuevo intento de login.

```ts
protected formSubmit() {
    if (this.loginForm.valid) {
      this.isSending.set(true);
      const formData: LoginRequest = this.loginForm.value;
      this.#auth
        .login(formData, {
          delayTime: 2000,
        })
        .pipe(takeUntilDestroyed(this.#destroyRef))
        .subscribe((state) => {
          // Navigate to the home page after successful login
          this.#router.navigate(['/auth', 'login', 'after-login'], {
            state,
          });
          this.loginForm.reset(); // Reset the form after submission
          this.isSending.set(false);
        });
    } else {
      // console.log('Form is invalid:', loginForm.value);
      this.#router.navigate(['/auth', 'login', 'after-login'], {
        state: {
          error: 'Form is invalid',
          token: '',
        },
      });
      this.loginForm.reset(); // Reset the form after submission
      this.isSending.set(false);
    }
  }
}
```

En la creación de formData, para adecuarlo al interface LoginForm, podríamos añadir las transformaciones y validaciones que fueran necesarias.

#### Resultado final

El resultado final del componente es el siguiente

```ts
import { JsonPipe } from '@angular/common';
import { Component, inject } from '@angular/core';
import { FormBuilder, FormGroup, ReactiveFormsModule, Validators } from '@angular/forms';
import { Router } from '@angular/router';

interface LoginForm {
  email: string;
  password: string;
  rememberMe: boolean;
}

@Component({
  selector: 'alc-login-form-mdf-rx',
  imports: [ReactiveFormsModule, JsonPipe],
  template: `
    <form [formGroup]="loginForm" (ngSubmit)="formSubmit()">
      <label class="form-control" for="email">
        <span> Email </span>
        <input type="email" id="email" formControlName="email" />
      </label>
      @if (loginForm.controls['email']?.invalid && loginForm.controls['email']?.touched) {
        <div class="error">
          @if (loginForm.controls['email']?.hasError('required')) {
            <p>El correo electrónico es obligatorio.</p>
          }
          @if (loginForm.controls['email']?.hasError('email')) {
            <p>Por favor, introduce una dirección de correo electrónico válida.</p>
          }
        </div>
      }
      <label class="form-control" for="password">
        <span>Password</span>
        <input type="password" id="password" formControlName="password" />
      </label>
      @if (loginForm.controls['password']?.invalid && loginForm.controls['password']?.touched) {
        <div class="error">
          @if (loginForm.controls['password']?.hasError('required')) {
            <p>La contraseña es obligatoria.</p>
          }
          @if (loginForm.controls['password']?.hasError('minlength')) {
            <p>La contraseña debe tener al menos 6 caracteres.</p>
          }
        </div>
      }
      <label class="form-control checkbox" for="rememberMe">
        <input type="checkbox" id="rememberMe" formControlName="rememberMe" />
        <span>Remember me</span>
      </label>
      <div class="form-control">
        <button type="submit" class="btn btn-primary" [disabled]="loginForm.invalid">Login</button>
      </div>
    </form>
    <pre>{{ loginForm.value | json }}</pre>
  `,
  styles: `
   /* Igual que en el caso anterior */
  `,
})
export class LoginFormMdfRx {
  readonly #fb = inject(FormBuilder);
  readonly #router = inject(Router);
  readonly #auth = inject(Auth);

  protected readonly isSending = signal(false);

  readonly loginForm: FormGroup = this.#fb.group({
    email: ['', [Validators.required, Validators.email]],
    password: ['', [Validators.required, Validators.minLength(6)]],
    rememberMe: [false],
  });

  protected formSubmit() {
    if (this.loginForm.valid) {
      this.isSending.set(true);
      const formData: LoginRequest = this.loginForm.value;
      this.#auth
        .login(formData, {
          delayTime: 2000,
        })
        .pipe(takeUntilDestroyed(this.#destroyRef))
        .subscribe((state) => {
          // Navigate to the home page after successful login
          this.#router.navigate(['/auth', 'login', 'after-login'], {
            state,
          });
          this.loginForm.reset(); // Reset the form after submission
          this.isSending.set(false);
        });
    } else {
      // console.log('Form is invalid:', loginForm.value);
      this.#router.navigate(['/auth', 'login', 'after-login'], {
        state: {
          error: 'Form is invalid',
          token: '',
        },
      });
      this.loginForm.reset(); // Reset the form after submission
      this.isSending.set(false);
    }
  }
}
```

#### 👁️‍🗨️Test del componente LoginFormMdfRx

Como la estrategia del test del formulario anterior ha sido en todo momento la de caja negra, es decir testar el comportamiento del componente sin entrar en su implementación, los tests de este componente son exactamente iguales a los del componente anterior, y por tanto no se repiten aquí.

## 📕Signals Forms. Conceptos básicos

Los formularios basados en signals son una nueva forma de crear formularios disponible de forma estable en Angular 22, que nos permite definir el formulario y sus controles desde el componente (igual que en los formularios MD, basados en RxJs ), pero en este caso como signals, y utilizar la directiva `FormField` para enlazar el formulario con el template. Recientemente se ha añadido la directiva `FormRoot` para mejorar el proceso de submit.

En el componente se define

- el **modelo de datos** del formulario como un interface, que nos permite anotar el tipo del valor del formulario y sus controles
- un signal que representa el **estado del formulario**, con un valor inicial de todos los campos, generalmente a cadena vacía, o false en el caso del checkbox
- un objeto de tipo `FieldTree`, creado mor la función `form`, que representa una **vista reactiva** del formulario:
  - es una vista reactiva sobre el signal que representa el estado del formulario
  - se crea a partir de dicha signal
- el contenido de dicha signal es un objeto `FieldState` que representa el **estado del formulario**
  - el estado nos permite acceder a sus propiedades como `value()`, `valid()`, `invalid()`, `dirty()` o `touched` y métodos `reset()`, etc. Son equivalentes a las propiedades y métodos de `FormGroup` y `FormControl`, pero en este caso basados en signals, y por tanto reactivos. (En este caso no existen propiedades como `pristine` o `untouched`)
- como segundo parámetro de la función `form`, podemos pasar un callback que define el **schema del formulario**, con sus validaciones. Este callback recibe como parámetro el path del formulario, que nos permite acceder a los controles del formulario y definir sus validaciones.

Esto supone algunas diferencias importantes en la forma de entender los formularios, por lo que hay quien habla de un cambio de paradigma:

- Tu **modelo** es la fuente de la verdad y define el schema. 
- El formulario es una **vista reactiva** sobre ese Signal. 
- Los errores viven en el **schema**. Con tipado automático basado en el modelo. Fine grained gracias a los Signals.

## Formulario de login basado en signals

### 🧿LoginFormSignalsForm: Signal-based Form

Como primer ejemplo de formulario basado en signals básico, crearemos un formulario de login, con los mismos campos que los formularios anteriores, y con las mismas validaciones.

#### Lógica en el componente

En el componente crearemos un signal para representar el valor o estado del formulario, anotando su tipo de acuerdo al interface correspondiente y dándole un valor inicial de todos los campos a cadena vacía, o false en el caso del checkbox.

```ts
export interface LoginModel {
  email: string;
  password: string;
  rememberMe: boolean;
}


@Component({
  ...
})
export class LoginFormSignals {

  readonly #loginModelInitialState: LoginModel = {
    email: '',
    password: '',
    rememberMe: false,
  };

  // Signal representing the form model
  readonly #loginModel = signal<LoginModel>(this.#loginModelInitialState);

  protected readonly loginFieldTree = form(this.#loginModel)

  protected formSubmit() {
    const fieldTree: FieldTree<LoginModel> = this.loginFieldTree;
    const fieldState: FieldState<LoginModel> = fieldTree();
    const formValue = fieldState.value();

    const fieldEmail: FieldTree<LoginModel['email']> = fieldTree.email;
    const fieldStateEmail: FieldState<LoginModel['email']> = fieldEmail();
    const emailValue = fieldStateEmail.value();
    
    console.log('Form submitted', formValue);
    console.log('Email submitted', emailValue);
  }
}
```

La función `form()` nos devuelve un objeto de tipo `FieldTree`, que representa una **vista reactiva** del formulario. En el se incluye cada uno de los controles del formulario, que son objetos de tipo `FieldTree`.

Si usamos el getter del signal `loginFieldTree`, o de cualquiera de los `FieldTree` de los controles, podemos acceder al `FielState` del formulario, y a sus propiedades y métodos, como `value()`, `valid()`, `invalid()`, `reset()`, etc.

#### Vinculación al template

En el template se utilizan las directivas `FormField` y `FormRoot`, importadas de `@angular/forms/signals` para enlazar el formulario con el template. La segunda de ellas se ha añadido recientemente para mejorar el proceso de submit.

```ts
  imports: [FormRoot, FormField],
  template: `
    <form [formRoot]="loginFieldTree" (submit)="formSubmit()">
      <label class="form-control" for="email">
        <span> Email </span>
        <input type="email" id="email" [formField]="loginFieldTree.email" />
      </label>
      ...
    </form>
  `
```

En este caso, la directiva `formRoot` se aplica al elemento `form`, y la directiva `formField` se aplica a cada uno de los controles del formulario, enlazándolos no con un nombre, sino con las propiedades correspondientes del objeto `loginFieldTree`.

En lugar del evento `ngSubmit`, que no está disponible en los formularios signals, utilizamos el evento nativo `submit` del elemento form, que nos permite ejecutar un método del componente cuando se envía el formulario.

- si se ha usado la directiva `formRoot`, el evento `submit` se controla para que no tenga su valor por defecto, y no recargue la página
- si no se ha usado la directiva `formRoot`, el evento `submit` se comporta como un evento nativo, y para evitar la recarga la página se debe usar `preventDefault()` en el evento recibido en el controlador del submit.

Hay que tener en cuente que tampoco estarán disponibles las clases de CSS que Angular aplica a los formularios y controles, como `ng-valid`, `ng-invalid`, `ng-touched`, `ng-untouched`, etc. por lo que habrá que definir nuestras propias clases de CSS para mostrar el feedback de las validaciones o utilizar las propiedades del objeto `FieldState` de cada control para añadir estilos al feedback de las validaciones.

#### Valor del formulario

De nuevo, como herramienta en desarrollo, que luego eliminaremos, añadimos un \<pre> que nos permite ver el valor del formulario en tiempo real, utilizando el pipe `json` para mostrarlo en formato JSON.

```html
  <pre>{{ loginFieldTree().value() | json }}</pre>
```

En este caso accedemos al estado del formulario, es decir el valor de la signal `loginFieldTree` mediante la llamada a su getter `loginFieldTree()`, y al valor de la propiedad `value` mediante la llamada a su getter `value()`.

#### Validación y feedback

Igual que el los formularios MD, las validaciones se definen en el componente, en este caso en un callback que se pasa como segundo parámetro al método `form()` como **schema** de validación, Esta función recibe el valor del formulario y devuelve un objeto con los errores de validación.

```ts
export class LoginFormSignals {
  ...
  protected readonly loginFieldTree = form(this.#loginFormState, (path) => {
    required(path.email, { message: 'El email es obligatorio' });
    email(path.email, { message: 'El email debe ser una dirección de correo válida' });
    required(path.password, { message: 'La contraseña es obligatoria' });
    minLength(path.password, 6, { message: 'La contraseña debe tener al menos 6 caracteres' });
  });
  ...
}
```

El API de signals forms nos permite definir validaciones para cada uno de los campos del formulario, utilizando funciones como `required`, `minLength`, `maxLength`, `pattern`, `email`, etc. que reciben como primer parámetro el path del campo, y como segundo parámetro un objeto con las opciones de la validación, como el mensaje de error.

La función  que define el schema puede declararse aparte, como cualquier callback, y pasarse como referencia al método `form()`, para mantener el código más limpio y legible, dejando más claro el concepto de **schema de validación** como una propiedad del componente.

```ts
readonly #loginFormSchema = (path: any) => {
  required(path.email, { message: 'El email es obligatorio' });
  email(path.email, { message: 'El email debe ser una dirección de correo válida' });
  required(path.password, { message: 'La contraseña es obligatoria' });
  minLength(path.password, 6, { message: 'La contraseña debe tener al menos 6 caracteres' });
};

protected readonly loginFieldTree = form(this.#loginModel, this.#loginFormSchema);
```

A la hora de mostrar el feedback de las validaciones, podemos utilizar la propiedad `errors` del control representado en el objeto `loginFieldTree`. A diferencia de los formularios reactivos, `errors()` nos devuelve un array con el error de validación activo en el campos del formulario como item 0.

```ts
  @if (loginFieldTree.email()?.invalid() && loginFieldTree.email()?.touched()) {
    <p class="error">{{ loginFieldTree.email().errors()[0].message }}</p>
  }
``` 

Opcionalmente, en lugar acceder al error activo, podemos iterar el array de errores del campo del formulario, mostrando nuevamente el error de validación activo en el campo, utilizando un bucle `for` de Angular. 

```ts
  @if (loginFieldTree.email()?.invalid() && loginFieldTree.email()?.touched()) {
    <ul class="error">
      @for (error of loginFieldTree.email().errors(); track $index) {
        <li>{{ error.message }}</li>
      }
    </ul>
  }
```

También es posible acceder a todo el conjunto de errores del formulario, utilizando la propiedad `errorSummary` del objeto `loginFieldTree`, que nos devuelve un array con los errores de validación de cada uno de los campos del formulario.

```ts
<p>Errores del formulario</p>
<ul class="error">
  @for (error of loginFieldTree().errorSummary(); track $index) {
    <li>{{ error.message }}</li>
  }
</ul>
```

### 📕Submit API en los Signal Forms

Inicialmente este proceso hace uso de diversos elementos, nativos de HTML y de Angular.

- el evento `submit` del elemento form, al que se le asigna un manejador en el componente
- el atributo `novalidate` del elemento form, para evitar que el navegador realice la validación nativa de HTML5
- en el manejador se recibe el evento `submit` y se llama a `preventDefault()` para evitar que el navegador recargue la página
-  la función `submit()`, que recibe un objeto `FieldTree` que representa el formulario, junto con un callback asíncrono, que solo sera ejecutado si el formulario es válido. En el callback suele incluirse la llamada a un servicio que se encargue de enviar los datos del formulario al backend, que se resolverá con la respuesta acertada o con un error. 
- Los errores pueden integrarse en el propio `FieldTree` como `ValidationError` asociados a campos específicos del formulario, integrándose la validación del lado servidor con la validación del lado cliente, y mostrando el feedback de los errores en el template del formulario.
-  Si la función callback retorna errores, el proceso de submit se considera fallido, y el formulario queda en estado inválido, mostrando los errores devueltos por el backend. Si la función callback no retorna un valor (undefined), el proceso de submit se considera exitoso, y el formulario queda en estado válido, mostrando un feedback de éxito al usuario.
  
```ts
protected async submitForm(event: Event) {
  event.preventDefault();
  
  submit(this.loginFieldTree, async (fieldTree: FieldTree<LoginModel>) => {
    const fieldState: FieldState<LoginModel> = fieldTree();
    const formValue = fieldState.value();
    console.log('Form submitted', formValue)
    
    const result = await this.#auth.loginPromise(formValue);

    if (result.status === 'error') {
      // Handle server-side validation errors
      const serverErrors: ValidationError.WithOptionalField[] = []

      if (result.errors) {
        for (const error of result.errors) {
          serverErrors.push({
            field: error.field,
            kind: 'server',
            message: error.message,
          });
        }
      }
    } 
  }
}  
```

-  el estado submit del formulario, que se puede obtener mediante la propiedad `submitted()` del objeto `FieldTree`, y que nos permite mostrar un feedback al usuario de que el formulario se está enviando.
-  el método `reset()` del objeto `FieldTree`, que nos permite resetear el formulario a su estado inicial, pasando a false las propiedades `dirty()` y `touched()` de cada control del formulario, pero sin cambiar su valor inicial. Esto último sucede si en la llamada al método pasamos los valores iniciales, que son asignados al modelo del formulario, utilizando internamente su método `set`.

#### Directiva formRoot

En las últimas actualizaciones del API de Signals en Angular, se ha añadido la directiva `formRoot`, que nos permite simplificar el proceso de submit, evitando tener que utilizar el evento `submit` del elemento form, y el atributo `novalidate`, y evitando tener que llamar en el manejador del submit tanto a `preventDefault()` como a la función `submit()`.

Al usar esta directiva, la llamada a `submit()` se realiza internamente, y la lógica del submit se define como parte del propio formulario, añadiendo un  objeto de opciones al crear el objeto `FieldTree` del formulario, con la propiedad `submission`, que también es un objeto, con la propiedad `action` que recibe un callback que se ejecuta cuando el formulario es válido.

```ts
  protected readonly loginFieldTree = form(
    this.#loginModel, 
    this.#loginFormSchema, 
    { submission: { action: this.submitForm.bind(this) }}
    );
```

El valor de la propiedad `submission.action` es un callback asíncrono sera recibido por submit() y se ejecutara cuando el formulario es válido, recibiendo como parámetro el objeto `FieldTree` del formulario, que nos permite acceder a su estado y valor.

```ts
  protected submitForm(fieldTree: FieldTree<LoginModel>) {
    const fieldState: FieldState<LoginModel> = fieldTree();
    const formValue = fieldState.value();
    console.log('Form submitted', formValue);
    //...
  }
```

Si se usa como callback un método de la clase, hay que hacer un bind del método al objeto `this` de la clase, para que el callback tenga acceso a las propiedades y métodos de la clase.

Este método es asíncrono y puede manejar la llamada al servicio responsable del login, recibiendo una promesa con el resultado, para luego continuar gestionando los errores de servidor o con la navegación a la página de inicio, una vez finalizado el proceso de login.

Al iniciarse la llamada a la función `submission.action`, el estado del formulario pasa a `submitting() = true`, y al finalizar la ejecución de la función tras resolverse la promesa, el estado del formulario pasa a `submitting() = false`. Esto nos permite mostrar un feedback al usuario de que el formulario se está enviando.

#### Submit y Navegación

El proceso de submit incluirá la llamada a un servicio, responsable del login de cara al backend, pasándole los datos de la signal value(), que ya están tipados de acuerdo con el interface LoginForm. 

En nuestro caso no nos importa el resultado de la `submission.action`, ya que en cualquier caso finalizado el login, se navega a la página de login, utilizando el servicio `Router` de Angular para pasea en el state la información con los resultados del login.

Aun así, si queremos conservar la funcionalidad de que el estado del formulario pase a `submitting() = false` al finalizar la ejecución de la función, debemos hacer que la función devuelva una promesa que se resuelva con un valor void, para que el submit() interno de la directiva `formRoot` pueda finalizar correctamente.

Nuestro servicio de login ya dispone de una función `loginPromise()` que devuelve una promesa, utilizando la función `firstValueFrom()` de RxJs, por lo que podemos hacer un `await` de la llamada a dicha función, y devolver un valor void al finalizar la ejecución de la función. Si no existe esa función en el servicio, la conversión mediante `firstValueFrom()` puede hacerse en el componente.

Una vez resuelta la promesa del método del servicio, podemos navegar incorporando su resultado a la ruta y resetear el formulario, tanto su estado como su valor, para dejarlo limpio para un nuevo intento de login.

```ts
  protected readonly loginFieldTree = form(this.#loginModel, this.#loginFormSchema, {
    submission: {
      action: this.submitForm.bind(this),
    },
  });

 protected async submitForm(loginFieldTree: FieldTree<LoginModel>) {
    const formData = loginFieldTree().value();
    const state = await this.#auth.loginPromise(formData);
    // Reset the form after submission
    loginFieldTree().reset(this.#loginModelInitialState);
    // Navigate to the home page after successful login
    this.#router.navigate(['/auth', 'login', 'after-login'], {
      state,
    });
  }
```
En la creación de formData podríamos añadir las transformaciones y validaciones que fueran necesarias.

Otra alternativa es que el propio `submission.action`, que también es un callback asíncrono, devuelva una promesa von valor distinto de void. En ese caso Angular entiende que es un error, y el estado del formulario pasa a `submitting() = false`, pero el formulario queda en estado inválido, mostrando los errores devueltos por el backend. Si la función callback no retorna un valor (undefined), el proceso de submit se considera exitoso, y el formulario queda en estado válido, mostrando un feedback de éxito al usuario. 

#### Resultado final

El resultado final del componente es el siguiente

```ts
export type LoginModel = LoginRequest;

@Component({
  selector: 'alc-login-form-signals',
  imports: [FormRoot, FormField, JsonPipe],
  template: `
    <form [formRoot]="loginFieldTree">
      <label class="form-control" for="email">
        <span> Email </span>
        <input type="email" id="email" [formField]="loginFieldTree.email" />
      </label>
      @if (loginFieldTree.email()?.invalid() && loginFieldTree.email()?.touched()) {
        <p class="error">{{ loginFieldTree.email().errors()[0].message }}</p>
      }
      <label class="form-control" for="password">
        <span>Password</span>
        <input type="password" id="password" [formField]="loginFieldTree.password" />
      </label>
      @if (loginFieldTree.password()?.invalid() && loginFieldTree.password()?.touched()) {
        <p class="error">{{ loginFieldTree.password().errors()[0].message }}</p>
      }
      <label class="form-control checkbox" for="rememberMe">
        <input type="checkbox" id="rememberMe" [formField]="loginFieldTree.rememberMe" />
        <span>Remember me</span>
      </label>
      <div class="form-control" [class.sending]="loginFieldTree().submitting()">
        <button type="submit" class="btn btn-primary" [disabled]="loginFieldTree().invalid()">
          Login
        </button>
      </div>
    </form>
    <div aria-label="Form Value" class="form-value">
      <p>Valor del formulario</p>
      <pre>{{ loginFieldTree().value() | json }}</pre>
      <p>Valor de errores activos del formulario</p>
      <pre>{{ loginFieldTree().errorSummary() | json }}</pre>
      <p>Errores activos del formulario</p>
      <ul class="error">
        @for (error of loginFieldTree().errorSummary(); track $index) {
          <li>{{ error.message }}</li>
        }
      </ul>
    </div>
  `,
  styleUrls: ['../forms.css'],
  styles: `
    ul.error {
      list-style: none;
      padding: 0;
      margin: 0;
    }
    .form-value {
      margin-top: 2rem;
      padding: 1rem;
      border: 1px solid #ccc;
      background-color: #f9f9f9;
    }
    .sending {
      pointer-events: none;
      position: relative;

      button {
        color: transparent;
      }

      &:before {
        content: '';
        position: absolute;
        background-color: var(--color-background);
        top: 0;
        bottom: 0;
        left: 0;
        right: 0;
        opacity: 0.8;
      }

      &::after {
        content: 'Sending...';
        font-size: 0.9rem;
        position: absolute;
        top: 0;
        bottom: 0;
        left: 0;
        right: 0;
        color: var(--color-primary);
        margin-top: 0.5rem;
        z-index: 10;
      }
    }
  `,
})
export class LoginFormSignals {
  readonly #router = inject(Router);
  readonly #auth = inject(Auth);

  readonly #loginModelInitialState: LoginModel = {
    email: '',
    password: '',
    rememberMe: false,
  };

  // Signal representing the form model
  readonly #loginModel = signal<LoginModel>(this.#loginModelInitialState);

  // Sin validaciones
  // protected readonly loginFieldTree = form(this.#loginModel)

  // Añadiendo validaciones
  // protected readonly loginFieldTree = form(this.#loginModel, (path) => {
  //   required(path.email, { message: 'El email es obligatorio' });
  //   email(path.email, { message: 'El email debe ser una dirección de correo válida' });
  //   required(path.password, { message: 'La contraseña es obligatoria' });
  //   minLength(path.password, 6, { message: 'La contraseña debe tener al menos 6 caracteres' });
  // });

  // Schema de validación como propiedad de la clase

  readonly #loginFormSchema = (path: SchemaPathTree<LoginModel>) => {
    required(path.email, { message: 'El email es obligatorio' });
    email(path.email, { message: 'El email debe ser una dirección de correo válida' });
    required(path.password, { message: 'La contraseña es obligatoria' });
    minLength(path.password, 6, { message: 'La contraseña debe tener al menos 6 caracteres' });
  };

  protected readonly loginFieldTree = form(this.#loginModel, this.#loginFormSchema, {
    submission: {
      action: this.submitForm.bind(this),
    },
  });

  // Muestra información que ayuda a entender los signal forms
  // Se puede eliminar completamente
  constructor() {
    const fieldTree: FieldTree<LoginModel> = this.loginFieldTree;
    const fieldState: FieldState<LoginModel> = fieldTree();
    const formValue = fieldState.value();
    console.log('Form value:', formValue);

    const fieldEmail: FieldTree<LoginModel['email']> = fieldTree.email;
    const fieldStateEmail: FieldState<LoginModel['email']> = fieldEmail();
    const emailValue = fieldStateEmail.value();
    console.log('Email value:', emailValue);
  }

  // Versión donde el servicio login devuelve una promesa
  protected async submitForm(loginFieldTree: FieldTree<LoginModel>) {
    console.log('Form submission');

    const formData = loginFieldTree().value();
    const state = await this.#auth.loginPromise(formData);
    console.log('Form submitted:', state);
    // Reset the form after submission
    loginFieldTree().reset(this.#loginModelInitialState);
    // Navigate to the home page after successful login
    this.#router.navigate(['/auth', 'login', 'after-login'], {
      state,
    });
  }
}
```

#### 👁️‍🗨️Test del componente LoginFormSignals

Nuevamente, como la estrategia de los tests de los formularios anteriores ha sido en todo momento la de caja negra, es decir testar el comportamiento del componente sin entrar en su implementación, los tests de este componente son similares a los de los componentes anterior, con una diferencia en un selector y sobre todo, un cambio respecto al uso del servicio, que ahora corresponde a un nuevo método, basado en promesas.

Para acceder a los errores de un control concreto usaremos `form .error` en lugar de `.error`, ya que fuera del formulario se han añadido los errores sumarios, usando también para ellos la clase `error`. 

Eliminamos el caso de uso `it('should NOT submit a INVALID form', ...)` ya que el submit de un formulario inválido no se ejecuta, y por tanto no hay que testarlo.

El método del servicio del que se crea un mock esta ahora basado en promesas:

```ts
providers: [
  {
    provide: Auth,
    useValue: {
      loginPromise: vi.fn().mockResolvedValue({}),
    },
  },
],
```

Al testar el submit, hay que tener en cuenta su asincronía, y por tanto el test debe ser asíncrono, y no es suficiente con hacer un `await` de la llamada a `fixture.whenStable()` para esperar a que se resuelva la promesa devuelta por el método del servicio.

En este caso, para esperar a que se resuelva la promesa devuelta por el método del servicio, podemos hacer un `await` de la llamada a `vi.runAllTicks()`, o utilizar `vi.waitFor` que nos permite esperar a que se cumpla una condición, en este caso que el método del servicio haya sido llamado.

```ts
it('should enable the submit button when the form is valid', async () => {
  vi.spyOn(router, 'navigate');
  vi.spyOn(console, 'log');
  //.mockImplementation(() => Promise.resolve(true));
  fixture.detectChanges();
  emailInput.value = 'test@example.com';
  emailInput.dispatchEvent(new Event('input'));
  passwordInput.value = 'password';
  passwordInput.dispatchEvent(new Event('input'));
  rememberMeInput.checked = true;
  rememberMeInput.dispatchEvent(new Event('change'));
  fixture.detectChanges();
  expect(submitButton.disabled).toBe(false);
  submitButton.click();
  fixture.detectChanges();
  expect(component['loginFieldTree']().submitting()).toBe(true);
  expect(auth.loginPromise).toHaveBeenCalledWith({
    email: 'test@example.com',
    password: 'password',
    rememberMe: false,
  });

  await vi.waitFor(() => {
    const state = {};
    expect(component['loginFieldTree']().submitting()).toBe(false);
    expect(router.navigate).toHaveBeenCalledWith(['/auth', 'login', 'after-login'], { state });
  });
});
```

## 📕Custom Controls

Angular define una serie de interfaces para **componentes personalizados**, `FormValueControl` y  `FormCheckboxControl`, que nos permiten crear componentes personalizados que se integren con el sistema de formularios de Angular. Cuando el componente implementa una de estas interfaces, nos permitan utilizar la directiva `FormField` para enlazar automáticamente el control personalizado con el estado, la validación y el enlace de datos del formulario.

Estos componentes se encargan de renderizar el control nativo de HTML. Externamente, reciben como input el `FieldTree` del control que representa el input o checkbox. 

Internamente utilizan obligatoriamente una `modelSignal`, para enlazar el valor del control con el valor del `FieldState`, y para actualizar el estado del control cuando cambia su valor.

```ts
readonly value = model.required<T>(); // bind to fieldState.value()
```

Ademas, el interface permite definir como inputs muchas de las propiedades del control, como `disabled`, `required`, `readonly`, `touched`, `invalid`, `errors` etc, que se enlazan automáticamente con los valores correspondientes del estado del control (fieldState).

```ts
readonly disabled = input.required<boolean>();
readonly required = input.required<boolean>();
readonly touched = model.required<boolean>();
readonly invalid = input.required<boolean>();
readonly errors = input.required<readonly ValidationError[]>();
```

Opcionalmente, se podrían usar estos valores y mostrar los errores de validación si los hay en función de los estados touched e invalid.

Otras alternativa es que un componente independiente se ocupe de mostrar los errores de validación, y que se pueda reutilizar en cualquier formulario y con cualquier control.

## Custom Controls en el login

En el formulario de login vemos que se repiten los inputs y que los checkboxes tienen un templete definido con el que podría pasar lo mismo.

En ambos casos podemos crear **componentes personalizados** para cada uno de estos controles, que nos permitan reutilizarlos en otros formularios, y que nos permitan encapsular la lógica de validación y feedback de cada control. De esta forma estaremos creando un **sistema de diseño** (Design System) propio, que nos permitirá mantener la consistencia visual y funcional de nuestra aplicación.

Creamos en el core los nuevos componentes `Input`, `CheckBox` y `Button`, que nos permitirán crear un sistema de diseño consistente, y que nos permitirán reutilizar los controles en otros formularios.

```shell
ng g c core/design/input --project course-02
ng g c core/design/check-box --project course-02
ng g c core/design/button --project course-02
```

### 🧿Custom Input

En el contexto de los formularios basados en señales, los controles personalizados dependen directamente del `FieldTree`, que será gestionado por la directive `FormField`, lo que permitirá

- integrar fácilmente las validaciones del formulario y el feedback de los errores
- que se puedan utilizar en cualquier formulario basado en signals. 

Podemos crear un componente personalizado para los inputs de tipo texto, que reciba como inputs 

- el texto que debe mostrar (label) y 
- el `FieldTree` del control que representa el input
 
El componente se encargue de

- renderizar el input nativo de HTML y su label
- enlazar el estado del input con el valor del FieldState
- mostrar los errores de validación si los hay.

```ts
import { Component, input } from '@angular/core';
import { FieldState, FormField } from '@angular/forms/signals';

@Component({
  selector: 'alc-input',
   imports: [],
  template: `
    <label for="{{ id() }}" class="form-control">
      <input
        type="{{ type() }}"
        id="{{ id() }}"
        placeholder=" "
        [value]="value()"
        (input)="value.set($event.target.value)"
        (blur)="touched.set(true)"
      />
      <span>{{ label() }}</span>
    </label>
    @if (invalid() && touched()) {
      <p class="error">
        {{ errors()?.[0]?.message }}
      </p>
    }
  `,
  styles: ``,
})
export class Input {

  readonly value = model.required<string>();
  readonly errors = input([] as readonly ValidationError[]);
  readonly invalid: InputSignal<boolean> = input(false);
  readonly touched = model(false);


  readonly type = input<string>('text');
  readonly label = input.required<string>();
  readonly id = computed<string>(() => this.#generateId());
  
  #generateId() {
    return `${this.type()}-${Math.random().toString(36).slice(2, 9)}`;
  }
}
```

En la plantilla, el valor del placeholder se establece a un carácter espacio, para que el label se muestre correctamente cuando el input está vacío. El label se mueve hacia arriba cuando el input está enfocado o tiene valor, y vuelve a su posición original cuando el input está vacío y no está enfocado.

En el componente la modelSignal `value` se enlaza con el valor del FieldState del control, y se actualiza cuando el usuario escribe en el input. La propiedad `errors` se enlaza con los errores de validación del control, y la propiedad `invalid` se enlaza con el estado de validación del control.

La propiedad `touched` es un signalModel que se establece a true cuando el input pierde el foco, y que nos permite mostrar los errores de validación solo cuando el usuario ha interactuado con el control.

#### Estilos del componente 

Los estilos del componente permiten darle el aspecto adecuado para conseguir un sistema de diseño consistente, y se pueden personalizar según las necesidades del proyecto.

- un label que se mueve hacia arriba cuando el input está enfocado o tiene valor
- un borde inferior que cambia de color cuando el input está enfocado o tiene error.

```css
.form-control {
  padding-block-start: 1rem;
  display: flex;
  flex-direction: column;
  gap: 0.5rem;
}
input,
textarea {
  padding: 0.5rem;
  font-size: 1rem;
  color: var(--color-primary-hot);
  background-color: var(--color-background-primary);
  border: none;
  border-block-end: 2px solid var(--color-primary);
  border-radius: 4px;

  &:focus-visible {
    outline: var(--color-primary) auto 1px;
    background-color: var(--color-background);
  }
}

span {
  font-size: 0.8rem;
  color: var(--color-primary);
  margin-bottom: 0.2rem;
  position: relative;
  top: -1rem;
}

input:focus-visible + span,
input:not(:placeholder-shown) + span {
  color: var(--color-primary-hot);
  top: -4rem;
}

.error {
  color: var(--color-tertiary);
  font-size: 0.8rem;
}
```

El uso desde el formulario se simplifica al máximo, ya que solo hay que pasar el FieldTree del control correspondiente, y el componente se encarga de todo lo demás.

```html
<alc-input
  [type]="'opcional; text se toma por defecto'"
  [label]="'Objetivo del input'"
  [formField]="registerForm.control"
/>
```

#### 👁️‍🗨️Alternativas en el Test del componente Input

El test de este componente presenta algunas particularidades:

- como en todos los componentes que definen inputs obligatorios, tenemos que proporcionárselos en los test, utilizando como ya hemos visto el método `setInput()` de `fixture.componentRef`.
- la función `form()` nos permite crear un objeto de tipo `FieldTree` a partir de un signal y un esquema de validación, pero no podemos utilizarla fuera de un contexto de inyección como el que proporciona un componente, o en nuestro caso, `TestBed` con su método `runInInjectionContext`.
- aunque tengamos un objeto de tipo `FieldTree`, no podemos pasarlo directamente al componente Input, ya que este no tiene un input de tipo `FieldTree`, que realmente existe en la directiva `FormField
- `FormField` no está dentro de Input: Angular la instancia sobre el elemento <alc-input> cuando compila el template del formulario, y le pasa como input el FieldTree del control correspondiente. 

Existen tres alternativas

- Para un test unitario puro de Input, usar TestBed.createComponent(Input) y probar solamente su API y DOM, sin esperar sincronización con FieldTree.
- Para probar la integración Input + FormField, se puede crear un HostComponent en el test, que utilice nuestro componente Input, y testarlo de la forma estándar.
- La creación programática del Input funciona sin host, y permite añadir la directiva `FormField` pero añade bastante infraestructura,  dejando de usar ComponentFixture.

#### 👁️‍🗨️Test unitario del componente Input

Se crea un mockFieldTree ejecutando la función form con el mockModel y el schema en el contexto de inyección de TestBed, para que las señales funcionen correctamente

```ts input.spec.ts
interface MockModel {
  name: string;
}
const mockModel = signal<MockModel>({
  name: '',
});

const schema = (path: SchemaPathTree<MockModel>) => {
  required(path.name, { message: 'Name is required' });
};


describe('Input', () => {
  let component: Input;
  let fixture: ComponentFixture<Input>;
  let mockFieldTree: FieldTree<MockModel>;

  beforeEach(async () => {
    mockFieldTree = TestBed.runInInjectionContext(() => {
      return form(mockModel, schema);
    });
    //...
  }
}
```

No se vincula el FieldTree al componente Input, ya que no se está probando la integración con un formulario real, sino el comportamiento del componente Input de manera aislada.

```ts input.spec.ts
describe('Input', () => {
  //...

  beforeEach(async () => {
    //...
    fixture.componentRef.setInput('label', 'Testing input');
    fixture.componentRef.setInput('type', 'text');

    // No hay vinculación con el formField del FieldTree, 
    // Solo inicialización de los valores de los inputs del componente
    fixture.componentRef.setInput('value', mockFieldTree.name());
    fixture.componentRef.setInput('errors', mockFieldTree.name().errors());
    fixture.componentRef.setInput('invalid', mockFieldTree.name().invalid());
    fixture.componentRef.setInput('touched', mockFieldTree.name().touched());
    await fixture.whenStable();
  }
}
```

El FieldTree solo aporta los valores iniciales y las validaciones para el componente Input, pero no se espera que el componente Input actualice el FieldTree en esta prueba aislada.

En los test unitarios del componente Input, podemos comprobar

- que se renderiza la label
- que se establece correctamente el tipo
- que si se escribe en el input, la información de recoge en la signal que representa el model del formulario
- que si el input pierde el foco y el control es inválido, se muestra el error de validación correspondiente.

```ts input.spec.ts
it('should have the correct label', () => {
  const labelElement: HTMLLabelElement = fixture.nativeElement.querySelector('label');
  expect(labelElement.textContent).toBe('Testing input');
});

it('should have the correct input type', () => {
  const inputElement: HTMLInputElement = fixture.nativeElement.querySelector('input');
  expect(inputElement.type).toBe('text');
});

it('should update the model when the input value changes', async () => {
  const inputElement: HTMLInputElement = fixture.nativeElement.querySelector('input');
  inputElement.value = 'New Value';
  inputElement.dispatchEvent(new Event('input'));
  inputElement.dispatchEvent(new Event('blur'));
  await fixture.whenStable();
  expect(component.value()).toBe('New Value');
  expect(component.touched()).toBe(true);
  expect(component.invalid()).not.toBe(false);
  // The invalid state is NOT updated automatically in this test
  // because the form control is not bound to the input component in this isolated test.
  // The invalid state would be updated in a real application
  // where the form control is bound to the input component.
  // expect(mockModel().name).toBe('New Value');
});

it('should display an error message when the field is invalid and touched', async () => {
  const inputElement: HTMLInputElement = fixture.nativeElement.querySelector('input');
  inputElement.value = '';
  inputElement.dispatchEvent(new Event('input'));
  inputElement.dispatchEvent(new Event('blur'));
  await fixture.whenStable();
  fixture.detectChanges();
  expect(component.touched()).toBe(true);
  expect(component.invalid()).toBe(true); // initial value
  const errorElement: HTMLParagraphElement = fixture.nativeElement.querySelector('p.error');
  expect(errorElement).toBeTruthy();
  expect(errorElement.textContent).toContain('Name is required');
});
```

#### 👁️‍🗨️Test del componente Input en un HostComponent

Se crea un HostComponent que utilice el componente Input, y se testa la integración del Input con el FieldTree y la directiva FormField.

```ts input.bounded.spec.ts
interface MockModel {
  name: string;
}

const  MOCK_MODEL = signal<MockModel>({
    name: '',
  });


@Component({
  selector: 'alc-test-host',
  imports: [Input, FormField],
  template: `
    <form>
      <alc-input
        [label]="'Testing input'"
        [type]="'text'"
        [formField]="fieldTree.name"
      />
    </form>
  `,
})
class TestHostComponent {
  mockModel = MOCK_MODEL;
  schema = (path: SchemaPathTree<MockModel>) => {
    required(path.name, { message: 'Name is required' });
  };

  fieldTree = form(this.mockModel, this.schema);
}
```

En los test tendremos los mismos casos que en el test unitario del componente Input, pero ahora el FieldTree está vinculado al componente Input, y se espera que el valor del FieldTree se actualice cuando se escriba en el input.

```ts input.bounded.spec.ts
it('should have the correct label', () => {
  const labelElement: HTMLLabelElement = fixture.nativeElement.querySelector('label');
  expect(labelElement.textContent).toBe('Testing input');
});

it('should have the correct input type', () => {
  const inputElement: HTMLInputElement = fixture.nativeElement.querySelector('input');
  expect(inputElement.type).toBe('text');
});

it('should update the model when the input value changes', async () => {
  // Initial state
  expect(customInput.value()).toBe('');
  expect(customInput.touched()).toBe(false);
  expect(customInput.invalid()).toBe(true);
  // Input change simulation
  const inputElement: HTMLInputElement = fixture.nativeElement.querySelector('input');
  inputElement.value = 'New Value';
  inputElement.dispatchEvent(new Event('input'));
  inputElement.dispatchEvent(new Event('blur'));
  await fixture.whenStable();
  // Expectation after input change
  expect(customInput.value()).toBe('New Value');
  expect(customInput.touched()).toBe(true);
  expect(customInput.invalid()).toBe(false);
  expect(MOCK_MODEL().name).toBe('New Value');
});

it('should display an error message when the field is invalid and touched', async () => {
  const inputElement: HTMLInputElement = fixture.nativeElement.querySelector('input');
  inputElement.value = '';
  inputElement.dispatchEvent(new Event('input'));
  inputElement.dispatchEvent(new Event('blur'));
  await fixture.whenStable();
  fixture.detectChanges();
  const errorElement: HTMLParagraphElement = fixture.nativeElement.querySelector('p.error');
  expect(errorElement).toBeTruthy();
  expect(errorElement.textContent).toContain('Name is required');
});
```

#### 👁️‍🗨️Test programático integrando el componente Input y el FieldTree

Utilizamos un enfoque alternativo a los test de componentes, donde normalmente TestBed.createComponent(Component) crea un fixture y un componente, y nos permite acceder a su DOM y a sus propiedades.

En este caso, utilizamos el createComponent() de Angular, no el de testing, que se emplea en los procesos de creación dinámica de componentes y que nos devolverá un ComponentRef. En esta creación del componente la podremos vincular la directiva FormField y los inputs del componente Input. Ademas necesitamos inyectar ApplicationRef como aplicación a la que engancharemos el componente, mediante su hostView, que actuara de forma similar a la fixture habitual, en los tests.

```ts input.no-host.spec.ts

describe('Input without host', () => {
  let componentRef: ComponentRef<Input>;
  let applicationRef: ApplicationRef;
  let mockModel: ReturnType<typeof signal<MockModel>>;
  let mockFieldTree: FieldTree<MockModel>;
  let host: HTMLElement
  beforeEach(async () => {
    await TestBed.configureTestingModule({
      imports: [Input, FormField],
    }).compileComponents();

    mockModel = signal({ name: '' });
    mockFieldTree = TestBed.runInInjectionContext(() =>
      form(mockModel, (path: SchemaPathTree<MockModel>) => {
        required(path.name, { message: 'Name is required' });
      }),
    );

    applicationRef = TestBed.inject(ApplicationRef);
    componentRef = createComponent(Input, {
      environmentInjector: TestBed.inject(EnvironmentInjector),
      directives: [
        {
          type: FormField,
          bindings: [inputBinding('formField', () => mockFieldTree.name)],
        },
      ],
      bindings: [inputBinding('label', () => 'Testing input'), inputBinding('type', () => 'text')],
    });
    applicationRef.attachView(componentRef.hostView);
    componentRef.changeDetectorRef.detectChanges();
    await applicationRef.whenStable();
    host = componentRef.location.nativeElement;
  });

  afterEach(() => {
    applicationRef.detachView(componentRef.hostView);
    componentRef.destroy();
  });
});
```

En cuanto a los casos de prueba, podemos comprobar nuevamente

- que se renderiza la label
- que se establece correctamente el tipo
- que si se escribe en el input, la información de recoge en la signal que representa el model del formulario
- que si el input pierde el foco y el control es inválido, se muestra el error de validación correspondiente.

```ts
it('should have the correct label', () => {
  const labelElement: HTMLLabelElement = host.querySelector('label')!;
  expect(labelElement.textContent).toBe('Testing input');
});

it('should have the correct input type', () => {
  const inputElement: HTMLInputElement = host.querySelector('input')!;
  expect(inputElement.type).toBe('text');
});


it('binds FormField to the custom control', async () => {
  const component = componentRef.instance;
  expect(componentRef.injector.get(FormField)).toBeTruthy();
  expect(component.invalid()).toBe(true);

  const inputElement: HTMLInputElement = host.querySelector('input')!;
  inputElement.value = 'New Value';
  inputElement.dispatchEvent(new Event('input'));
  inputElement.dispatchEvent(new Event('blur'));
  await applicationRef.whenStable();

  expect(component.value()).toBe('New Value');
  expect(component.touched()).toBe(true);
  expect(component.invalid()).toBe(false);
  expect(mockModel().name).toBe('New Value');
});

it('should display an error message when the field is invalid and touched', async () => {
  const inputElement: HTMLInputElement = host.querySelector('input')!;
  inputElement.value = '';
  inputElement.dispatchEvent(new Event('input'));
  inputElement.dispatchEvent(new Event('blur'));
  await applicationRef.whenStable();

  const errorElement: HTMLParagraphElement = host.querySelector('p.error')!;
  expect(errorElement).toBeTruthy();
  expect(errorElement.textContent).toContain('Name is required');
});
  ```

### 🧿Custom CheckBox

Siguiendo el ejemplo anterior podemos crear un componente personalizado para los inputs de tipo checkbox. El componente 

- recibe como inputs el texto que debe mostrar (label) y el FieldTree del control que representa el checkbox, 
- implementa la interfaz `FormCheckboxControl` de Angular, que nos permite utilizar la directiva `FormField` para enlazar automáticamente el estado del checkbox con el valor del FieldTree, y de mostrar los errores de validación si los hay.
- expone un modelSignal `checked` que se enlaza con el valor del FieldState, y que se actualiza cuando el usuario interactúa con el checkbox.
- expone también los inputs `disabled`, `required`, `touched`, `invalid` y `errors`, que se enlazan automáticamente con los valores correspondientes del estado del control (fieldState).
- añadimos como opcional un input `id` para aportar el id del input, o creamos uno cuando no se suministra.   

```ts
import { Component, input } from '@angular/core';
import { FieldState, FormField } from '@angular/forms/signals';

@Component({
  selector: 'alc-check-box',
  imports: [],
  template: `
    <label for="{{ id() }}" class="custom-checkbox">
      <input type="checkbox" 
        [id]="id()" 
        [checked]="checked()" 
        (change)="checked.set(!checked())"   
        (blur)="touched.set(true)"/>
      <span class="check-mark"></span>
      <span class="label-text">{{ label() }}</span>
    </label>
    @if (invalid() && touched()) {
      <p class="error">
        {{ errors()?.[0]?.message }}
      </p>
    }
  `,
  styles: ``,
})
export class CheckBox implements FormCheckboxControl {
  readonly checked = model.required<boolean>();

  readonly errors = input([] as readonly ValidationError[]);
  readonly invalid = input(false);
  readonly touched = model(false);

  readonly label = input.required<string>();
  readonly id = input<string>(this.generateId());

  generateId() {
    return `checkbox-${Math.random().toString(36).slice(2, 9)}`;
  }
}
```

Como ya hemos visto, el uso desde el formulario se simplifica al máximo, ya que solo hay que pasar el FieldState del control correspondiente, y el componente se encarga de todo lo demás.

```html
<alc-check-box
  [label]="'Remember me'"
  [formField]="loginFieldTree.rememberMe"
/>
```

En el css podemos usar un enfoque similar al de material design, que ya hemos usado en el input, con:

- un label que se mueve hacia arriba cuando el input está enfocado o tiene valor,
- un borde inferior que cambia de color cuando el input está enfocado o tiene error.

```css
.custom-checkbox {
  display: flex;
  align-items: center;
  gap: 8px;
  padding: 0.8rem 0;
  cursor: pointer;
  user-select: none;

  input[type='checkbox'] {
    margin: 0;
    opacity: 0;
  }
}

.check-mark {
  width: 18px;
  height: 18px;
  border: 2px solid #ddd;
  border-radius: 3px;
  display: inline-block;
  position: relative;
}

.custom-checkbox input[type='checkbox']:checked + .check-mark {
  background-color: var(--color-secondary);
  border-color: var(--color-tertiary-hot);
}

.custom-checkbox input[type='checkbox']:checked + .check-mark::after {
  content: '✓';
  position: absolute;
  color: var(--color-background);
  font-size: 14px;
  top: -2px;
  left: 2px;
}

.error {
  color: var(--color-tertiary);
  font-size: 0.8rem;
}
```

#### 👁️‍🗨️Test del componente CheckBox

La preparación del test es exactamente igual que en el caso del componente Input, con la diferencia de que el FieldTree es de tipo booleano, y por tanto el signal que representa el model del formulario debe ser de tipo booleano.

De nuevo tendremos las tres alternativas que ya vimos en el caso del componente Input, y los casos de prueba serán exactamente iguales que en el caso del componente Input, con la diferencia de que el valor del FieldState es booleano, y por tanto el valor del input es true o false.

El test unitario puro del componente CheckBox, sin vinculación con el FieldTree, sería algo así:

```ts check-box.spec.ts
interface MockModel {
  isTest: boolean;
}

const mockModel = signal<MockModel>({
  isTest: false,
});

const schema = (path: SchemaPathTree<MockModel>) => {
  required(path.isTest, { message: 'Checkbox is required' });
};

describe('CheckBox', () => {
  let component: CheckBox;
  let fixture: ComponentFixture<CheckBox>;
  let mockFieldTree: FieldTree<MockModel>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      imports: [CheckBox, FormField],
    }).compileComponents();

    fixture = TestBed.createComponent(CheckBox);
    component = fixture.componentInstance;

    mockFieldTree = TestBed.runInInjectionContext(() => {
      return form(mockModel, schema);
    });

    fixture.componentRef.setInput('label', 'Testing checkbox');

    // No hay vinculación con el formField del FieldTree,
    // Solo inicialización de los valores de los inputs del componente
    fixture.componentRef.setInput('checked', mockFieldTree.isTest().value());
    fixture.componentRef.setInput('errors', mockFieldTree.isTest().errors());
    fixture.componentRef.setInput('invalid', mockFieldTree.isTest().invalid());
    fixture.componentRef.setInput('touched', mockFieldTree.isTest().touched());
    await fixture.whenStable();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });

  it('should render the label', () => {
    const labelElement: HTMLLabelElement = fixture.nativeElement.querySelector('label');
    expect(labelElement.textContent).toContain('Testing checkbox');
  });

  it('should toggle the checkbox state', async () => {
    // Initial values
    expect(mockFieldTree.isTest().value()).toBe(false);
    const checkboxElement: HTMLInputElement =
      fixture.nativeElement.querySelector('input[type="checkbox"]');
    expect(checkboxElement.checked).toBe(false);

    // Simulate a click to toggle the checkbox
    checkboxElement.click();
    checkboxElement.dispatchEvent(new Event('blur'));
    await fixture.whenStable();
    fixture.detectChanges();

    // Check the updated values
    expect(checkboxElement.checked).toBe(true);
    expect(component.touched()).toBe(true);
    // Note: The model's value is not automatically updated in this isolated test
    // because the form control is not bound to the checkbox component in this isolated test.
    // The model's value would be updated in a real application
    // where the form control is bound to the checkbox component.
    expect(mockFieldTree.isTest().value()).not.toBe(true); //Received false
  });

  it.skip('should display an error message when the field is invalid and touched', async () => {
    const checkboxElement: HTMLInputElement =
      fixture.nativeElement.querySelector('input[type="checkbox"]');
    checkboxElement.checked = false;
    checkboxElement.dispatchEvent(new Event('input'));
    checkboxElement.dispatchEvent(new Event('blur'));
    await fixture.whenStable();
    fixture.detectChanges();

    expect(component.touched()).toBe(true);
    expect(component.invalid()).toBe(true); // initial value
    const errorElement: HTMLElement = fixture.nativeElement.querySelector('.error');
    expect(errorElement).toBeTruthy();
    expect(errorElement.textContent).toContain('Checkbox is required');
  });
});
```

El test de integración del componente CheckBox con el FieldTree y la directiva FormField, sería algo así:

```ts check-box.bounded.spec.ts
interface MockModel {
  isTest: boolean;
}

const  MOCK_MODEL = signal<MockModel>({
    isTest: false,
  });


@Component({
  selector: 'alc-test-host',
  imports: [CheckBox, FormField],
  template: `
    <form>
      <alc-check-box
        [label]="'Testing checkbox'"
        [formField]="fieldTree.isTest"
      />
    </form>
  `,
})
class TestHostComponent {
  mockModel = MOCK_MODEL;
  schema = (path: SchemaPathTree<MockModel>) => {
    required(path.isTest, { message: 'Checkbox is required' });
  };

  fieldTree = form(this.mockModel, this.schema);
}

describe('TestHostComponent', () => {
  let component: TestHostComponent;
  let fixture: ComponentFixture<TestHostComponent>;
  let customInput: CheckBox;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      imports: [CheckBox],
    }).compileComponents();

    fixture = TestBed.createComponent(TestHostComponent);
    component = fixture.componentInstance;
    customInput = fixture.debugElement.query(By.directive(CheckBox)).componentInstance;
    await fixture.whenStable();
  });

  it('should create TestHostComponent & Input', () => {
    expect(component).toBeTruthy();
    expect(customInput).toBeTruthy();
  });

  it('should have the correct label', () => {
    const labelElement: HTMLLabelElement = fixture.nativeElement.querySelector('label');
    expect(labelElement.textContent).toBe('Testing checkbox');
  });

  it('should have the correct input type', () => {
    const inputElement: HTMLInputElement = fixture.nativeElement.querySelector('input');
    expect(inputElement.type).toBe('checkbox');
  });

  it('should update the model when the input value changes', async () => {
    // Initial state
    expect(customInput.checked()).toBe(false);
    expect(customInput.touched()).toBe(false);
    expect(customInput.invalid()).toBe(true);
    // Input change simulation
    const inputElement: HTMLInputElement = fixture.nativeElement.querySelector('input');
    inputElement.value = 'New Value';
    inputElement.dispatchEvent(new Event('input'));
    inputElement.dispatchEvent(new Event('blur'));
    await fixture.whenStable();
    // Expectation after input change
    expect(customInput.checked()).toBe(true);
    expect(customInput.touched()).toBe(true);
    expect(customInput.invalid()).toBe(false);
    expect(MOCK_MODEL().isTest).toBe(true);
  });

  it('should display an error message when the field is invalid and touched', async () => {
    const inputElement: HTMLInputElement = fixture.nativeElement.querySelector('input');
    inputElement.value = '';
    inputElement.dispatchEvent(new Event('input'));
    inputElement.dispatchEvent(new Event('blur'));
    await fixture.whenStable();
    fixture.detectChanges();
    const errorElement: HTMLParagraphElement = fixture.nativeElement.querySelector('p.error');
    expect(errorElement).toBeTruthy();
    expect(errorElement.textContent).toContain('Name is required');
  });
});
```

### 🧿Custom Button

En este caso el componente es independiente del formulario en el que se usará: recibe como inputs el texto que debe mostrar (label) y un booleano que indica si el botón está deshabilitado o no, y se encarga de renderizar el botón nativo de HTML con el texto y el estado correspondiente.

Este sería el template del componente

```html
<div class="form-control">
      <button [type]="type()" [disabled]="disabled() || sending()">
        {{ sending() ? alternativeLabel() : label() }}
      </button>
    </div>
```

Esta sería la lógica del componente

```ts
export class Button {
  readonly label = input.required<string>();
  readonly alternativeLabel = input<string>('Sending...');
  readonly disabled = input<boolean>(false);
  readonly sending = input<boolean>(false);
  readonly type = input<string>('submit');
}
```

Desde fuera podemos controlar los estados disabled() y sending() del botón, que si están en true lo deshabilitan, y podemos cambiar el texto del botón cuando se está enviando el formulario, para que el usuario sepa que la acción está en curso.

Vemos un ejemplo de como se usaría nuestro componente desde un formulario de login, donde el botón se deshabilita cuando el formulario es inválido, y cambia su texto cuando se está enviando el formulario.

```html
<alc-button
  [label]="'Login'"
  [disabled]="loginFieldTree().invalid()"
  [sending]="isSubmitting()"
  [alternativeLabel]="'Sending...'"
  [type]="'submit'"
/>
```

#### 👁️‍🗨️Test del componente Button

A diferencia de los anteriores componentes, la preparación del test es simple, 
ya que no hay un FieldTree que pasar como input, para que lo recoja la directiva FormField, que en este caso sno se aplica al ni implementarse ningún interface que lo indique.

```ts
describe('Button', () => {
  let component: Button;
  let fixture: ComponentFixture<Button>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      imports: [Button],
    }).compileComponents();

    fixture = TestBed.createComponent(Button);
    component = fixture.componentInstance;
    fixture.componentRef.setInput('label', 'Testing button');
    // fixture.componentRef.setInput('disabled', false);
    // fixture.componentRef.setInput('type', 'submit');
    await fixture.whenStable();
  });
  // ... (rest of the test cases)
});
```

En cuanto a los casos de prueba, podemos comprobar

- que se renderiza la label
- que se establece correctamente el tipo
- que por defecto el botón está habilitado
- que si se establece el input disabled a true, el botón se deshabilita
- que si se establece el input disabled a false, el botón se habilita
- que si se establece el input sending a true, el botón se deshabilita y cambia su texto al valor de alternativeLabel

```ts
 it('should render the label', () => {
    const buttonElement: HTMLButtonElement = fixture.nativeElement.querySelector('button');
    expect(buttonElement.textContent).toContain('Testing button');
  });
  
  it('should have the correct type', () => {
    const buttonElement: HTMLButtonElement = fixture.nativeElement.querySelector('button');
    expect(buttonElement.type).toBe('submit');
  });

  it('should be enabled by default', () => {
    const buttonElement: HTMLButtonElement = fixture.nativeElement.querySelector('button');
    expect(buttonElement.disabled).toBe(false);
  });

  it('should be disabled when the disabled input is true', async () => {
    fixture.componentRef.setInput('disabled', true);
    await fixture.whenStable();
    const buttonElement: HTMLButtonElement = fixture.nativeElement.querySelector('button');
    expect(buttonElement.disabled).toBe(true);
  }); 

  it('should be enabled when the disabled input is false', async () => {
    fixture.componentRef.setInput('disabled', false);
    await fixture.whenStable();
    const buttonElement: HTMLButtonElement = fixture.nativeElement.querySelector('button');
    expect(buttonElement.disabled).toBe(false);
  });
    
  it('should display alternative label when sending is true', async () => {
    fixture.componentRef.setInput('sending', true);
    await fixture.whenStable();
    const buttonElement: HTMLButtonElement = fixture.nativeElement.querySelector('button');
    expect(buttonElement.textContent).toContain('Sending...');
    expect(buttonElement.disabled).toBe(true);
  });
});
```

### 🧿LoginPage

Para utilizar estos componentes en los formularios, vamos a crear una nueva versión del formulario de login y modificar login-page para que lo utilice

En LoginPage

- ampliamos el type `LoginFormType` añadiendo 'custom'

```ts
type LoginFormType = 'simulate' | 'tdf' | 'mdf-rx' | 'signals' |  'custom' | 'after-login';
```

- modificamos @switch del template para que utilice el nuevo componente `LoginFormCustom` cuando el tipo de formulario sea 'custom'

```html
@case ('custom') {
  <alc-card>
    <alc-login-form-custom />
    <p>Ejemplo de Formulario Personalizado</p>
  </alc-card>
} 
```

- añadimos una nueva opción al menu que estamos proyectando en el sidebar

```ts
{
  label: 'Custom Form',
  path: '/auth/login/custom',
}
```

#### 👁️‍🗨️Update Test de LoginPage

Al haber añadido una nueva opción en la navegación del sidebar, debemos actualizar el test de LoginPage para que tenga en cuenta esta nueva opción, y que el coverage del test de navegación continúe siendo del 100%.

Utilizamos el `RouterTestingHarness` y reproducimos el test de los casos anteriores.

```ts
it('should have render the view for "custom"', async () => {
  await harness.navigateByUrl('/login/custom', LoginPage);
  await harness.fixture.whenStable();
  component = debugHarness.query(By.directive(LoginPage)).componentInstance;

  const debugForm = debugHarness.query(By.directive(LoginFormCustom));
  expect(debugForm).toBeTruthy();
  expect(debugForm.nativeElement).toBeInstanceOf(HTMLElement);
  expect(component['formType']()).toBe('custom');
});
```

### 🧿LoginFormCustom y su test

Creamos el nuevo componente de formulario

```shell
ng g c features/auth/components/login-form-custom --project course-02
```

Otra opción es que clonemos y renombremos el componente `LoginFormSignals` a `LoginFormCustom`, 

Modificamos su template para que utilice los componentes `Input`, `CheckBox` y `Button` en lugar de los elementos nativos.

```html
<form [formRoot]="loginFieldTree">
<alc-input
  type="email"
  [label]="'Email'"
  [formField]="loginFieldTree.email"
/>
<alc-input
  type="password"
  [label]="'Password'"
  [formField]="loginFieldTree.password"
/>
<alc-check-box
  [label]="'Remember me'"
  [formField]="loginFieldTree.rememberMe"
/>
<alc-button [label]="'Login'" [disabled]="loginFieldTree().invalid()" />
</form>
```

Una vez mas, como testamos el comportamiento, el test para la nueva versión del formulario es exactamente el mismo que para las anteriores versiones, y por tanto no se repite aquí.

## 📕Controles de selección

En HTML, los controles de selección son aquellos que permiten al usuario seleccionar una opción de un conjunto de opciones, como los radio buttons y los select.

Con independencia del tipo de formulario que estemos utilizando, se pueden crear estos controles programáticamente a partir de un conjunto de datos, lo que facilita que se puedan actualizar dinámicamente los valores a elegir.

Esta información forma parte de lo que sse conoce como **datos maestros** (**master data**), y se puede definir en un archivo de constantes, o bien en un servicio que nos devuelva los datos desde el backend.

```ts
interface masterData {
  key: string;
  message: string;
};
```

A partir de este punto se pueden adoptar dos enfoques: 

- el modelo de datos del formulario guarda la clave del objeto seleccionado, y el control de selección devuelve la clave del objeto seleccionado, que luego se puede utilizar para buscar el objeto completo en el array de datos maestros

- el modelo de datos del formulario guarda el objeto completo seleccionado, y el control de selección devuelve el objeto completo seleccionado. En el caso de los formularios basados en signals, esto dificulta la forma en que trabajan los controles, ya que los radio buttons y los select devuelven el valor de la opción seleccionada, y no el objeto completo. 

## Signals Forms y controles de selección

Para tener un ejemplo algo más completo, añadimos un formulario de registro basado en signals, que incluye otros elementos, como radio buttons y select, y que nos permite ver cómo se comportan estos elementos en un formulario basado en signals.

El conjunto de controles iniciales del formulario será

- email: input type email (string);
- password: input type password (string);
- gender: radio buttons vinculados a datos maestros de tipo Gender;
- isOk: checkbox (boolean) para aceptar los términos y condiciones;
- hasCourse: checkbox (boolean) para indicar si el usuario tiene un curso asociado;

Por último, si el checkbox hasCourse está marcado, se mostrará una serie de controles opcionales:

- firstName: input type text (string);
- surname: input type text (string);
- course: select con los cursos disponibles, vinculados a datos maestros de tipo Course.

### 🕸️Servicio Auth y tipos para el registro

Antes de abordar los formularios definimos

- el mock del servicio, similar al que tenemos en el caso del login

```ts services/auth.ts
register(
  data: RegisterRequest,
  options: Partial<AuthOptions> = {},
): Observable<RegisterResponse> {
  const finalOptions = { ...DEFAULT_AUTH_OPTIONS, ...options };

  const hasError =
    !finalOptions.success || !data.email || !data.password || data.email.includes('error');

  // console.log('Service data', data);

  const result = hasError
    ? {
        error: 'Registro incorrecto',
      }
    : {
        error: '',
        info: {
          id: this.#generateId(),
          email: data.email,
          firstName: data.firstName,
          surname: data.surname,
          gender: data.gender,
          hasCourse: data.hasCourse,
          course: data.course,
          isOk: data.isOk,
          registerDate: new Date(),
        },
      };

  return of(result).pipe(delay(options.delayTime as number));
}
```

- los tipos correspondientes a los datos maestros, como ya hemos mencionado

```ts types/auth.ts
interface masterData {
  key: string;
  message: string;
};

export interface Gender extends masterData {
  key: 'm' | 'f' | 'o' | 'n' | '';
  message: 'hombre' | 'mujer' | 'otro' | 'prefiero no decirlo' | '';
};

export interface Course extends masterData {
  key: 'a' | 'r' | 'n' | 'h' | 'j' | 't' | '';
  message: 'Angular' | 'React' | 'Node.js' | 'HTML/CSS' | 'JavaScript' | 'TypeScript' | '';
};
```

- los tipos necesarios para el registro, como los definidos para el login

```ts types/auth.ts
export interface RegisterRequest {
  email: string;
  password: string;
  firstName: string;
  surname: string;
  gender: Gender;
  hasCourse: boolean;
  course: Course;
  isOk: boolean;
}

export interface RegisterResponse {
  error: string; // Mensaje de error en caso de fallo, si no cadena vacía
  info?: {
    id: number;
    email: string;
    firstName: string;
    surname: string;
    gender: Gender;
    hasCourse: boolean;
    course: Course;
    isOk: boolean;
  };
}
```

#### 👁️‍🗨️Actualización del test del servicio Auth: register()

El test del servicio de registro es muy similar al del login, y comprueba que el servicio devuelve un observable que emite un objeto de tipo RegisterResponse, con la información del usuario registrado en caso de éxito, o un mensaje de error en caso de fallo.

```ts services/auth.spec.ts
describe('register', () => {
  it('should return a successful register response', () => {
    vi.useFakeTimers();
    const registerRequest: RegisterRequest = {
      email: 'user@example.com',
      password: 'password123',
      firstName: 'John',
      surname: 'Doe',
      gender: {key: 'm', message: 'hombre'  },
      hasCourse: true,
      course: {key: 'a', message: 'Angular'},
      isOk: true,
    };

    service.register(registerRequest).subscribe((registerResponse) => {
      expect(registerResponse.error).toBe('');
      expect(registerResponse.info?.id).toEqual(
        expect.any(Number)
      );
      expect(registerResponse.info?.email).toBe(registerRequest.email);
      expect(registerResponse.info?.firstName).toBe(registerRequest.firstName);
      expect(registerResponse.info?.surname).toBe(registerRequest.surname);
      expect(registerResponse.info?.gender).toEqual(registerRequest.gender);
      expect(registerResponse.info?.hasCourse).toBe(registerRequest.hasCourse);
      expect(registerResponse.info?.course).toEqual(registerRequest.course);
      expect(registerResponse.info?.isOk).toBe(registerRequest.isOk);
    });

    vi.advanceTimersByTime(1100);
  });

  it('should return a failed register response', () => {
    vi.useFakeTimers();
    const registerRequest: RegisterRequest = {
      email: '',
      password: '',
      firstName: '',
      surname: '',
      gender: {key: '', message: ''},
      hasCourse: false,
      course: {key: '', message: ''},
      isOk: false,
    };

    service.register(registerRequest, { success: false }).subscribe((registerResponse) => {
      expect(registerResponse.error).toBe('Registro incorrecto');
      expect(registerResponse.info).toBeUndefined();
    });

    vi.advanceTimersByTime(1100);
  });
});
```

### Páginas u navegación interna en la feature Auth

Al tiempo que lo hacíamos para el login hemos creado

- la página `register-page`, capaz de renderizar condicionalmente distintos formularios
- un sidebar que permite elegir entre los distintos formularios disponibles
- las rutas con parámetros en el fichero de routing de la feature auth
- los componentes correspondientes a los formularios, aunque de momento sin contener el formulario

Los componentes/formularios previstos hasta el momento, disponibles aunque sin implementar internamente son

- `register-form`, que será el formulario de registro basado en signals
- `register-form-custom`, que será el formulario de registro basado en componentes personalizados

En este punto añadimos, como en su momento en el login

- `register-form-simulate`, que simulara un registro acertado o erróneo gracias a dos simples botones
- una opción en `register-page` cuando el parámetro de la ruta sea 'simulate', que renderiza el formulario `register-form-simulate` 
- la navegación desde el sidebar
- una opción en `register-page` cuando el parámetro de la ruta sea `register-error`, que renderiza el feedback al usuario directamente en register page, sin necesitar un componente extra, aunque también podría crearse 

#### 🧿Componente RegisterFormSimulate

El componente reproduce el que teníamos para el login:

- dos botones, uno para simular un registro correcto y otro para simular un registro erróneo

```html register-simulates.ts
<p>Simulación de register</p>
<div>
  <button (click)="register()">Registro</button>
  <button (click)="register(false)">Registro con Error</button>
</div>
```

- el método del componente que responde a los botones, limitándose a navegar:
  - a la página de login, si el registro es correcto 
  - a la página de registro, con el parámetro ``, si el registro es erróneo 

```ts
export class RegisterSimulated {
  readonly #router = inject(Router);

  protected register(successes = true) {
    console.log('Register button clicked');
    if (successes) {
       this.#router.navigate(['auth', 'login', 'after-register'])
    } else {
      const response: RegisterResponse = {
        error: 'Problema en el registro',
      };
      this.#router.navigate(['auth', 'register', 'register-error'], {
        state: { ...response },
      });
    }
  }
}
```

Este planteamiento implica que una vez completada la feature de Auth, un registro correcto no hará login automáticamente, sino que llevará al usuario a la página de login para que realice este proceso de forma manual

Para completar el feedback en esta redirección al login hacemos un pequeño ajuste en LoginPage, como veremos

##### 👁️‍🗨️Test del componente RegisterFormSimulate

El test del componente, muy similar al equivalente pare el login, comprueba que se renderizan los dos botones, y que al pulsarlos se llama al método del router responsable de navegar a la página de login o a la página de registro con el parámetro `register-error`, según corresponda.

```ts
it('should navigate to login on click of successful register', () => {
  const elementButtons = fixture.nativeElement.querySelectorAll('button');
  elementButtons[0].click();
  fixture.detectChanges();
  expect(router.navigate).toHaveBeenCalledWith(['auth', 'login'])
});

it('should navigate to register-error on click of failed register', () => {
  const elementButtons = fixture.nativeElement.querySelectorAll('button');
  elementButtons[1].click();
  fixture.detectChanges();
  expect(router.navigate).toHaveBeenCalledWith(['auth', 'register', 'register-error'], expect.objectContaining({
    state: expect.objectContaining({
      error: 'Problema en el registro',
    })
  }))
});
```

#### 🌐Actualizaciones de RegisterPage

Completamos el tipo con las nuevas opciones disponibles: `simulated` y `register-error`

```ts
type RegisterFormType = 'simulated' | 'monolithic' | 'custom-components' | 'register-error';
```

Completamos la renderización condicional en función de los parámetros de la ruta, para sus nuevos posibles valores: `simulated` y `register-error`.

Como el el login, también podríamos haber creado un componente `register-error` para renderizar el error, pero en este caso hemos optado por renderizarlo directamente en `register-page`, ya que es un caso muy simple.

Igualmente podríamos haber usado un @switch, pero en este caso hemos optado por un @if, para tener ejemplos de ambas estructuras de control.

```html    
<alc-card>
  @if(formType() === 'simulated') {
    <alc-register-simulated />
  } @else if (!formType() || formType() === 'monolithic') {
    <p>Ejemplo de registro en un solo componente</p>
    <alc-register-form />
  } @else if (formType() === 'custom-components') {
    <p>Ejemplo de registro con componentes personalizados</p>
    <alc-register-form-custom />
  } @else if (formType() === 'register-error') {
    <p>Error en el registro.</p>
    <p>{{ navState().error }}</p>
  }
</alc-card>
```

Para el menu que se proyecta en el sidebar, añadimos la opción de registro simulado, que nos permite probar el flujo completo de registro y login, sin necesidad de implementar los formularios de registro.

```ts
{
  label: 'Registro simulado',
  path: '/auth/register/simulated',
},
```

Finalmente, para el caso de `register-error`, añadimos un signal que representa el estado de la navegación, y que se actualiza en el constructor del componente, a partir del estado de la navegación actual, si lo hay.

```ts
constructor() {
  this.#router.events.pipe(takeUntilDestroyed(this.#destroyRef)).subscribe((event) => {
    if (event instanceof NavigationEnd) {
      const currentNav = this.#router.currentNavigation();
      if (currentNav && currentNav.extras.state) {
        this.navState.set(currentNav.extras.state as RegisterResponse);
      }
    }
  });
}
```

##### 👁️‍🗨️Actualización de los tests de RegisterPage

Respecto al caso de `register-simulated`, el test es muy similar al equivalente en el login, y comprueba que se renderiza el componente `register-simulated` y que el tipo indicado en los parámetros de la ruta es correcto.

```ts
it('should have render the view for the component register-simulated', async () => {
  await harness.navigateByUrl('/register/simulated', RegisterPage);
  await harness.fixture.whenStable();
  component = debugHarness.query(By.directive(RegisterPage)).componentInstance;

  const debugForm = debugHarness.query(By.css('alc-register-simulated'));
  expect(debugForm).toBeTruthy();
  expect(component['formType']()).toBe('simulated');
  expect(debugForm.nativeElement).toBeInstanceOf(HTMLElement);
});
```
En el caso de la opción `register-error`, además de comprobar que se renderiza la vista, necesitamos comprobar que se actualiza la información del estado de la navegación.

Como ya vimos en el login, para ello no podemos usar el método `navigateByUrl()` del `RouterTestingHarness`, ya que no nos permite pasar un estado de navegación, por lo que necesitamos acceder directamente al servicio `Router` inyectado en el harness utilizar su método `navigate()` para navegar a la ruta correspondiente, pasando el estado de navegación como parámetro.

```ts
it('should have render the view for the register-error', async () => {
  await harnessRouter.navigateByUrl('/register/register-error', {
    state: { error: 'Problema en el registro' },
  });
  await harness.fixture.whenStable();
  component = debugHarness.query(By.directive(RegisterPage)).componentInstance;

  const state = component['navState']();
  expect(state).toEqual({ error: 'Problema en el registro' });
  expect(component['formType']()).toBe('register-error');

  const debugP = debugHarness.query(By.css('alc-card p:nth-child(1)'));
  const elementP = debugP.nativeElement as HTMLElement;
  expect(elementP.textContent).toContain('Error en el registro.');
});
```

#### 🌐Actualizaciones de LoginPage

Actualizamos LoginPage para que cuando el parámetro de la ruta sea `after-register`, se muestre un mensaje de éxito al usuario.

- añadimos el tipo necesario

```ts
type LoginFormType = 'simulate' | 'tdf' | 'mdf-rx' | 'signals' |  'custom' | 'after-login' | 'after-register';
```

- añadimos la opción en el @switch del template, asimilándola a la carga de cualquiera de los formularios de login, tal como permite hacer `@switch`

```html
@case('after-register')
@case ('tdf') {
  <alc-login-form-tdf />
  <p>Ejemplo de Template Driven Forms (TDF)</p>
}
```

- renderizamos condicionalmente el mensaje de feedback al usuario, si el tipo de formulario es `after-register`

```html
@if (formType() === 'after-register') {
  <p>Registro exitoso. Por favor, inicia sesión.</p>
} @else {
  <p>Si no tienes cuenta, <a [routerLink]="['/auth', 'register']">regístrate aquí</a>.</p>
}
```

##### 👁️‍🗨️Actualización del test de LoginPage

Añadimos el nuevo caso de prueba para comprobar que cuando el parámetro de la ruta es `after-register`, se renderiza la vista correspondiente, que el parámetro del tipo es correcto, y que aparece en el footer el mensaje adecuado.

```ts
it('should have render the view for "after-register" after success register', async () => {
  await harness.navigateByUrl('/login/after-register', LoginPage);
  await harness.fixture.whenStable();
  component = debugHarness.query(By.directive(LoginPage)).componentInstance;

  const debugForm = debugHarness.query(By.directive(LoginFormTdf));
  expect(debugForm).toBeTruthy();
  expect(debugForm.nativeElement).toBeInstanceOf(HTMLElement);
  expect(component['formType']()).toBe('after-register');
  const footerP = debugHarness.query(By.css('footer p'));
  expect(footerP.nativeElement.textContent).toContain('Registro exitoso');
});
```

### 🧿RegisterForm basado en signals (sin controles personalizados)

#### Controles de selección del registro

Se puede definir un array de opciones, incluyendo los valores y las etiquetas que se mostrarán en el template, donde se renderizarán los controles iterando los datos con un `@for`.

```ts
export const GENDERS: Gender[] = [
  { key: 'm', message: 'hombre' },
  { key: 'f', message: 'mujer' },
  { key: 'o', message: 'otro' },
  { key: 'n', message: 'prefiero no decirlo' },
];

export const COURSES: Course[] = [
  { key: 'a', message: 'Angular' },
  { key: 'r', message: 'React' },
  { key: 'n', message: 'Node.js' },
  { key: 'h', message: 'HTML/CSS' },
  { key: 'j', message: 'JavaScript' },
  { key: 't', message: 'TypeScript' },
];
```

En el componente importamos las contantes como propiedades de la clase, para poder utilizarlas en el template.

```ts
export class RegisterForm {
  protected readonly genders = signal(GENDERS);
  protected readonly courses = signal(COURSES);
}
```

En el caso del radio button, el resultado sería

```html
<fieldset aria-label="Gender">
  <legend>Gender</legend>
  @for (item of genders(); track item.key) {
    <label [for]="'gender-' + item.key " class="form-control checkbox">
      <input
        type="radio"
        [id]="'gender-' + item.key"
      />
      {{ item.message | titlecase }}
    </label>
  }
</fieldset>
```

En el caso del select, el resultado sería

```html
<label class="form-control" for="course">
  <span>Course</span>
  <select id="course">
    @for (item of courses(); track item.key) {
      <option [value]="item.key">{{ item.message }}</option>
    }
  </select>
</label>
```

####  Lógica en el componente

En primer lugar definimos el interface que representa el formulario, y un array de opciones para los radio buttons y el select.

```ts
interface RegisterForm {
  email: string;
  password: string;
  firstName: string;
  surname: string;
  gender: Gender;
  genderKey: Gender['key'];
  hasCourse: boolean;
  course: Course;
  isOk: boolean;
}
```

Para los valores objetos que representan  datos maestros (o master data)  incluimos la posibilidad de guardar el objeto completo, y también solo la clave del objeto, para poder enlazarlo con los radio buttons y el select, que solo devuelven el valor de la opción seleccionada.

Los pasos siguientes son ya conocidos, y son los mismos que en el caso del formulario de login basado en signals:

1. Definimos un objeto con los valores iniciales del formulario, con todos los campos a cadena vacía, o false en el caso del checkbox
2. Creamos un signal que representa el estado del formulario, con el valor inicial definido en el paso anterior
3. Creamos un objeto de tipo `FieldTree`, que representa una **vista reactiva** del formulario, a partir del signal que representa el estado del formulario.
4. Al crear el objeto `FieldTree`, podemos añadir  el **schema del formulario** con sus validaciones, como callback anónimo, o mejor a través de una propiedad de la clase. Cualquiera de ambos recibe como parámetro el path del formulario, que nos permite acceder a los controles del formulario y definir sus validaciones.
 
```ts
export class RegisterForm {
  readonly #initialValues: RegisterModel = {
    email: '',
    password: '',
    firstName: '',
    surname: '',
    gender: { key: '', message: '' },
    isOk: false,
    hasCourse: false,
    course: { key: '', message: '' },
  };

  protected readonly genders = signal(GENDERS);
  protected readonly courses = signal(COURSES);

  protected readonly user = signal<RegisterModel>(this.#initialValues);
  protected readonly registerForm: FieldTree<RegisterModel> = form(this.user, (schema) => {
    required(schema.email);
    email(schema.email);
    ...
  });

  submitForm() {
    console.log('Form submitted:', this.registerForm().value());
  }
}
```

#### Vinculación al template

El formulario se enlaza con el template utilizando las directivas `FormRoot` y `FormField`, importadas de `@angular/forms/signals`.

En el caso de los inputs text (email, password) y checkboxes, lo hacemos de la misma forma que en el formulario de login basado en signals.

```html
<form [formRoot]="registerForm" (submit)="submitForm()">
  <div aria-label="Email">
    <label for="email" class="form-control"
      >Email Email
      <input type="email" id="email" [formField]="registerForm.email" />
    </label>
  </div>
  <div aria-label="Password">
    <label for="password" class="form-control">
      Password
      <input type="password" id="password" [formField]="registerForm.password" />
    </label>
  </div>
  <div aria-label="Fist Name">
    <label for="firstName" class="form-control">
      First Name
      <input type="text" id="firstName" [formField]="registerForm.firstName" />
    </label>
  </div>
  <div aria-label="Surname">
    <label for="surname" class="form-control">
      Surname
      <input type="text" id="surname" [formField]="registerForm.surname" />
    </label>
  </div>
  <div aria-label="Buttons" class="form-control">
    <button type="submit" [disabled]="registerForm().invalid()">Register</button>
  </div>

  <div aria-label="Terms">
    <label for="isOk" class="form-control checkbox">
      <input type="checkbox" id="isOk" [formField]="registerForm.isOk" />
      Accept Terms ...
    </label>
  </div>

  <div aria-label="Has Course">
    <label for="hasCourse" class="form-control checkbox">
      <input type="checkbox" id="hasCourse" [formField]="registerForm.hasCourse" />
      ¿Quieres hacer un curso con nosotros?
    </label>
  </div>
</form>
<pre>{{ registerForm().value() | json }}</pre>
```

#### Valor del formulario

De nuevo, como herramienta en desarrollo, que luego eliminaremos, añadimos un \<pre> que nos permite ver el valor del formulario en tiempo real, utilizando el pipe `json` para mostrarlo en formato JSON.

```html
  <pre>{{ registerForm().value() | json }}</pre>
```

En este caso accedemos al estado del formulario (`FieldState`), es decir el valor de la signal `registerForm` mediante la llamada a su getter `registerForm()`

En el `FieldState` obtenemos al valor de la propiedad `value` mediante la llamada a su getter `value()`.

#### Vinculación al template: problemas con objetos

En el caso de los radio buttons y los select, solo podemos obtener el valor de la opción seleccionada, y no el objeto completo, enlazando el control con la propiedad del formulario que contiene la clave del objeto (key), y no con la propiedad que contiene el objeto completo.

Para terminar utilizamos el evento `(change)` del control radio button, para actualizar el objeto completo, accediendo al value de FieldState del control radio button (`registerForm.gender()`), y haciendo set a la signal que almacena el valor con el objeto completo.


```html
<fieldset aria-label="Gender">
  <legend>Gender</legend>
  @for (item of genders(); track $index) {
    <label [for]="'gender-' + item.key" class="form-control checkbox">
      <input
        type="radio"
        [id]="'gender-' + item.key"
        [formField]="registerForm.gender.key"
        [value]="item.key"
        (change)="registerForm.gender().value.set(item)"
      />
      {{ item.message | titlecase }}
    </label>
  }
</fieldset>
```

En el select la respuesta al evento change es similar, pero en este caso accedemos al target del evento y debemos haces casting del tipo, por lo que pasamos el código a un método del componente.

```html
<label class="form-control" for="course">
  <span>Course</span>
  <select
    id="course"
    [formField]="registerForm.course.key"
    (change)="selectChange($event)"
  >
    @for (item of courses(); track $index) {
      <option [value]="item.key">{{ item.message }}</option>
    }
  </select>
```

En el handler del evento change, aprovechamos la propiedad `selectedIndex` del target del evento, nativa de HTML, que nos da el índice de la opción seleccionada, para obtener el objeto completo del array de opciones, que asignamos a la signal correspondiente.

```ts
protected selectChange(event: Event) {
  console.log('Select', this.registerForm.course())
  const index = (event.target as HTMLSelectElement).selectedIndex;
  const selectedOption = index >= 0 ? this.courses()[index] : { key: '', message: '' } as Course;
  this.registerForm.course().value.set(selectedOption);
}
```

#### Validación y feedback

Las validaciones se definen en el componente, y como buena práctica lo haremos en una propiedad de la clase, que es un callback que se pasa como segundo parámetro al método `form()` como **schema** de validación. Esta función recibe el path del formulario, que nos permite acceder a los controles del formulario y definir sus validaciones.

```ts
readonly #registerFormSchema = (path: any) => {
  required(path.email, { message: 'El email es obligatorio' });
  email(path.email, { message: 'El email no es válido' });
  required(path.password, { message: 'La contraseña es obligatoria' });
  minLength(path.password, 6, { message: 'La contraseña debe tener al menos 6 caracteres' });
  required(path.gender.key, { message: 'Indicar el género es obligatorio' });
  //....
};

protected readonly registerForm = form(this.user, this.#registerFormSchema);
```

El feedback al usuario se realiza en el template, comprobando si el control es inválido y ha sido tocado, y mostrando el primer mensaje de error del array de errores del control.

Sin embargo los casos en que el control es un objeto, como en el caso de gender, son un poco particulares, ya que

- la validación esta definida sobre la propiedad que contiene la clave del objeto, y no sobre la propiedad que contiene el objeto completo
- la propiedad `errors` del control que contiene el objeto completo estará vacía, ya que no tiene validaciones, 
- disponemos de la propiedad errorSummary, que almacena los errores de un nodo y de todo sus descendientes, donde SI encontraremos los errores de validación del control que contiene la clave del objeto.

```ts
@if (registerForm.gender()?.invalid() && registerForm.gender()?.touched()) {
  <p class="error">
    {{ registerForm.gender()?.errorSummary()?.[0]?.message }}
  </p>
}
```
#### Validaciones dependientes de otros campos

A nivel visual, los campos de nombre, apellidos y selección del curso solo aparecen cuando se ha seleccionado la opción de hacer un curso, por lo que las validaciones de estos campos solo se deben aplicar cuando el checkbox `hasCourse` está marcado.

```ts
required(schema.firstName, {
  when: ({ valueOf }) => valueOf(schema.hasCourse) === true,
  message: 'El nombre es obligatorio',
});
```

Podemos crear un segundo schema de validación, con aquellas que solo se aplican cuando el checkbox `hasCourse` está marcado, y que se añada al schema principal mediante la función `applyWhen()`, que nos permite añadir un schema adicional condicional.

```ts
readonly #optionalsSchema = (path: SchemaPathTree<RegisterModel>) => {
  required(path.firstName, { message: 'El nombre es obligatorio' });
  required(path.surname, { message: 'El apellido es obligatorio' });
  required(path.course.key, { message: 'El curso es obligatorio' });
};

protected readonly registerForm: FieldTree<RegisterModel> = form(this.user, (path) => {
  this.#registerFormSchema(path);
  applyWhen(
    path,
    ({ valueOf }) => valueOf(path.hasCourse) === true,
    (path) => this.#optionalsSchema(path)
  );
});
```

#### Submit y Navegación

Como en los ejemplos anteriores, de formularios con signals, aprovechamos la directiva FormRoot y la forma de gestionar el submit como unas lógica interna del propio formulario. 

```ts
protected readonly registerForm: FieldTree<RegisterModel> = form(
    //..
  {
  submission: { action: this.submitForm.bind(this) },
  },
}
```

El proceso de submit será muy similar al del formulario de login, incluyendo: 

- la llamada a un servicio, responsable del registro de cara al backend, pasándole los datos de la signal value(), esperando qa qu se resuelva la promesa.
- un reset del formulario (ahora con el método `reset(INITIAL_VALUES)` del FormGroup) para dejarlo limpio para un nuevo intento de login.
- la navegación dependiendo del resultado,  utilizando el servicio `Router` de Angular.

```ts
  protected async submitForm(registerFieldTree: FieldTree<RegisterModel>) {
    const formData = registerFieldTree().value();
    const state = await this.#auth.registerPromise(formData, { delayTime: 2000 });

    console.log('Form submitted:', state);
    registerFieldTree().reset(this.#initialValues);

    if (state.error) {
      await this.#router.navigate(['/auth', 'register', 'register-error'], {
        state,
      });
    } else {
      await this.#router.navigate(['/auth', 'login', 'after-register']);
    }
  }
```

En la creación de formData, para adecuarlo al interface LoginForm, podríamos añadir las transformaciones y validaciones que fueran necesarias.

#### Resultado final

```ts
import { JsonPipe, TitleCasePipe } from '@angular/common';
import { Component, inject, signal } from '@angular/core';
import {
  applyWhen,
  email,
  FieldTree,
  form,
  FormField,
  FormRoot,
  minLength,
  required,
  SchemaPathTree,
} from '@angular/forms/signals';
import { RegisterRequest } from '../../types/auth';
import { GENDERS, COURSES } from '../../types/register-controls';
import { Router } from '@angular/router';
import { Auth } from '../../services/auth';

// Versión inicial del formulario de registro,
// sin los campos de selección de género y curso, y sin validación de los campos.
// sin usar componentes de UI personalizados

type RegisterModel = RegisterRequest;

@Component({
  selector: 'alc-register-form',
  imports: [FormField, FormRoot, JsonPipe, TitleCasePipe],
  template: `
    <form [formRoot]="registerForm">
      <div aria-label="Email">
        <label for="email" class="form-control"
          >Email Email
          <input type="email" id="email" [formField]="registerForm.email" />
        </label>
        @if (registerForm.email()?.invalid() && registerForm.email()?.touched()) {
          <p class="error">
            {{ registerForm.email()?.errors()?.[0]?.message }}
          </p>
        }
      </div>
      <div aria-label="Password">
        <label for="password" class="form-control">
          Password
          <input type="password" id="password" [formField]="registerForm.password" />
        </label>
        @if (registerForm.password()?.invalid() && registerForm.password()?.touched()) {
          <p class="error">
            {{ registerForm.password()?.errors()?.[0]?.message }}
          </p>
        }
      </div>

      <fieldset aria-label="Gender">
        <legend>Gender</legend>
        @for (item of genders(); track item.key) {
          <label [for]="'gender-' + item.key" class="form-control checkbox">
            <input
              type="radio"
              [id]="'gender-' + item.key"
              [formField]="registerForm.gender.key"
              [value]="item.key"
              (change)="registerForm.gender().key.set(item)"
            />
            {{ item.message | titlecase }}
          </label>
        }
      </fieldset>

      <div aria-label="Terms">
        <label for="isOk" class="form-control checkbox">
          <input type="checkbox" id="isOk" [formField]="registerForm.isOk" />
          Accept Terms ...
        </label>
        @if (registerForm.isOk()?.invalid() && registerForm.isOk()?.touched()) {
          <p class="error">
            {{ registerForm.isOk()?.errors()?.[0]?.message }}
          </p>
        }
      </div>

      <div aria-label="Has Course">
        <label for="hasCourse" class="form-control checkbox">
          <input type="checkbox" id="hasCourse" [formField]="registerForm.hasCourse" />
          ¿Quieres hacer un curso con nosotros?
        </label>
        <!-- @if (registerForm.hasCourse()?.invalid() && registerForm.hasCourse()?.touched()) {
          <p class="error">
            {{ registerForm.hasCourse()?.errors()?.[0]?.message }}
          </p>
        } -->
      </div>

      @if (registerForm.hasCourse().value()) {
        <div aria-label="First Name">
          <label for="firstName" class="form-control">
            First Name
            <input type="text" id="firstName" [formField]="registerForm.firstName" />
          </label>
          @if (registerForm.firstName()?.invalid() && registerForm.firstName()?.touched()) {
            <p class="error">
              {{ registerForm.firstName()?.errors()?.[0]?.message }}
            </p>
          }
        </div>

        <div aria-label="Surname">
          <label for="surname" class="form-control">
            Surname
            <input type="text" id="surname" [formField]="registerForm.surname" />
          </label>
          @if (registerForm.surname()?.invalid() && registerForm.surname()?.touched()) {
            <p class="error">
              {{ registerForm.surname()?.errors()?.[0]?.message }}
            </p>
          }
        </div>

        <div aria-label="Select Course">
          <label class="form-control" for="course">
            <span>Course</span>
            <select
              id="course"
              [formField]="registerForm.course.key"
              (change)="selectChange($event)"
            >
              @for (item of courses(); track item.key) {
                <option [value]="item.key">{{ item.message }}</option>
              }
            </select>
          </label>
          @if (registerForm.course()?.invalid() && registerForm.course()?.touched()) {
            <p class="error">
              {{ registerForm.course()?.errorSummary()?.[0]?.message }}
            </p>
          }
        </div>
      }

      <div aria-label="Buttons" class="form-control">
        <button type="submit" [disabled]="registerForm().invalid() || registerForm().submitting()">
          {{ registerForm().submitting() ? 'Submitting...' : 'Register' }}
        </button>
      </div>
    </form>
    <div aria-label="Form Value" class="form-value">
      <p>Submitting state: {{ registerForm().submitting() }}</p>
      <p>Valor del formulario</p>
      <pre>{{ registerForm().value() | json }}</pre>
      <p>Valor de errores activos del formulario</p>
      <pre>{{ registerForm().errorSummary() | json }}</pre>
      <p>Errores activos del formulario</p>
      <ul class="error">
        @for (error of registerForm().errorSummary(); track $index) {
          <li>{{ error.message }}</li>
        }
      </ul>
    </div>
  `,
  styleUrls: ['../forms.css'],
  styles: `
    form {
      max-width: 500px;
    }
    fieldset {
      border: 1px solid #ccc;
      padding: 1rem;
      display: flex;
      gap: 1rem;
    }
    ul.error {
      list-style: none;
      padding: 0;
      margin: 0;
    }
    .form-value {
      margin-top: 2rem;
      padding: 1rem;
      border: 1px solid #ccc;
      background-color: #f9f9f9;
    }
  `,
})
export class RegisterForm {
  readonly #router = inject(Router);
  readonly #auth = inject(Auth);

  readonly #initialValues: RegisterRequest = {
    email: '',
    password: '',
    firstName: '',
    surname: '',
    gender: { key: '', message: '' },
    isOk: false,
    hasCourse: false,
    course: { key: '', message: '' },
  };

  protected readonly genders = signal(GENDERS);
  protected readonly courses = signal(COURSES);

  protected readonly user = signal<RegisterModel>(this.#initialValues);

  readonly #optionalsSchema = (path: SchemaPathTree<RegisterModel>) => {
    required(path.firstName, { message: 'El nombre es obligatorio' });
    required(path.surname, { message: 'El apellido es obligatorio' });
    required(path.course.key, { message: 'El curso es obligatorio' });
  };

  readonly #registerFormSchema = (path: SchemaPathTree<RegisterModel>) => {
    required(path.email, { message: 'El email es obligatorio' });
    email(path.email, { message: 'El email no es válido' });
    required(path.password, { message: 'La contraseña es obligatoria' });
    minLength(path.password, 6, { message: 'La contraseña debe tener al menos 6 caracteres' });
    required(path.isOk, { message: 'Debes aceptar los términos y condiciones' });
  };



  protected readonly registerForm: FieldTree<RegisterModel> = form(
    this.user,
    (path) => {
      this.#registerFormSchema(path);
      applyWhen(
        path,
        ({ valueOf }) => valueOf(path.hasCourse) === true,
        (path) => this.#optionalsSchema(path),
      );
    },
    {
      submission: { action: this.submitForm.bind(this) },
    },
  );

  protected selectChange(event: Event) {
    console.log('Select', this.registerForm.course())
    const index = (event.target as HTMLSelectElement).selectedIndex;
    const selectedOption = index >= 0 ? this.courses()[index] : { key: '', message: '' } as Course;
    this.registerForm.course().value.set(selectedOption);
  }

  protected async submitForm(registerFieldTree: FieldTree<RegisterModel>) {
    const formData = registerFieldTree().value();
    const state = await this.#auth.registerPromise(formData, { delayTime: 2000 });

    console.log('Form submitted:', state);
    registerFieldTree().reset(this.#initialValues);

    if (state.error) {
      await this.#router.navigate(['/auth', 'register', 'register-error'], {
        state,
      });
    } else {
      await this.#router.navigate(['/auth', 'login', 'after-register']);
    }
  }
}
```

#### 👁️‍🗨️Test del componente registerForm

Como son muchos los controles, podemos crear un interface que los incorpore a todos y una serie de funciones helper que permitan acceder a ellos de forma más sencilla, distinguiendo los que siempre aparecen en el formulario de los que lo hacen opcionalmente

```ts

interface FormControls {
  email: HTMLInputElement;
  password: HTMLInputElement;
  gender: HTMLInputElement[];
  isOk: HTMLInputElement;
  hasCourse: HTMLInputElement;
  firstName?: HTMLInputElement;
  surname?: HTMLInputElement;
  course?: HTMLSelectElement;
}

describe('RegisterForm', () => {
 //...

  const getControls = () => {
    return {
      email: fixture.nativeElement.querySelector('#email'),
      password: fixture.nativeElement.querySelector('#password'),
      gender: fixture.nativeElement.querySelectorAll('[aria-label="Gender"] input'),
      hasCourse: fixture.nativeElement.querySelector('#hasCourse'),
      isOk: fixture.nativeElement.querySelector('#isOk'),
    };
  };
  
  const getOptionalControls = () => {
    return {
      firstName: fixture.nativeElement.querySelector('#firstName'),
      surname: fixture.nativeElement.querySelector('#surname'),
      course: fixture.nativeElement.querySelector('#course'),
    };
  };
};
```

Los mecanismos para acceder a los controles pueden ser diversos, y en este caso utilizamos `querySelector` y `querySelectorAll`, pero también podríamos utilizar `By.css()` del `DebugElement` de Angular. En cualquier caso los selectores empleados dependerán de la estructura del template, y de los atributos que hayamos definido en los controles, como `id`, `name`, `class`, `aria-label`, etc

Además incluimos un mck de datos y nuevas helpers capaces de rellenar todos los controles no opcionales o también los opcionales.

```ts
const MOCK_REGISTER: RegisterModel = {
  email: 'test@example.com',
  password: 'password123',
  firstName: 'Pepe',
  surname: 'Pérez',
  gender: { key: 'o', message: 'otro' },
  isOk: true,
  hasCourse: true,
  course: { key: 'a', message: 'Angular' },
};

const completeFixedControls = () => {
  controls = getControls();
  controls.email.value = MOCK_REGISTER.email;
  controls.email.dispatchEvent(new Event('input'));
  controls.email.dispatchEvent(new Event('blur'));
  controls.password.value = MOCK_REGISTER.password;
  controls.password.dispatchEvent(new Event('input'));
  controls.password.dispatchEvent(new Event('blur'));
  controls.gender[2].checked = true; // Selecting 'otro'
  controls.gender[2].dispatchEvent(new Event('change'));
  controls.gender[2].dispatchEvent(new Event('blur'));
  controls.isOk.checked = MOCK_REGISTER.isOk;
  controls.isOk.dispatchEvent(new Event('input'));
  controls.isOk.dispatchEvent(new Event('blur'));
  controls.hasCourse.checked = MOCK_REGISTER.hasCourse;
  controls.hasCourse.dispatchEvent(new Event('input'));
};

const completeOptionalControls = () => {
  controls = {
    ...controls,
    ...getOptionalControls(),
  };
  controls.firstName!.value = MOCK_REGISTER.firstName;
  controls.firstName!.dispatchEvent(new Event('input'));
  controls.firstName!.dispatchEvent(new Event('blur'));
  controls.surname!.value = MOCK_REGISTER.surname;
  controls.surname!.dispatchEvent(new Event('input'));
  controls.surname!.dispatchEvent(new Event('blur'));
  controls.course!.value = MOCK_REGISTER.course.key;
  controls.course!.dispatchEvent(new Event('change'));
};
```

A nivel de preparación de los tests, debemos tener en cuenta que el componente usa un servicio y que probablemente nos intereses convertir sus métodos en mock, para poder controlar su comportamiento y simular diferentes escenarios. Lo podemos conseguir redefiniendo el provider o utilizando `vi.spyOn()` de Vitest y añadiendo al espía una determinada implementación. En cualquiera de loos casos el `TestBed.inject()` nos proporcionara acceso a la misma instancia disponible dentro del componente.

```ts
beforeEach(async () => {
  await TestBed.configureTestingModule({
    imports: [RegisterForm],
    providers: [
      {
        provide: Auth,
        useValue: {
          registerPromise: vi.fn().mockReturnValue({}),
        },
      },
    ],
  }).compileComponents();

  fixture = TestBed.createComponent(RegisterForm);
  component = fixture.componentInstance;
  router = TestBed.inject(Router);
  auth = TestBed.inject(Auth);
  await fixture.whenStable();
});
```

Como suele ocurrir en los formularios, el número de casos de prueba es elevado:

- Renderizado del formulario
  - Controles no opcionales
  - Controles opcionales
- Valores del formulario
  - Valores iniciales
  - Valores después de rellenar los controles
- Envío del formulario
  - Register con éxito
  - Botón de submit deshabilitado cuando el formulario esa enviándose
  - Respuesta cuando el servidor devuelve un error en el registro
- Validación del formulario
  - Invalido si el registro esta vacío
  - Botón de submit deshabilitado si el registro es invalido (e.g. vacío)
  - Válido si todos los campos están completos
- Feedback de errores en controles no opcionales
  - Errores de validación visibles cuando el email es invalido y touched
  - Errores de validación visibles cuando la contraseña es invalida y touched
  - Errores de validación visibles cuando el checkbox de términos no está marcado y está touched
- Feedback de errores en controles opcionales
  - Errores de validación visibles cuando el nombre es vacío y touched
  - Errores de validación visibles cuando el apellido es vacío y touched
  - Errores de validación visibles cuando el curso no está seleccionado y touched

Renderizado del formulario

```ts
describe('Form Rendered', () => {
  it('should have a form with the non optional controls', () => {
    controls = getControls();
    expect(controls.email).toBeTruthy();
    expect(controls.password).toBeTruthy();
    expect(controls.gender.length).toBe(4);
    controls.gender.forEach((genderControl) => {
      expect(genderControl).toBeTruthy();
    });
    expect(controls.isOk).toBeTruthy();
    expect(controls.hasCourse).toBeTruthy();
  });

  it('should have the optional controls when needed, not when not needed', () => {
    controls = getControls();
    controls.hasCourse.click();
    fixture.detectChanges();
    controls = {
      ...controls,
      ...getOptionalControls(),
    };
    expect(controls.firstName).toBeTruthy();
    expect(controls.surname).toBeTruthy();
    expect(controls.course).toBeTruthy();
    controls.hasCourse.click();
    fixture.detectChanges();
    controls = {
      ...controls,
      ...getOptionalControls(),
    };
    expect(controls.firstName).toBeFalsy();
    expect(controls.surname).toBeFalsy();
    expect(controls.course).toBeFalsy();
  });
});
```

Valores del formulario

```ts
describe('Form Value', () => {
  it('should have the correct initial values', () => {
    expect(component['registerForm']().value()).toEqual({
      email: '',
      password: '',
      firstName: '',
      surname: '',
      gender: { key: '', message: '' },
      isOk: false,
      hasCourse: false,
      course: { key: '', message: '' },
    });
  });

  it('should update the form value when controls are changed', async () => {
    completeFixedControls();
    fixture.detectChanges();
    await fixture.whenStable();
    completeOptionalControls();
    fixture.detectChanges();
    await fixture.whenStable();
    expect(component['registerForm']().value()).toEqual(MOCK_REGISTER);
  });
});
```

Envío del formulario

```ts
describe('Form Submission with the full values', () => {
  beforeEach(async () => {
    vi.spyOn(router, 'navigate').mockResolvedValue(true);

    completeFixedControls();
    fixture.detectChanges();
    await fixture.whenStable();
    completeOptionalControls();
    fixture.detectChanges();
    await fixture.whenStable();
  });

  it('should be registered successfully by the service ', async () => {
    vi.mocked(auth.registerPromise).mockResolvedValue({
      error: '',
      info: {},
    } as RegisterResponse);
    const registerForm = component['registerForm'];
    const button = fixture.nativeElement.querySelector('button[type="submit"]');
    button.click();
    fixture.detectChanges();
    await fixture.whenStable();

    expect(auth.registerPromise).toHaveBeenCalledWith(MOCK_REGISTER, {
      delayTime: 2000,
    });

    expect(registerForm().submitting()).toBe(true);

    // El servicio register es asíncrono aunque se usa un mock,
    // por lo que debemos esperar a que se complete antes de hacer la aserción.

    await vi.waitFor(() => {
      expect(registerForm().submitting()).toBe(false);
      expect(router.navigate).toHaveBeenCalledWith(['/auth', 'login', 'after-register']);
    });

    // Después del submit y el reset del formulario
    expect(component['registerForm']().value()).toEqual({
      email: '',
      password: '',
      firstName: '',
      surname: '',
      gender: { key: '', message: '' },
      isOk: false,
      hasCourse: false,
      course: { key: '', message: '' },
    });
  });

  it('should show and disable the submit button while registration is in progress', async () => {
    vi.mocked(auth.registerPromise).mockResolvedValue({
      info: {},
    } as RegisterResponse);
    vi.mocked(router.navigate).mockResolvedValue(true);

    const button: HTMLButtonElement =
      fixture.nativeElement.querySelector('button[type="submit"]');
    button.click();
    fixture.detectChanges();

    expect(component['registerForm']().submitting()).toBe(true);
    expect(button.disabled).toBe(true);
    expect(button.textContent?.trim()).toBe('Submitting...');

    await vi.waitFor(() => {
      expect(component['registerForm']().submitting()).toBe(false);
      // Se recupera elñ texto del botón
      expect(button.textContent?.trim()).toBe('Register');
      // Se resetea el formulario, por lo que el botón quede disabled
      expect(button.disabled).toBe(true);
    });
  });

  it('should NOT register when the form is SERVER SIDE invalid', async () => {
    // Make the form invalid at server side
    vi.mocked(auth.registerPromise).mockResolvedValue({
      error: 'Registro incorrecto',
    } as RegisterResponse);
    // Make the form invalid at server side by setting the email to an invalid value
    controls.email.value = 'error@sample.com';
    controls.email.dispatchEvent(new Event('input'));
    controls.email.dispatchEvent(new Event('blur'));
    fixture.detectChanges();
    await fixture.whenStable();

    const button = fixture.nativeElement.querySelector('button[type="submit"]');
    button.click();
    fixture.detectChanges();
    await fixture.whenStable();

    expect(auth.registerPromise).toHaveBeenCalledWith(
      {
        ...MOCK_REGISTER,
        email: 'error@sample.com',
      },
      { delayTime: 2000 },
    );

    await vi.waitFor(() => {
      // expect(registerForm().submitting()).toBe(false);
      expect(router.navigate).toHaveBeenCalledWith(
        ['/auth', 'register', 'register-error'],
        expect.objectContaining({
          state: expect.objectContaining({
            error: 'Registro incorrecto',
          }),
        }),
      );
    });
  });
});
```

Validación del formulario

```ts
describe('Form Validation', () => {
  it('should be invalid when the form is empty', () => {
    expect(component['registerForm']().invalid()).toBe(true);
  });

  it('should disable the submit button when the form is invalid', () => {
    const button = fixture.nativeElement.querySelector('button[type="submit"]');
    expect(button.disabled).toBe(true);
  });

  it('should be valid when the required controls are filled correctly', async () => {
    controls = getControls();
    controls.email.value = 'test@example.com';
    controls.email.dispatchEvent(new Event('input'));
    controls.email.dispatchEvent(new Event('blur'));
    controls.password.value = 'password123';
    controls.password.dispatchEvent(new Event('input'));
    controls.password.dispatchEvent(new Event('blur'));
    controls.isOk.checked = true;
    controls.isOk.dispatchEvent(new Event('input'));
    controls.isOk.dispatchEvent(new Event('blur'));

    fixture.detectChanges();
    await fixture.whenStable();

    // console.log('Form Value:', component['registerForm']().value());
    expect(component['registerForm']().valid()).toBe(true);
  });
});
```

Feedback de errores en controles no opcionales

```ts
describe('Form Errors feedback in fixed controls', () => {
  it('should show error messages when the email is invalid and touched', async () => {
    controls = getControls();
    controls.email.value = 'invalid-email';
    controls.email.dispatchEvent(new Event('input'));
    controls.email.dispatchEvent(new Event('blur')); // Trigger blur event to mark the field as touched
    fixture.detectChanges();
    await fixture.whenStable();

    const emailError = fixture.nativeElement.querySelector('[aria-label="Email"] .error');
    expect(emailError).toBeTruthy();
    expect(emailError.textContent).toContain('El email no es válido');
  });

  it('should show error messages when the password is too short and touched', async () => {
    controls = getControls();
    controls.password.value = '123';
    controls.password.dispatchEvent(new Event('input'));
    controls.password.dispatchEvent(new Event('blur')); // Trigger blur event to mark the field as touched
    fixture.detectChanges();
    await fixture.whenStable();

    const passwordError = fixture.nativeElement.querySelector('[aria-label="Password"] .error');
    expect(passwordError).toBeTruthy();
    expect(passwordError.textContent).toContain('La contraseña debe tener al menos 6 caracteres');
  });

  it('should show error messages when the terms checkbox is not checked and touched', async () => {
    controls = getControls();
    controls.isOk.checked = false;
    controls.isOk.dispatchEvent(new Event('input'));
    controls.isOk.dispatchEvent(new Event('blur')); // Trigger blur event to mark the field as touched
    fixture.detectChanges();
    await fixture.whenStable();

    const isOkError = fixture.nativeElement.querySelector('[aria-label="Terms"] .error');
    expect(isOkError).toBeTruthy();
    expect(isOkError.textContent).toContain('Debes aceptar los términos y condiciones');
  });
});
```

Feedback de errores en controles opcionales

```ts
describe('Form Errors feedback in optional controls', () => {
  beforeEach(() => {
    controls = getControls();
    controls.hasCourse.checked = true;
    controls.hasCourse.dispatchEvent(new Event('input'));
    fixture.detectChanges();
  });

  it('should show error messages when the first name is empty and touched', async () => {
    controls = {
      ...controls,
      ...getOptionalControls(),
    };
    controls.firstName!.value = '';
    controls.firstName!.dispatchEvent(new Event('input'));
    controls.firstName!.dispatchEvent(new Event('blur')); // Trigger blur event to mark the field as touched
    fixture.detectChanges();
    await fixture.whenStable();

    const firstNameError = fixture.nativeElement.querySelector(
      '[aria-label="First Name"] .error',
    );
    expect(firstNameError).toBeTruthy();
    expect(firstNameError.textContent).toContain('El nombre es obligatorio');
  });

  it('should show error messages when the surname is empty and touched', async () => {
    controls = {
      ...controls,
      ...getOptionalControls(),
    };
    controls.surname!.value = '';
    controls.surname!.dispatchEvent(new Event('input'));
    controls.surname!.dispatchEvent(new Event('blur')); // Trigger blur event to mark the field as touched
    fixture.detectChanges();
    await fixture.whenStable();

    const surnameError = fixture.nativeElement.querySelector('[aria-label="Surname"] .error');
    expect(surnameError).toBeTruthy();
    expect(surnameError.textContent).toContain('El apellido es obligatorio');
  });

  it('should show error messages when the course is not selected and touched', async () => {
    controls = {
      ...controls,
      ...getOptionalControls(),
    };
    controls.course!.value = '';
    controls.course!.dispatchEvent(new Event('change'));
    controls.course!.dispatchEvent(new Event('blur')); // Trigger blur event to mark the field as touched
    fixture.detectChanges();
    await fixture.whenStable();

    const courseError = fixture.nativeElement.querySelector('[aria-label="Select Course"] .error');
    expect(courseError).toBeTruthy();
    expect(courseError.textContent).toContain('El curso es obligatorio');
  });
});
```


### Custom Controls y selección de opciones

En el formulario de registro vemos que se repiten los inputs y los checkboxes, e incluso los radio buttons y los select, tienen un templete definido con el que podría pasar lo mismo.

En todos los casos podemos crear **componentes personalizados** para cada uno de estos controles, que nos permitan reutilizarlos en otros formularios, y que nos permitan encapsular la lógica de validación y feedback de cada control. De esta forma estaremos creando un sistema de diseño (System Design) propio, que nos permitirá mantener la consistencia visual y funcional de nuestra aplicación.

#### 🧿Custom RadioButtons

Al crear un componente custom para los radiobutton siguiendo el mismo planteamiento que en casos anteriores, es fácil solucionar el problema en nuestro modelo del formulario, donde hemos definido la propiedad que usará el control de radio buttons como un objeto (`gender: Gender`), mientras que el elemento HTML nativo de tipo radio button solo devuelve el valor de la opción seleccionada, y no el objeto completo. 

Como antes, debemos crear un componente que implemente el interface `FormValueControl<T>`, donde T corresponderá al tipo de datos maestros que manejen las opciones del radio button.

En consecuencia, el componente reciba como inputs 
- el texto que debe mostrar (label), 
- un array de opciones
- el FieldState del control que representa el radio button, que es recogido por la directiva `FormField` y que nos permite acceder al valor del control, a su estado de validación y a sus errores.

```ts
@Component({
  ...
})
export class RadioButtons<T extends { key: string; message: string }> implements FormValueControl<T> {
 
  readonly value = model.required<T>();

  readonly errors = input([] as readonly ValidationError[]);
  readonly invalid: InputSignal<boolean> = input(false);
  readonly touched = model(false);

  readonly legend = input.required<string>();
  readonly options = input.required<T[]>();
  readonly id = computed<string>(() => this.#generateId());

  #generateId() {
    return `radio-${Math.random().toString(36).slice(2, 9)}`;
  }
```

El uso del tipado genérico nos permite crear un componente que pueda ser utilizado con cualquier objeto que tenga las propiedades `key` y `message`, como en nuestro caso los objetos `Gender` y `Course`.

En el template, iteramos sobre las opciones y creamos un input de tipo radio para cada una de ellas, con su correspondiente label.

Como en otros controles personalizados, usamos el binding de `[checked]` con la expresión `value().key === item.key`  y el evento `(change)` para actualizar el valor del la signalModel `value()` cuando se selecciona una opción.

```html
<fieldset>
  <legend>{{ legend() }}</legend>
  @for (item of options(); track $index) {
    <label [for]="'gender-' + item.key" class="form-control checkbox">
      <input
        type="radio"
        [id]="'radio-' + item.key"
        [checked]="value().key === item.key"
        (change)="value.set(item)"
        (blur)="touched.set(true)"
        [value]="item.key"
      />
      <span>{{ item.message | titlecase }}</span>
    </label>
  }
</fieldset>
@if (invalid() && touched()) {
  <p class="error">{{ errors()?.[0]?.message }}</p>
}
```

El componente necesita dos pequeños ajustes:

- marcarlo touched cuando se pierde el foco, para que se muestren los errores de validación cuando el usuario interactúa con el control
- mostrar los errores de validación gracias a la vinculación que hace internamente la directiva FormField, entre los inputs del componente y los estados en el FieldState, y que nos permite acceder a los errores de validación como `errors()` o 

El problema esta en que no podemos acceder a los errores de validación como `errorSummary()`, lo que implicará un cambio en la validación cuando usemos el control, como veremos en el test del componente.

El css proporciona a los radio buttons aspecto similar al de botones.

```css
fieldset {
  border: 1px solid #ccc;
  padding: 1rem;
  margin-bottom: 1rem;
  border-radius: 0.5rem;
  display: flex;
  gap: 1rem;
  flex-wrap: wrap;
  justify-content: space-around;
}

legend {
  font-weight: bold;
  margin-bottom: 0.5rem;
}

label {
  appearance: none;
  -webkit-appearance: none;
  -moz-appearance: none;

  span {
    color: color-mix(in srgb, var(--color-primary) 85%, transparent);
    background-color: color-mix(in srgb, var(--color-primary) 25%, transparent);
    border: 2px solid rgba(0, 0, 0, 0.2);
    border-radius: 10%;
    padding: 0.5rem;
    cursor: pointer;
  }

  input {
    opacity: 0;
    position: absolute;
    right: 0;
    &:checked + span {
      background: var(--color-secondary);
      border-color: var(--color-tertiary-hot);
      color: var(--color-background);
    }
  }
}

.error {
  color: var(--color-tertiary);
  font-size: 0.8rem;
}
```

#### Validaciones personalizadas

Entre las opciones ya mencionadas para testar un control personalizado, vamos a optar por añadir un componente de prueba que utilice el componente `RadioButtons`, y que nos permita tester su comportamiento en un formulario, y su integración con la directiva `FormField`.

Este enfoque, además de su sencillez, nos permite documenter la forma de uso de nuestro control personalizado, que implicará
crear un esquema de validación personalizado para el control. Así podremos comprobar que los errores de validación se muestran correctamente en el template.

```ts
interface RadioOption { key: string; message: string }

interface MockModel {
  name: { key: string; message: string };
}

@Component({
  selector: 'alc-test-host',
  imports: [RadioButtons, FormField],
  template: `
    <form>
      <alc-radio-buttons
        [legend]="'Testing radio buttons'"
        [options]="radioOptions"
        [formField]="fieldTree.name"
      />
    </form>
  `,
})
class TestHostComponent {
  mockModel = signal<MockModel>({
    name: { key: '', message: '' },
  });
  schema = (path: SchemaPathTree<MockModel>) => {
     validate(path.name, ({ value }) => {
      if (value().key === '') {
        return {
          kind: 'required',
          message: 'Name is required',
        };
      }
      return null;
    });
  };

  radioOptions: RadioOption[] = [
    { key: 'option1', message: 'Option 1' },
    { key: 'option2', message: 'Option 2' },
    { key: 'option3', message: 'Option 3' },
  ];

  fieldTree = form(this.mockModel, this.schema);
}
```
Como vemos, al preparar el componente host para el test, utilizamos un esquema diferente, con una **validación personalizada**.

- importamos `validate` desde '@angular/forms/signals';
- su segundo parámetro es la función de validación, que recibe un objeto con la propiedad `value`, que es un signal que representa el valor del control, y que nos permite acceder a su valor actual mediante `value()`;
- si el valor del control es inválido, devolvemos un objeto con la propiedad `kind` y `message`, que representa el tipo de error y el mensaje de error correspondiente;
- si el valor del control es válido, devolvemos `null`.

De esta forma, los errores de validación se almacenan en el FieldState del control, y podemos acceder a ellos mediante `errors()` en lugar de necesitar `errorSummary()`.

##### 👁️‍🗨️Test del componente RadioButtons

Partiendo del componente host que acabamos de describir, la preparación de nuestro test incluirá su montaje en TestBed, para luego acceder tanto al host como al componente RadioButtons

```ts
describe('TestHostComponent with RadioButtons', () => {
  let component: TestHostComponent;
  let fixture: ComponentFixture<TestHostComponent>;
  let radioButtonsComponent: RadioButtons<RadioOption>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      imports: [TestHostComponent],
    }).compileComponents();


    fixture = TestBed.createComponent(TestHostComponent);
    component = fixture.componentInstance;
    radioButtonsComponent = fixture.debugElement.query(By.directive(RadioButtons<RadioOption>)).componentInstance;
    await fixture.whenStable();
  });
  //...
});
```

En cuanto a los casos de prueba, podemos comprobar que inicialmente

- el componente renderiza correctamente la leyenda, 
- las opciones se muestran
- el estado es correcto: invalido y no touched, y que por tanto no se muestran errores de validación.

```ts
it('should have initially the correct legend', () => {
  const legendElement: HTMLLegendElement = fixture.nativeElement.querySelector('legend');
  expect(legendElement.textContent).toBe('Testing radio buttons');
});


it('should have the correct radio option labels', () => {
  const labelElements: NodeListOf<HTMLLabelElement> = fixture.nativeElement.querySelectorAll('label');
  expect(labelElements[0].textContent).toContain('Option 1');
  expect(labelElements[1].textContent).toContain('Option 2');
  expect(labelElements[2].textContent).toContain('Option 3');
});

it('should have the correct initial state', () => {
  expect(radioButtonsComponent.invalid()).toBe(true);
  expect(radioButtonsComponent.touched()).toBe(false); // Not touched initially
  const errorElement: HTMLParagraphElement = fixture.nativeElement.querySelector('.error');
  expect(errorElement).toBeNull();
});
```

Igualmente probaremos el funcionamiento del control

- que se selecciona una opción al hacer click, cambiando el value del componente y su estado
- que los errores de validación se muestran cuando el control es inválido y touched.

```ts
  it('should select the correct radio option when clicked and change it', () => {
    const radioInputs: NodeListOf<HTMLInputElement> =
      fixture.nativeElement.querySelectorAll('input[type="radio"]');
    radioInputs[1].click();
    fixture.detectChanges();
    expect(radioButtonsComponent.value()).toEqual({ key: 'option2', message: 'Option 2' });
    radioInputs[2].click();
    radioInputs[2].dispatchEvent(new Event('blur')); // Trigger blur event to mark the field as touched
    fixture.detectChanges();
    expect(radioButtonsComponent.value()).toEqual({ key: 'option3', message: 'Option 3' });
    expect(radioButtonsComponent.invalid()).toBe(false);
    expect(radioButtonsComponent.touched()).toBe(true); // Should be touched after interaction
  });

  it('should display an error message when no option is selected and the field is touched', () => {
    expect(radioButtonsComponent.invalid()).toBe(true);
    const radioInputs: NodeListOf<HTMLInputElement> =
      fixture.nativeElement.querySelectorAll('input[type="radio"]');
    radioInputs[0].dispatchEvent(new Event('blur')); // Trigger blur event to mark the field as touched
    fixture.detectChanges();

    const errorElement: HTMLParagraphElement = fixture.nativeElement.querySelector('.error');
    expect(errorElement).toBeTruthy();
    expect(errorElement.textContent).toBe('Name is required');
  });
  ```

#### 🧿Custom Select

La situación es prácticamente la misma que en los radiobutton.

El componente es genérico y recibe los mismos inputs

```ts
@Component({
  ...
})
export class Select<T extends { key: string; message: string }> implements FormValueControl<T> {
  readonly value = model.required<T>();

  readonly errors = input([] as readonly ValidationError[]);
  readonly invalid: InputSignal<boolean> = input(false);
  readonly touched = model(false);

  readonly label = input.required<string>();
  readonly options = input.required<T[]>();
  readonly id = computed<string>(() => this.#generateId());

  #generateId() {
    return `select-${Math.random().toString(36).slice(2, 9)}`;
  }

```

El binding en el template es muy similar al de los radio buttons, utilizando en este caso 

- el binding de `[value]` con la propiedad `key` del signalModel `value`, que recibe su valor del FieldState gracias a la directiva `FormField`, 


```html
<label for="select" class="form-control">
  <span>{{ label() }}</span>
  <select 
    [id]="id()" 
    [value]="value().key" 
    (change)="onSelectChange($event.target)"
    (blur)="touched.set(true)"
  >
    <option [value]=""></option>
    @for (option of options(); track $index) {
      <option [value]="option.key">{{ option.message }}</option>
    }
  </select>
</label>
@if (invalid() && touched()) {
  <p class="error">
    {{ errors()?.[0]?.message }}
  </p>
}
```

- el binding del evento `(change)` para actualizar el valor del `value` cuando se selecciona una opción, debe hacerse en un método, porque necesitamos incluir un casting de tipos y algo de lógica.

```ts
protected onSelectChange(target: EventTarget | null) {
  const index = (target as HTMLSelectElement).selectedIndex;
  const selectedOption =
    this.options()[index - 1] ?? ({ key: '', message: '' } as T);
  this.value.set(selectedOption);
}
```

De nuevo encontramos las dos particularidades mencionadas en los radio buttons

- marcarlo touched cuando se pierde el foco, para que se muestren los errores de validación cuando el usuario interactúa con el control
- mostrar los errores de validación como `errors()`, habiendo definido una validación personalizada que tenga en cuentea que el valor que se debe validar es un objeto.

El css alinea los selects con el aspecto de los inputs

```css
.form-control {
  padding-block-start: 1rem;
  display: flex;
  flex-direction: column;
  gap: 0.5rem;
}
select {
  padding: 0.5rem;
  font-size: 1rem;
  color: var(--color-primary-hot);
  background-color: var(--color-background-primary);
  border: none;
  border-block-end: 2px solid var(--color-primary);
  border-radius: 4px;
  -webkit-appearance: none;
  -ms-appearance: none;
  -moz-appearance: none;
  appearance: none;

  &:focus-visible {
    outline: var(--color-primary) auto 1px;
    background-color: var(--color-background);
  }
}

span {
  font-size: 0.8rem;
  color: var(--color-primary);
  margin-bottom: 0.2rem;
  position: relative;
  top: -2rem;
}

select:focus-visible + span,
select.is-valid + span {
  color: var(--color-primary-hot);
  top: -4rem;
}

.error {
  color: var(--color-tertiary);
  font-size: 0.8rem;
}
```

##### 👁️‍🗨️Test del componente Select

Seguimos el mismo patrón que en el componente RadioButton, con un test prácticamente igual

- se crea un componente host que utiliza el componente Select, con un esquema de validación personalizado
- se prepara el test con TestBed, para acceder al host y al componente Select 
- se comprueba que inicialmente el componente renderiza correctamente la leyenda, las opciones se muestran y el estado es correcto: invalido y no touched, y que por tanto no se muestran errores de validación.
- se prueba el funcionamiento del control, que se selecciona una opción al hacer click, cambiando el value del componente y su estado, y que los errores de validación se muestran cuando el control es inválido y touched.

```ts
interface SelectOption {
  key: string;
  message: string;
}

interface MockModel {
  name: { key: string; message: string };
}

@Component({
  selector: 'alc-test-host',
  imports: [Select, FormField],
  template: `
    <form>
      <alc-select
        [label]="'Testing select'"
        [options]="selectOptions"
        [formField]="fieldTree.name"
      />
    </form>
  `,
})
class TestHostComponent {
  mockModel = signal<MockModel>({
    name: { key: '', message: '' },
  });
  schema = (path: SchemaPathTree<MockModel>) => {
    validate(path.name, ({ value }) => {
      if (value().key === '') {
        return {
          kind: 'required',
          message: 'Name is required',
        };
      }
      return null;
    });
  };

  selectOptions: SelectOption[] = [
    { key: 'option1', message: 'Option 1' },
    { key: 'option2', message: 'Option 2' },
    { key: 'option3', message: 'Option 3' },
  ];

  fieldTree = form(this.mockModel, this.schema);
}
describe('TestHostComponent with Select', () => {
  let component: TestHostComponent;
  let fixture: ComponentFixture<TestHostComponent>;
  let selectComponent: Select<SelectOption>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      imports: [TestHostComponent],
    }).compileComponents();

    fixture = TestBed.createComponent(TestHostComponent);
    component = fixture.componentInstance;
    selectComponent = fixture.debugElement.query(
      By.directive(Select<SelectOption>),
    ).componentInstance;
    await fixture.whenStable();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
    expect(selectComponent).toBeTruthy();
  });

  describe('Initial render and state', () => {
    it('should have the correct label', () => {
      const labelElement: HTMLLabelElement = fixture.nativeElement.querySelector('label span');
      expect(labelElement.textContent).toBe('Testing select');
    });

    it('should have the correct options', () => {
      const optionElements: NodeListOf<HTMLOptionElement> =
        fixture.nativeElement.querySelectorAll('option');
      expect(optionElements.length).toBe(4); // 3 options + 1 default option
      expect(optionElements[1].textContent).toBe('Option 1');
      expect(optionElements[2].textContent).toBe('Option 2');
      expect(optionElements[3].textContent).toBe('Option 3');
    });

    it('should have initial state invalid with no option selected', () => {
      const selectElement: HTMLSelectElement = fixture.nativeElement.querySelector('select');
      expect(selectElement.value).toBe(''); // No option selected initially
      expect(selectComponent.invalid()).toBe(true); // Invalid because no option is selected
      expect(selectComponent.touched()).toBe(false); // Not touched initially
      const errorElement: HTMLParagraphElement = fixture.nativeElement.querySelector('.error');
      expect(errorElement).toBeNull();
    });
  });

  describe('Interaction and validation', () => {
    it('should update the model when an option is selected', () => {
      const selectElement: HTMLSelectElement = fixture.nativeElement.querySelector('select');
      selectElement.selectedIndex = 2; // Select "Option 2"
      selectElement.dispatchEvent(new Event('change'));
      selectElement.dispatchEvent(new Event('blur')); // Trigger blur event to mark the field as touched
      fixture.detectChanges();
      
      expect(selectComponent.value()).toEqual({ key: 'option2', message: 'Option 2' });
      expect(component.mockModel().name.key).toBe('option2');
      expect(component.mockModel().name.message).toBe('Option 2');
      expect(selectComponent.invalid()).toBe(false);
    });

    it('should display an error message when the field is invalid and touched', async () => {
      const selectElement: HTMLSelectElement = fixture.nativeElement.querySelector('select');
      expect(selectComponent.invalid()).toBe(true);
      selectElement.dispatchEvent(new Event('change'));
      selectElement.dispatchEvent(new Event('blur')); // Trigger blur event to mark the field as touched
      fixture.detectChanges();

      const errorElement: HTMLParagraphElement = fixture.nativeElement.querySelector('.error');
      expect(errorElement).toBeTruthy();
      expect(errorElement.textContent).toContain('Name is required');
    });
  });
});
```

### 🧿RegisterFormCustom usando los controles personalizados 

Tal como ya tenemos previsto en el diseño de la página de registro, podemos incluir un segundo componente que utilice todos los controles personalizados que hemos creado, y que nos permita comparar su funcionamiento con el formulario original.
 
```html 
<form [formRoot]="registerForm">
  <alc-input aria-label="Email" type="email"
    [label]="'Email'"
    [formField]="registerForm.email"
  />
  <alc-input aria-label="Password" type="password"
    [label]="'Password'"
    [formField]="registerForm.password"
  />
  <alc-radio-buttons aria-label="Gender"
    [options]="genders()"
    [legend]="'Gender'"
    [formField]="registerForm.gender"
  />
  <alc-check-box aria-label="Terms"
    [label]="'Acepto los términos ...'"
    [formField]="registerForm.isOk"
  />
  <div aria-label="Has Course">
    <alc-check-box
      [label]="'Haz echo algún curso con nosotros'"
      [formField]="registerForm.hasCourse"
    />
  </div>
  @if (registerForm.hasCourse().value()) {
    <alc-input aria-label="First Name" type="text"
      [label]="'First Name'"
      [formField]="registerForm.firstName"
    />
    <alc-input aria-label="Surname" type="text"
      [label]="'Surname'"
      [formField]="registerForm.surname"
    />
    <alc-select aria-label="Course"
      [options]="courses()"
      [label]="'Selecciona un curso'"
      [formField]="registerForm.course"
    />
  }
  <div aria-label="Buttons" class="form-control">
    <alc-button
      [label]="'Register'"
      [disabled]="registerForm().invalid()"
      [sending]="registerForm().submitting()"
    />
  </div>
</form>

<div aria-label="Form Value" class="form-value">
  <p>Valor del formulario</p>
  <pre>{{ registerForm().value() | json }}</pre>
  <p>Valor de errores activos del formulario</p>
  <pre>{{ registerForm().errorSummary() | json }}</pre>
  <p>Errores activos del formulario</p>
  <ul class="error">
    @for (error of registerForm().errorSummary(); track $index) {
      <li>{{ error.message }}</li>
    }
  </ul>
</div>
```

La lógica del componente también es muy similar a la del formulario original

- creamos un objeto con los valores iniciales del formulario, que incluye los campos opcionales
- creamos un signal con el objeto del formulario, que será el que se pase al form
- definimos un esquema de validación para los campos obligatorios, y otro para los campos opcionales, que se aplicará solo cuando el checkbox `hasCourse` esté marcado
- definimos el form con el signal del formulario, el esquema de validación y la acción de envío del formulario.

En los controles de selección multiple, como los radio buttons y el select, debemos tener en cuenta que el valor del control es un objeto, y no un string, por lo que la validación personalizada debe comprobar la propiedad `key` del objeto para determinar si el valor es válido o no.

```ts
export class RegisterFormCustom {
  readonly #router = inject(Router);
  readonly #auth = inject(Auth);

  readonly #initialValues: RegisterModel = {
    email: '',
    password: '',
    firstName: '',
    surname: '',
    gender: { key: '', message: '' },
    isOk: false,
    hasCourse: false,
    course: { key: '', message: '' },
  };

  protected readonly genders = signal(GENDERS);
  protected readonly courses = signal(COURSES);

  protected readonly user = signal<RegisterModel>(this.#initialValues);

  readonly #optionalsSchema = (path: SchemaPathTree<RegisterModel>) => {
    required(path.firstName, { message: 'El nombre es obligatorio' });
    required(path.surname, { message: 'El apellido es obligatorio' });
    validate(path.course, ({ value }) => {
      if (value().key === '') {
        return {
          kind: 'required',
          message: 'El curso es obligatorio',
        };
      }
      return null;
    });
  };

  readonly #registerFormSchema = (path: SchemaPathTree<RegisterModel>) => {
    required(path.email, { message: 'El email es obligatorio' });
    email(path.email, { message: 'El email no es válido' });
    required(path.password, { message: 'La contraseña es obligatoria' });
    minLength(path.password, 6, { message: 'La contraseña debe tener al menos 6 caracteres' });
    validate(path.gender, ({ value }) => {
      if (value().key === '') {
        return {
          kind: 'required',
          message: 'Indicar el género es obligatorio',
        };
      }
      return null;
    });
    required(path.isOk, { message: 'Debes aceptar los términos y condiciones' });
  };

  protected readonly registerForm = form(
    this.user,
    (schema) => {
      this.#registerFormSchema(schema);
      applyWhen(
        schema,
        ({ valueOf }) => valueOf(schema.hasCourse) === true,
        (schema) => this.#optionalsSchema(schema),
      );
    },
    {
      submission: {
        action: this.submitForm.bind(this),
      },
    },
  );
```

El submit no varía en nada respecto al formulario original

```ts
protected async submitForm(registerFieldTree: FieldTree<RegisterModel>) {
  const formData = registerFieldTree().value();
  const state = await this.#auth.registerPromise(formData, { delayTime: 2000 });

  console.log('Form submitted:', state);
  registerFieldTree().reset(this.#initialValues);

  if (state.error) {
    await this.#router.navigate(['/auth', 'register', 'register-error'], {
      state,
    });
  } else {
    await this.#router.navigate(['/auth', 'login', 'after-register']);
  }
}
```

#### 👁️‍🗨️Test del componente RegisterFormCustom

Como hemos visto varias veces, al intentar que los test se centren en el funcionamiento del componente, y no en la implementación de los controles personalizados, podemos utilizar un test similar al del formulario anterior.

Uno de los cambios es la localización de los elementos del DOM, que en este caso utilizará el atributo `aria-label` para asignado a cada uno de los controles personalizados utilizados como elementos del formulario, ya que no tienen un atributo `name` que podamos utilizar porque este es incompatible con la directiva `formField`.

Por lo demás la preparación de los test es practicamente la misma

```ts

interface FormControls {
  email: HTMLInputElement;
  password: HTMLInputElement;
  gender: HTMLInputElement[];
  isOk: HTMLInputElement;
  hasCourse: HTMLInputElement;
  firstName?: HTMLInputElement;
  surname?: HTMLInputElement;
  course?: HTMLSelectElement;
}

// interface OptionalsControls {
//   firstName: HTMLInputElement;
//   surname: HTMLInputElement;
//   course: HTMLSelectElement;
// }

const MOCK_REGISTER: RegisterModel = {
  email: 'test@example.com',
  password: 'password123',
  firstName: 'Pepe',
  surname: 'Pérez',
  gender: { key: 'o', message: 'otro' },
  isOk: true,
  hasCourse: true,
  course: { key: 'a', message: 'Angular' },
};

describe('RegisterFormCustom', () => {
  let component: RegisterFormCustom;
  let fixture: ComponentFixture<RegisterFormCustom>;

  let controls: FormControls;
  let router: Router;
  let auth: Auth;

  const getControls = () => {
    return {
      email: fixture.nativeElement.querySelector('[aria-label="Email"] input'),
      password: fixture.nativeElement.querySelector('[aria-label="Password"] input'),
      gender: fixture.nativeElement.querySelectorAll('[aria-label="Gender"] input'),
      hasCourse: fixture.nativeElement.querySelector('[aria-label="Has Course"] input'),
      isOk: fixture.nativeElement.querySelector('[aria-label="Terms"] input'),
    };
  };

  const getOptionalControls = () => {
    return {
      firstName: fixture.nativeElement.querySelector('[aria-label="First Name"] input'),
      surname: fixture.nativeElement.querySelector('[aria-label="Surname"] input'),
      course: fixture.nativeElement.querySelector('[aria-label="Course"] select'),
    };
  };

  const completeFixedControls = () => {
    controls = getControls();
    controls.email.value = MOCK_REGISTER.email;
    controls.email.dispatchEvent(new Event('input'));
    controls.email.dispatchEvent(new Event('blur'));
    controls.password.value = MOCK_REGISTER.password;
    controls.password.dispatchEvent(new Event('input'));
    controls.password.dispatchEvent(new Event('blur'));
    controls.gender[2].checked = true; // Selecting 'otro'
    controls.gender[2].dispatchEvent(new Event('change'));
    controls.gender[2].dispatchEvent(new Event('blur'));
    controls.isOk.checked = MOCK_REGISTER.isOk;
    controls.isOk.dispatchEvent(new Event('change'));
    controls.isOk.dispatchEvent(new Event('blur'));
    controls.hasCourse.checked = MOCK_REGISTER.hasCourse;
    controls.hasCourse.dispatchEvent(new Event('change'));
    controls.hasCourse.dispatchEvent(new Event('blur'));
  };

  const completeOptionalControls = () => {
    controls = {
      ...controls,
      ...getOptionalControls(),
    };
    console.log('Controls:', controls);
    controls.firstName!.value = MOCK_REGISTER.firstName;
    controls.firstName!.dispatchEvent(new Event('input'));
    controls.firstName!.dispatchEvent(new Event('blur'));
    controls.surname!.value = MOCK_REGISTER.surname;
    controls.surname!.dispatchEvent(new Event('input'));
    controls.surname!.dispatchEvent(new Event('blur'));
    controls.course!.value = MOCK_REGISTER.course.key;
    controls.course!.dispatchEvent(new Event('change'));
  };

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      imports: [RegisterFormCustom],
      providers: [
        {
          provide: Auth,
          useValue: {
            registerPromise: vi.fn().mockReturnValue({}),
          },
        },
      ],
    }).compileComponents();

    fixture = TestBed.createComponent(RegisterFormCustom);
    component = fixture.componentInstance;
    router = TestBed.inject(Router);
    auth = TestBed.inject(Auth);
    await fixture.whenStable();
  });
```

Y lo mismo sucede con los casos de uso, que como vimos son muy numerosos, como suele siuceder si se intenta tetar de forma  exhaustiva un formulario.

```ts
  describe('Form Rendered', () => {
    it('should have a form with the non optional controls', () => {
      controls = getControls();
      expect(controls.email).toBeTruthy();
      expect(controls.password).toBeTruthy();
      expect(controls.gender.length).toBe(4);
      controls.gender.forEach((genderControl) => {
        expect(genderControl).toBeTruthy();
      });
      expect(controls.isOk).toBeTruthy();
      expect(controls.hasCourse).toBeTruthy();
    });

    it('should have the optional controls when needed, not when not needed', () => {
      controls = getControls();
      controls.hasCourse.click();
      fixture.detectChanges();
      controls = {
        ...controls,
        ...getOptionalControls(),
      };
      expect(controls.firstName).toBeTruthy();
      expect(controls.surname).toBeTruthy();
      expect(controls.course).toBeTruthy();
      controls.hasCourse.click();
      fixture.detectChanges();
      controls = {
        ...controls,
        ...getOptionalControls(),
      };
      expect(controls.firstName).toBeFalsy();
      expect(controls.surname).toBeFalsy();
      expect(controls.course).toBeFalsy();
    });
  });

  describe('Form Value', () => {
    it('should have the correct initial values', () => {
      expect(component['registerForm']().value()).toEqual({
        email: '',
        password: '',
        firstName: '',
        surname: '',
        gender: { key: '', message: '' },
        isOk: false,
        hasCourse: false,
        course: { key: '', message: '' },
      });
    });

    it('should update the form value when controls are changed', async () => {
      completeFixedControls();
      fixture.detectChanges();
      await fixture.whenStable();
      expect(component['registerForm']().value().hasCourse).toEqual(true);
      completeOptionalControls();
      fixture.detectChanges();
      await fixture.whenStable();
      expect(component['registerForm']().value()).toEqual(MOCK_REGISTER);
    });
  });

  describe('Form Submission with the full values', () => {
    beforeEach(async () => {
      vi.spyOn(router, 'navigate').mockResolvedValue(true);

      completeFixedControls();
      fixture.detectChanges();
      await fixture.whenStable();
      completeOptionalControls();
      fixture.detectChanges();
      await fixture.whenStable();
    });

    it('should be registered successfully by the service ', async () => {
      vi.mocked(auth.registerPromise).mockResolvedValue({
        error: '',
        info: {},
      } as RegisterResponse);
      const registerForm = component['registerForm'];
      const button = fixture.nativeElement.querySelector('button[type="submit"]');
      button.click();
      fixture.detectChanges();
      await fixture.whenStable();

      expect(auth.registerPromise).toHaveBeenCalledWith(MOCK_REGISTER, {
        delayTime: 2000,
      });

      expect(registerForm().submitting()).toBe(true);

      // El servicio register es asíncrono aunque se usa un mock,
      // por lo que debemos esperar a que se complete antes de hacer la aserción.

      await vi.waitFor(() => {
        expect(registerForm().submitting()).toBe(false);
        expect(router.navigate).toHaveBeenCalledWith(['/auth', 'login', 'after-register']);
      });

      // Después del submit y el reset del formulario
      expect(component['registerForm']().value()).toEqual({
        email: '',
        password: '',
        firstName: '',
        surname: '',
        gender: { key: '', message: '' },
        isOk: false,
        hasCourse: false,
        course: { key: '', message: '' },
      });
    });

    it('should show and disable the submit button while registration is in progress', async () => {
      vi.mocked(auth.registerPromise).mockResolvedValue({
        info: {},
      } as RegisterResponse);
      vi.mocked(router.navigate).mockResolvedValue(true);

      const button: HTMLButtonElement =
        fixture.nativeElement.querySelector('button[type="submit"]');
      button.click();
      fixture.detectChanges();

      expect(component['registerForm']().submitting()).toBe(true);
      expect(button.disabled).toBe(true);
      expect(button.textContent?.trim()).toBe('Sending...');

      await vi.waitFor(() => {
        expect(component['registerForm']().submitting()).toBe(false);
        // Se recupera elñ texto del botón
        expect(button.textContent?.trim()).toBe('Register');
        // Se resetea el formulario, por lo que el botón quede disabled
        expect(button.disabled).toBe(true);
      });
    });

    it('should NOT register when the form is SERVER SIDE invalid', async () => {
      // Make the form invalid at server side
      vi.mocked(auth.registerPromise).mockResolvedValue({
        error: 'Registro incorrecto',
      } as RegisterResponse);
      // Make the form invalid at server side by setting the email to an invalid value
      controls.email.value = 'error@sample.com';
      controls.email.dispatchEvent(new Event('input'));
      controls.email.dispatchEvent(new Event('blur'));
      fixture.detectChanges();
      await fixture.whenStable();

      const button = fixture.nativeElement.querySelector('button[type="submit"]');
      button.click();
      fixture.detectChanges();
      await fixture.whenStable();

      expect(auth.registerPromise).toHaveBeenCalledWith(
        {
          ...MOCK_REGISTER,
          email: 'error@sample.com',
        },
        { delayTime: 2000 },
      );

      await vi.waitFor(() => {
        expect(component['registerForm']().submitting()).toBe(false);
        expect(router.navigate).toHaveBeenCalledWith(
          ['/auth', 'register', 'register-error'],
          expect.objectContaining({
            state: expect.objectContaining({
              error: 'Registro incorrecto',
            }),
          }),
        );
      });
    });
  });

  describe('Form Validation', () => {
    it('should be invalid when the form is empty', () => {
      expect(component['registerForm']().invalid()).toBe(true);
    });

    it('should disable the submit button when the form is invalid', () => {
      const button = fixture.nativeElement.querySelector('button[type="submit"]');
      expect(button.disabled).toBe(true);
    });

    it('should be valid when the required controls are filled correctly', async () => {
      controls = getControls();
      controls.email.value = 'test@example.com';
      controls.email.dispatchEvent(new Event('input'));
      controls.email.dispatchEvent(new Event('blur'));
      controls.password.value = 'password123';
      controls.password.dispatchEvent(new Event('input'));
      controls.password.dispatchEvent(new Event('blur'));
      controls.gender[2].checked = true; // Selecting 'otro'
      controls.gender[2].dispatchEvent(new Event('change'));
      controls.gender[2].dispatchEvent(new Event('blur'));
      controls.isOk.checked = true;
      controls.isOk.dispatchEvent(new Event('change'));
      controls.isOk.dispatchEvent(new Event('blur'));

      fixture.detectChanges();
      await fixture.whenStable();

      expect(component['registerForm']().valid()).toBe(true);
    });
  });

  describe('Form Errors feedback in fixed controls', () => {
    it('should show error messages when the email is invalid and touched', async () => {
      controls = getControls();
      controls.email.value = 'invalid-email';
      controls.email.dispatchEvent(new Event('input'));
      controls.email.dispatchEvent(new Event('blur')); // Trigger blur event to mark the field as touched
      fixture.detectChanges();
      await fixture.whenStable();

      const emailError = fixture.nativeElement.querySelector('[aria-label="Email"] .error');
      expect(emailError).toBeTruthy();
      expect(emailError.textContent).toContain('El email no es válido');
    });

    it('should show error messages when the password is too short and touched', async () => {
      controls = getControls();
      controls.password.value = '123';
      controls.password.dispatchEvent(new Event('input'));
      controls.password.dispatchEvent(new Event('blur')); // Trigger blur event to mark the field as touched
      fixture.detectChanges();
      await fixture.whenStable();

      const passwordError = fixture.nativeElement.querySelector('[aria-label="Password"] .error');
      expect(passwordError).toBeTruthy();
      expect(passwordError.textContent).toContain('La contraseña debe tener al menos 6 caracteres');
    });

    it('should show error messages when the terms checkbox is not checked and touched', async () => {
      controls = getControls();
      controls.isOk.checked = false;
      controls.isOk.dispatchEvent(new Event('input'));
      controls.isOk.dispatchEvent(new Event('blur')); // Trigger blur event to mark the field as touched
      fixture.detectChanges();
      await fixture.whenStable();

      const isOkError = fixture.nativeElement.querySelector('[aria-label="Terms"] .error');
      expect(isOkError).toBeTruthy();
      expect(isOkError.textContent).toContain('Debes aceptar los términos y condiciones');
    });
  });

  describe('Form Errors feedback in optional controls', () => {
    beforeEach(() => {
      controls = getControls();
      controls.hasCourse.checked = true;
      controls.hasCourse.dispatchEvent(new Event('change'));
      fixture.detectChanges();
    });

    it('should show error messages when the first name is empty and touched', async () => {
      controls = {
        ...controls,
        ...getOptionalControls(),
      };
      controls.firstName!.value = '';
      controls.firstName!.dispatchEvent(new Event('input'));
      controls.firstName!.dispatchEvent(new Event('blur')); // Trigger blur event to mark the field as touched
      fixture.detectChanges();
      await fixture.whenStable();

      const firstNameError = fixture.nativeElement.querySelector(
        '[aria-label="First Name"] .error',
      );
      expect(firstNameError).toBeTruthy();
      expect(firstNameError.textContent).toContain('El nombre es obligatorio');
    });

    it('should show error messages when the surname is empty and touched', async () => {
      controls = {
        ...controls,
        ...getOptionalControls(),
      };
      controls.surname!.value = '';
      controls.surname!.dispatchEvent(new Event('input'));
      controls.surname!.dispatchEvent(new Event('blur')); // Trigger blur event to mark the field as touched
      fixture.detectChanges();
      await fixture.whenStable();

      const surnameError = fixture.nativeElement.querySelector('[aria-label="Surname"] .error');
      expect(surnameError).toBeTruthy();
      expect(surnameError.textContent).toContain('El apellido es obligatorio');
    });

    it('should show error messages when the course is not selected and touched', async () => {
      controls = {
        ...controls,
        ...getOptionalControls(),
      };
      controls.course!.value = '';
      controls.course!.dispatchEvent(new Event('change'));
      controls.course!.dispatchEvent(new Event('blur')); // Trigger blur event to mark the field as touched
      fixture.detectChanges();
      await fixture.whenStable();

      const courseError = fixture.nativeElement.querySelector(
        '[aria-label="Course"] .error',
      );
      expect(courseError).toBeTruthy();
      expect(courseError.textContent).toContain('El curso es obligatorio');
    });
  });
});
```

## Extras (descartes)

### Custom Controls

#### 🧿Custom CheckBox-Free

Este componente es un wrapper del input nativo de tipo checkbox, que nos permite utilizarlo como un componente de Angular, y que nos permite enlazarlo con un signal de tipo boolean. Como no estamos pasando elementos de formulario, podríamos usar el componente libremente, fuera de un formulario.

El componente recibe dos inputs:

- `checked`: un signal de tipo boolean que representa el estado del checkbox
- `label`: un signal de string que representa el texto que se muestra al lado del checkbox

Y un output

- `checkedChange`: un output que emite un evento cuando el estado del checkbox cambia, para que el componente padre pueda actualizar su signal correspondiente.

```ts
export class  CheckBoxFree { {

  readonly checked = input.required<boolean>();
  readonly checkedChange = output<boolean>();
  readonly label = input.required<string>();
}
```

En el template, utilizamos un label que contiene el input de tipo checkbox, y un span que muestra el texto del label. 

El input está enlazado con el signal `checked` mediante la propiedad `[checked]`, y el evento `(checkedChange)` emite el evento `emitCheckedChange`, que corresponde al cambio de estado del checkbox, realizado a nivel visual en el HTML.

```html
<label class="custom-checkbox">
  <input type="checkbox" 
        [checked]="checked()" 
        (change)="checkedChange.emit(!checked())" />
  <span class="check-mark"></span>
  <span class="label-text">{{ label() }}</span>
</label>
```

##### Uso desde el formulario

En el formulario de registro, utilizamos el componente `CheckBoxFree` para el campo `hasCourse` y enlazamos su estado con el signal `registerForm.hasCourse()` mediante la propiedad `[checked]`, y el evento `(emitCheckedChange)` llama al método `updateHasCourse()` que actualiza el signal correspondiente.

```html
  <alc-check-box
    [label]="'Accept Terms ...'"
    [checked]="registerForm.hasCourse().value()"
    (emitCheckedChange)="updateHasCourse($event)"
  />
```

```ts
  protected updateHasCourse(newChecked: boolean) {
    this.registerForm.hasCourse().value.set( newChecked )
  }
```

#### Signal model()

En el caso anterior, en el componente `CheckBoxFree`,   podríamos utilizar otra primitiva de señales, `model()`, que nos permite agrupar en un solo elemento las dos direcciones de comunicación, input y output.

```ts
export class CheckBoxFree {
    // readonly checked = input.required<boolean>();
    // readonly checkedChange = output<boolean>();
  readonly checked = model.required<boolean>();
  
  readonly label = input.required<string>();
}
```

En el template, en lugar de utilizar las propiedades `[checked]` hace el binding con la propiedad igual que antes pero el binding del evento cambia. Ahora no tenemos acceso directo al output: cambiamos el valor de la signal de tipo model y esta emite automáticamente un evento de cambio, que podemos escuchar en el componente padre. Para ello solo hay que tener encuentra que el nombre del evento es el nombre de la propiedad con el sufijo `Change`, en este caso `checkedChange`.

```html 
<input type="checkbox" [checked]="checked()" (change)="checked.set(!checked())" />
```

En el formulario de registro, el binding es igual que antes, ya que el evento que escuchamos ya se denominaba `checkedChange`, que es el nombre de la propiedad con el sufijo `Change`.

```html
<div aria-label="Has Course">
  <alc-check-box-free
    [label]="'Haz echo algún curso con nosotros'"
    [checked]="registerForm.hasCourse().value()"
    (checkedChange)="updateHasCourse($event)"
  />
</div>
```

Por el momento no existe una directiva similar a `[(ngModel)]` para hacer el binding bidireccional con un signal de tipo model, pero es posible crearla, y de hecho ya existe un PR abierto en Angular para implementarla.

### Mejoras en los custom controls

Los componentes creados son solo ejemplos simples de controles personalizados. En la practica puede ser necesario tener en cuenta la posibilidad de otros atributos y eventos, que se pueden definir de forma específica.

Por el hecho de utilizar los interfaces `FormValueControl` y `FormCheckboxControl`, ya tenemos un API definido para los componentes, que nos permite utilizarlos de forma consistente y predecible.

En ella se incluyen todas las siguientes propiedades, que son opcionales, en caso de definirlas en el componente, recibirán directamente su valor a partir del FieldTree del control del formulario, que estamos vinculando gracias a la directiva `formField`.

- errors: InputSignal
- disabled: InputSignal
- disabledReason: InputSignal
- readonly: InputSignal
- hidden: InputSignal
- invalid: InputSignal
- pending: InputSignal
- touched: InputSignal
- dirty: InputSignal
- name: InputSignal
- required: InputSignal
- min: InputSignal
- minLength: InputSignal
- max: InputSignal
- maxLength: InputSignal
- pattern: InputSignal
- touch: OutputRef<void>

El interface incluye también los métodos 

- focus?(options?: FocusOptions): void 
- reset?(): void 


Al margen de estas, el API expuesto por los componentes podría incluir:

- tabIndex?: number
- autocomplete?: string
- indeterminate?: boolean
- ariaLabel?: string
- ariaDescribedBy?: string
- nativeAttrs?: Record<string, string | number | boolean | null | undefined>

La última propiedad `nativeAttrs` es un objeto que permite pasar cualquier atributo nativo de HTML al input nativo del control, para que el componente sea más flexible y se pueda utilizar en diferentes contextos.

Para usar esta propiedad necesitamos:

-  en el template, añadir una referencia al control nativo.
-  en el componente, definir el input que recibirá `nativeAttrs`
-  en el constructor, crear un effect vinculado a este input que invoque un método auxiliar
-  en el método, iterar sobre las propiedades del objeto `nativeAttrs` y añadirlas al control nativo mediante Renderer2 y su método `setAttribute()`
 
```html
<input type="checkbox" #nativeInput />
```

```ts
type NativeAttrs = Record<string, string | number | boolean | null | undefined>;

export class CheckBox {
  readonly #renderer = inject(Renderer2);
  readonly #nativeAttrs = input<NativeAttrs>({});
  readonly #nativeInput = viewChild.required<ElementRef<HTMLInputElement>>('nativeInput');

  #previousNativeAttrKeys = new Set<string>();

  constructor() {
    effect(() => {
      const inputEl = this.#nativeInput();
      if (!inputEl) {
        return;
      }

      this.applyNativeAttrs(inputEl.nativeElement, this.#nativeAttrs());
    });
  }

  private applyNativeAttrs(element: HTMLInputElement, attrs: NativeAttrs): void {
    const nextKeys = new Set(Object.keys(attrs));

    for (const key of this.#previousNativeAttrKeys) {
      if (!nextKeys.has(key)) {
        this.renderer.removeAttribute(element, key);
      }
    }

    for (const [key, value] of Object.entries(attrs)) {
      if (value === null || value === undefined || value === false) {
        this.renderer.removeAttribute(element, key);
        continue;
      }

      if (value === true) {
        this.renderer.setAttribute(element, key, '');
        continue;
      }

      this.renderer.setAttribute(element, key, String(value));
    }

    this.#previousNativeAttrKeys = nextKeys;
  }
}
```

Este sería un buen caso de uso de cuando crear un directiva propia en Angular, incorporando este código y evitando la necesidad de repetirlo en cada uno de los controles personalizados. 

#### 🧿Directiva NativeAttrs

Creamos la directiva

```shell
ng g d core/directives/native-attrs --project demo-03 
```

Incluimos la funcionalidad que podríamos haber añadido en los componentes

- creamos un tipo para los posibles atributos nativos, que es un objeto "Record" con claves de tipo string y valores de tipo string, number, boolean, null o undefined

```ts
export type NativeAttrRecord = Record<string, string | number | boolean | null | undefined>;
```

-  en el selector, limitamos el uso de la directiva a los controles de formulario

```ts
@Directive({
  selector: 'input[alcNativeAttrs], textarea[alcNativeAttrs], select[alcNativeAttrs]',
  // selector: '[alcNativeAttrs]',
})
```

- en la directiva, como suele ser habitual, inyectamos Renderer2 y ElementRef, que nos permiten acceder al elemento nativo del control y modificar sus atributos de forma segura y compatible con Angular.

- como input recibimos los atributos, de acuerdo con el tipo `NativeAttrRecord` definido anteriormente, y creamos un set para almacenar las claves de los atributos que se han aplicado previamente, para poder eliminarlos si ya no están presentes en el objeto de atributos.

```ts
export class NativeAttrs {
  readonly alcNativeAttrs = input<NativeAttrRecord>({});
  readonly #elementRef = inject(
    ElementRef<HTMLInputElement | HTMLTextAreaElement | HTMLSelectElement>,
  );
  readonly #renderer = inject(Renderer2);

  #previousKeys = new Set<string>();
}
```

-  en el constructor, crear un effect vinculado al input que invoque un método auxiliar

```ts
constructor() {
  effect(() => {
    this.applyAttrs(this.alcNativeAttrs());
  });
}
```

-  en el método, igual que anteriormente, podemos iterar sobre las propiedades del objeto `nativeAttrs` y añadirlas al control nativo mediante Renderer2 y su método `setAttribute()`

```ts
private applyAttrs(attrs: NativeAttrRecord): void {
  const element = this.#elementRef.nativeElement;
  const nextKeys = new Set(Object.keys(attrs));

  // Remove attributes that are no longer present
  for (const key of this.#previousKeys) {
    if (!nextKeys.has(key)) {
      this.#renderer.removeAttribute(element, key);
    }
  }

  // Add or update attributes based on the new values
  for (const [key, value] of Object.entries(attrs)) {
    if (value === null || value === undefined || value === false) {
      this.#renderer.removeAttribute(element, key);
      continue;
    }

    if (value === true) {
      this.#renderer.setAttribute(element, key, '');
      continue;
    }

    this.#renderer.setAttribute(element, key, String(value));
  }

  this.#previousKeys = nextKeys;
}
```

En el template de nuestros componentes personalizado, aplicamos la directiva al control nativo (input, textarea, select).

```html
<input 
  type="{{ type() }}" 
  id="{{ id() }}" 
  [value]="value()"
  (input)="value.set($event.target.value)"
  (blur)="touched.set(true)"
  placeholder=" " 
  [alcNativeAttrs]="nativeAttrs()"
/>
```

Y en el componente recogemos el objeto de atributos nativos como input.

```ts
export class Input {
  //...
    readonly nativeAttrs = input<NativeAttrRecord>({});
}
```

En el formulario que utiliza nuestro componente personalizado, podemos pasar cualquier atributo nativo que queramos aplicar al control nativo, como los atributos `aria-` que permiten mejorar la accesibilidad del formulario, o cualquier otro atributo que necesitemos.

```html
<alc-input
      aria-label="Email"
      [label]="'Email'"
      type="email"
      [formField]="registerForm.email()"
      [nativeAttrs]="{ 'aria-describedby': 'email-error' }"
    />
```



